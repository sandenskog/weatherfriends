---
phase: 05-utokade-vyer
plan: 02
type: execute
wave: 2
depends_on:
  - "05-01"
files_modified:
  - HotAndColdFriends/Features/FriendList/FriendCategoryView.swift
  - HotAndColdFriends/Features/FriendList/FriendsTabView.swift
  - HotAndColdFriends/Services/DailyWeatherNotificationService.swift
  - project.yml
autonomous: true
requirements:
  - VIEW-02
  - PUSH-02

must_haves:
  truths:
    - "Kategorier-fliken visar vänner grupperade i väderkategorier (Arctic/Cold/Cool/Warm/Tropical)"
    - "Varje kategori visas som en horisontell karusell med kort som har profilbild, namn, temperatur och SF Symbol"
    - "Tomma kategorier döljs automatiskt"
    - "Tap på väderkort öppnar WeatherDetailSheet"
    - "Kortens bakgrundsfärg matchar befintlig Color.temperatureColor"
    - "Daglig lokal notis schemaläggs kl 07:00 med favoriters vädersammanfattning"
    - "Notis skickas inte om inga favoriter finns"
    - "Omschemaläggning ersätter befintlig notis (inget dubbletter)"
  artifacts:
    - path: "HotAndColdFriends/Features/FriendList/FriendCategoryView.swift"
      provides: "Kategorivy med horisontella karuseller per väderkategori"
      contains: "WeatherCategory"
    - path: "HotAndColdFriends/Services/DailyWeatherNotificationService.swift"
      provides: "Daglig lokal notis-schemaläggning"
      contains: "DailyWeatherNotificationService"
  key_links:
    - from: "HotAndColdFriends/Features/FriendList/FriendsTabView.swift"
      to: "FriendCategoryView"
      via: "switch selectedTab case .categories"
      pattern: "FriendCategoryView"
    - from: "HotAndColdFriends/Features/FriendList/FriendsTabView.swift"
      to: "DailyWeatherNotificationService"
      via: ".task {} anropar schedule() efter data laddats"
      pattern: "dailyNotificationService.*schedule"
    - from: "HotAndColdFriends/Features/FriendList/FriendCategoryView.swift"
      to: "WeatherDetailSheet"
      via: ".sheet(item:) vid tap på kort"
      pattern: "sheet.*selectedFriendWeather"
---

<objective>
Implementera grupperade väderkort (VIEW-02) med horisontella karuseller per kategori, och daglig lokal vädersammanfattning (PUSH-02) som schemaläggs kl 07:00.

Purpose: Kategorivyn ger ett snabbt överblick av vänners väder sorterat per temperaturgrupp. Daglig notis skapar en morgonrutin kopplad till appen. Tillsammans med kartvy (Plan 05-01) ger detta tre komplementära sätt att utforska väderdata.

Output: FriendCategoryView med WeatherCategory-enum och karuseller, DailyWeatherNotificationService, uppdaterad FriendsTabView med kategorier-case.
</objective>

<execution_context>
@/Users/richardsandenskog/.claude/get-shit-done/workflows/execute-plan.md
@/Users/richardsandenskog/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-utokade-vyer/05-RESEARCH.md
@.planning/phases/05-utokade-vyer/05-01-SUMMARY.md

@HotAndColdFriends/Features/FriendList/FriendsTabView.swift
@HotAndColdFriends/Features/FriendList/FriendListView.swift
@HotAndColdFriends/Features/FriendList/FriendRowView.swift
@HotAndColdFriends/Features/FriendList/WeatherDetailSheet.swift
@HotAndColdFriends/Models/FriendWeather.swift
@HotAndColdFriends/Models/Friend.swift
@HotAndColdFriends/App/AppDelegate.swift

<interfaces>
<!-- Key types and contracts the executor needs. -->

From HotAndColdFriends/Models/FriendWeather.swift:
```swift
struct FriendWeather: Identifiable {
    let friend: Friend
    let weather: CurrentWeather?
    var id: String
    var temperatureCelsius: Double?
    var temperatureFormatted: String
    var symbolName: String
}
```

From HotAndColdFriends/Features/FriendList/FriendRowView.swift:
```swift
extension Color {
    static func temperatureColor(celsius: Double) -> Color
    // <0: isblå, 0-10: kylig blå, 10-20: neutral grön, 20-28: varm orange, 28+: het röd
}
```

From HotAndColdFriends/Features/FriendList/FriendsTabView.swift (skapad i Plan 05-01):
```swift
enum FriendsTab: String, CaseIterable {
    case list = "Lista"
    case map = "Karta"
    case categories = "Kategorier"
}
// FriendsTabView har switch selectedTab som renderar .categories-case
// FriendListViewModel ägs av FriendsTabView (favorites, others arrays)
```

From HotAndColdFriends/App/AppDelegate.swift:
```swift
// UNUserNotificationCenterDelegate redan implementerad
// registerForPushNotifications() finns med requestAuthorization([.alert, .badge, .sound])
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Skapa FriendCategoryView med WeatherCategory-enum och horisontella karuseller</name>
  <files>
    HotAndColdFriends/Features/FriendList/FriendCategoryView.swift,
    HotAndColdFriends/Features/FriendList/FriendsTabView.swift,
    project.yml
  </files>
  <action>
**1. Skapa FriendCategoryView.swift med WeatherCategory-enum:**

Definiera `WeatherCategory` enum och `FriendCategoryView` i samma fil.

```swift
enum WeatherCategory: String, CaseIterable {
    case tropical = "Tropical"
    case warm = "Warm"
    case cool = "Cool"
    case cold = "Cold"
    case arctic = "Arctic"

    var label: String { rawValue }
    var emoji: String {
        switch self {
        case .tropical: return "🔥"
        case .warm: return "☀️"
        case .cool: return "🌤"
        case .cold: return "❄️"
        case .arctic: return "🧊"
        }
    }

    static func category(for celsius: Double) -> WeatherCategory {
        switch celsius {
        case ..<0:      return .arctic
        case 0..<10:    return .cold
        case 10..<20:   return .cool
        case 20..<28:   return .warm
        default:        return .tropical
        }
    }
}
```

**Obs:** Ordningen i `allCases` ska vara tropical first (varmast) till arctic sist (kallast) — matchar befintlig sortering i FriendListView.

**2. FriendCategoryView:**

Tar `[FriendWeather]` och `@Binding var selectedFriendWeather: FriendWeather?` som parametrar.

Layout:
- Vertikal `ScrollView` med `LazyVStack(alignment: .leading, spacing: 24)`
- `ForEach(WeatherCategory.allCases, id: \.self)` — hoppa över kategorier utan vänner
- Per kategori: rubrik + horisontell `ScrollView` med `LazyHStack`

```swift
struct FriendCategoryView: View {
    let friendWeathers: [FriendWeather]
    @Binding var selectedFriendWeather: FriendWeather?

    private var categorized: [WeatherCategory: [FriendWeather]] {
        Dictionary(grouping: friendWeathers.filter { $0.temperatureCelsius != nil }) { fw in
            WeatherCategory.category(for: fw.temperatureCelsius!)
        }
    }

    var body: some View {
        ScrollView(.vertical) {
            LazyVStack(alignment: .leading, spacing: 24) {
                ForEach(WeatherCategory.allCases, id: \.self) { category in
                    if let friends = categorized[category], !friends.isEmpty {
                        categoryRow(category: category, friends: friends)
                    }
                }
            }
            .padding(.vertical)
        }
    }
}
```

**3. Skapa categoryRow-metod:**

Rubrik med emoji + kategorinamn, sedan horisontell karusell med iOS 17 scroll target behavior.

```swift
private func categoryRow(category: WeatherCategory, friends: [FriendWeather]) -> some View {
    VStack(alignment: .leading, spacing: 8) {
        Text("\(category.emoji) \(category.label)")
            .font(.headline)
            .padding(.horizontal)
        ScrollView(.horizontal, showsIndicators: false) {
            LazyHStack(spacing: 12) {
                ForEach(friends) { fw in
                    FriendWeatherCard(friendWeather: fw)
                        .onTapGesture { selectedFriendWeather = fw }
                }
            }
            .padding(.horizontal)
            .scrollTargetLayout()
        }
        .scrollTargetBehavior(.viewAligned)
    }
}
```

**4. Skapa FriendWeatherCard som privat struct i samma fil:**

Kort med:
- Fast bredd (~140pt) och höjd (~160pt) — tillräckligt för profilbild, namn, temperatur och ikon
- Bakgrundsfärg: `Color.temperatureColor(celsius:).opacity(0.15)` — subtil men tydlig
- `RoundedRectangle(cornerRadius: 16)` som bakgrund
- Layout (vertikal VStack):
  - Profilbild (AsyncImage med initialer-fallback, 44x44 cirkel)
  - Namn (caption, lineLimit 1)
  - Temperatur (title3 bold, färgad med temperatureColor)
  - SF Symbol väderikon (body, färgad med temperatureColor)

```swift
private struct FriendWeatherCard: View {
    let friendWeather: FriendWeather

    private var tempColor: Color {
        friendWeather.temperatureCelsius.map { Color.temperatureColor(celsius: $0) } ?? .secondary
    }

    var body: some View {
        VStack(spacing: 8) {
            profileImage
                .frame(width: 44, height: 44)
                .clipShape(Circle())

            Text(friendWeather.friend.displayName.components(separatedBy: " ").first ?? friendWeather.friend.displayName)
                .font(.caption)
                .lineLimit(1)

            Text(friendWeather.temperatureFormatted)
                .font(.title3.weight(.bold))
                .foregroundStyle(tempColor)

            Image(systemName: friendWeather.symbolName)
                .font(.body)
                .foregroundStyle(tempColor)
        }
        .frame(width: 140, height: 160)
        .background(
            RoundedRectangle(cornerRadius: 16)
                .fill(tempColor.opacity(0.12))
        )
    }

    // profileImage: AsyncImage med initialer-fallback (samma mönster som FriendRowView)
}
```

**5. Empty state:**

Om `categorized` är helt tom (inga vänner med temperaturdata), visa `ContentUnavailableView` med meddelande: "Lägg till vänner för att se kategoriserat väder".

**6. Koppla FriendCategoryView till FriendsTabView:**

I FriendsTabView:s `switch selectedTab`, ersätt placeholder `Text("Kategorier kommer snart")` med:
```swift
case .categories:
    FriendCategoryView(
        friendWeathers: viewModel.favorites + viewModel.others,
        selectedFriendWeather: $selectedFriendWeather
    )
```

Verifiera att `selectedFriendWeather` redan hanteras i FriendsTabView (sheet(item:) med WeatherDetailSheet) — kopplingen bör redan finnas från Plan 05-01.

**7. Kör xcodegen:**
```bash
cd /Users/richardsandenskog/Claude/weatherfriends && xcodegen generate
```
  </action>
  <verify>
    <automated>cd /Users/richardsandenskog/Claude/weatherfriends && xcodegen generate 2>&1 && xcodebuild build -project HotAndColdFriends.xcodeproj -scheme HotAndColdFriends -destination 'platform=iOS Simulator,name=iPhone 16' -quiet 2>&1 | tail -5</automated>
  </verify>
  <done>
    - FriendCategoryView.swift existerar med WeatherCategory-enum och karusell-layout
    - WeatherCategory har fem fall: tropical (28+), warm (20-28), cool (10-20), cold (0-10), arctic (<0)
    - Horisontella karuseller med FriendWeatherCard per kategori
    - Tomma kategorier döljs
    - Kort visar profilbild, namn, temperatur och SF Symbol med temperaturfärgad bakgrund
    - Tap på kort öppnar WeatherDetailSheet
    - FriendsTabView:s kategorier-case renderar FriendCategoryView
    - Projektet kompilerar utan fel
  </done>
</task>

<task type="auto">
  <name>Task 2: Implementera DailyWeatherNotificationService och koppla till FriendsTabView</name>
  <files>
    HotAndColdFriends/Services/DailyWeatherNotificationService.swift,
    HotAndColdFriends/Features/FriendList/FriendsTabView.swift,
    project.yml
  </files>
  <action>
**1. Skapa DailyWeatherNotificationService.swift:**

Tjänst som schemalägger en daglig lokal notis kl 07:00 med favoriters vädersammanfattning.

```swift
import Foundation
import UserNotifications

@MainActor
class DailyWeatherNotificationService {
    private let notificationCenter = UNUserNotificationCenter.current()
    private let identifier = "daily-weather-summary"

    /// Schemalägger daglig notis kl 07:00 med favoriters väderdata.
    /// Anropas vid varje app-start efter att väderdata laddats.
    /// Ersätter alltid befintlig schemalagd notis (undviker dubbletter).
    func schedule(favorites: [FriendWeather]) async {
        // Schemalägg inte om inga favoriter finns
        guard !favorites.isEmpty else {
            cancel()
            return
        }

        // Kontrollera att notis-behörighet är beviljad
        let settings = await notificationCenter.notificationSettings()
        guard settings.authorizationStatus == .authorized else { return }

        // Bygg notis-body: "Anna 28° · Erik 12° · Lisa -3°"
        let summary = favorites.compactMap { fw -> String? in
            guard fw.temperatureCelsius != nil else { return nil }
            let firstName = fw.friend.displayName.components(separatedBy: " ").first ?? fw.friend.displayName
            return "\(firstName) \(fw.temperatureFormatted)"
        }.joined(separator: " \u{00B7} ")  // middle dot separator

        guard !summary.isEmpty else {
            cancel()
            return
        }

        let content = UNMutableNotificationContent()
        content.title = "God morgon! \u{2600}\u{FE0F}"  // sol-emoji
        content.body = summary
        content.sound = .default

        var dateComponents = DateComponents()
        dateComponents.calendar = Calendar.current
        dateComponents.hour = 7
        dateComponents.minute = 0

        let trigger = UNCalendarNotificationTrigger(dateMatching: dateComponents, repeats: true)
        let request = UNNotificationRequest(identifier: identifier, content: content, trigger: trigger)

        // Ta bort befintlig notis innan ny schemaläggs — undviker dubbletter
        notificationCenter.removePendingNotificationRequests(withIdentifiers: [identifier])

        do {
            try await notificationCenter.add(request)
        } catch {
            // Notis-schemaläggning är icke-kritisk — logga men propagera ej
        }
    }

    /// Avbryter schemalagd daglig notis.
    func cancel() {
        notificationCenter.removePendingNotificationRequests(withIdentifiers: [identifier])
    }
}
```

**Designbeslut:**
- `@MainActor` men INTE `@Observable` — bakgrundstjänst utan UI-binding (samma mönster som WeatherAlertService, se STATE.md decision)
- Fast identifier `"daily-weather-summary"` — `removePendingNotificationRequests` + `add` ersätter alltid
- Behörighetskontroll (`notificationSettings()`) innan schemaläggning — förhindrar tyst misslyckande
- Unicode-escape för emojis i content.title för att undvika kodningsproblem

**2. Koppla till FriendsTabView:**

Lägg till en `@State private var dailyNotificationService = DailyWeatherNotificationService()` i FriendsTabView.

Schemalägg notisen i en `.task {}` eller `.onChange(of: viewModel.isLoading)` som triggar EFTER att data laddats:

```swift
.onChange(of: viewModel.isLoading) { _, isLoading in
    if !isLoading {
        Task {
            await dailyNotificationService.schedule(favorites: viewModel.favorites)
        }
    }
}
```

Alternativt (enklare): Anropa `schedule()` i slutet av `.task {}` som redan laddar data, EFTER `viewModel.load()`:

```swift
.task {
    guard let uid = authManager.currentUser?.id else { return }
    await viewModel.load(uid: uid, friendService: friendService, weatherService: weatherService)
    // Schemalägg daglig notis med aktuell favoritdata
    await dailyNotificationService.schedule(favorites: viewModel.favorites)
}
```

Välj den approach som passar bäst med FriendsTabView:s befintliga `.task {}` från Plan 05-01.

**3. Kör xcodegen:**
```bash
cd /Users/richardsandenskog/Claude/weatherfriends && xcodegen generate
```
  </action>
  <verify>
    <automated>cd /Users/richardsandenskog/Claude/weatherfriends && xcodegen generate 2>&1 && xcodebuild build -project HotAndColdFriends.xcodeproj -scheme HotAndColdFriends -destination 'platform=iOS Simulator,name=iPhone 16' -quiet 2>&1 | tail -5</automated>
  </verify>
  <done>
    - DailyWeatherNotificationService.swift existerar med schedule() och cancel()
    - schedule() schemalägger UNCalendarNotificationTrigger kl 07:00 med repeats:true
    - schedule() anropar removePendingNotificationRequests innan add (undviker dubbletter)
    - schedule() kontrollerar notis-behörighet innan schemaläggning
    - schedule() bygger body med favoriters namn + temperatur
    - schedule() avbryter om inga favoriter finns
    - FriendsTabView anropar schedule() efter data laddats
    - Projektet kompilerar utan fel
  </done>
</task>

</tasks>

<verification>
1. `xcodebuild build` kompilerar utan fel
2. Kategorier-fliken visar horisontella karuseller med väderkort per temperaturkategori
3. Tomma kategorier döljs (bara kategorier med vänner visas)
4. Kort har temperaturfärgad bakgrund, profilbild, namn, temperatur och SF Symbol
5. Tap på kort öppnar WeatherDetailSheet
6. DailyWeatherNotificationService schemalägger notis kl 07:00 med favoriters data
7. Notis-schemaläggning sker automatiskt vid app-start (efter data laddats)
8. Omschemaläggning ersätter befintlig notis (inget dubbletter)
</verification>

<success_criteria>
- VIEW-02 uppfyllt: Grupperade väderkort i fem kategorier med horisontella karuseller
- PUSH-02 uppfyllt: Daglig lokal notis kl 07:00 med favoriters vädersammanfattning
- Alla tre segmented control-vyer (Lista, Karta, Kategorier) fungerar
- Projektet kompilerar och körs
</success_criteria>

<output>
After completion, create `.planning/phases/05-utokade-vyer/05-02-SUMMARY.md`
</output>
