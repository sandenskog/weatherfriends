---
phase: 04.5-vanprofil-docs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - HotAndColdFriends/Features/Profile/FriendProfileView.swift
  - HotAndColdFriends/Features/FriendList/WeatherDetailSheet.swift
  - .planning/phases/01-foundation/01-02-SUMMARY.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - PROF-03
  - AUTH-01
  - AUTH-02
  - AUTH-03

must_haves:
  truths:
    - "Användare kan tappa på headern (profilbild + namn + stad) i WeatherDetailSheet för att öppna vänprofil"
    - "FriendProfileView visar vännens namn, profilbild och stad utan nätverksanrop"
    - "AUTH-01, AUTH-02, AUTH-03 finns i 01-02-SUMMARY.md frontmatter under requirements-completed"
    - "REQUIREMENTS.md traceability visar PROF-03 som Complete och korrekt coverage-summering"
  artifacts:
    - path: "HotAndColdFriends/Features/Profile/FriendProfileView.swift"
      provides: "Vänprofil-vy som tar Friend direkt"
      contains: "struct FriendProfileView"
    - path: "HotAndColdFriends/Features/FriendList/WeatherDetailSheet.swift"
      provides: "Tappbar header med sheet-navigation till FriendProfileView"
      contains: "showFriendProfile"
    - path: ".planning/phases/01-foundation/01-02-SUMMARY.md"
      provides: "Dokumenterad requirements-completed för AUTH-01/02/03"
      contains: "requirements-completed"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Korrekt traceability-status för PROF-03"
      contains: "PROF-03 | Phase 4.5 | Complete"
  key_links:
    - from: "HotAndColdFriends/Features/FriendList/WeatherDetailSheet.swift"
      to: "HotAndColdFriends/Features/Profile/FriendProfileView.swift"
      via: ".sheet(isPresented: $showFriendProfile) med FriendProfileView(friend:)"
      pattern: "FriendProfileView\\(friend:"
---

<objective>
Gör vänprofilen nåbar via WeatherDetailSheet-headern och fixa dokumentationsgap i SUMMARY-frontmatter och REQUIREMENTS-traceability.

Purpose: PROF-03 (visa andra användares profiler) är delvis uppfyllt — ProfileView finns men är ej nåbar för vänner. Dokumentationsfix säkerställer korrekt spårbarhet för AUTH-01/02/03.
Output: FriendProfileView.swift (ny fil), uppdaterad WeatherDetailSheet, fixad 01-02-SUMMARY.md frontmatter, uppdaterad REQUIREMENTS.md traceability.
</objective>

<execution_context>
@/Users/richardsandenskog/.claude/get-shit-done/workflows/execute-plan.md
@/Users/richardsandenskog/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.5-vanprofil-docs/04.5-CONTEXT.md
@.planning/phases/04.5-vanprofil-docs/04.5-RESEARCH.md

@HotAndColdFriends/Features/Profile/ProfileView.swift
@HotAndColdFriends/Features/FriendList/WeatherDetailSheet.swift
@HotAndColdFriends/Models/Friend.swift

<interfaces>
<!-- Friend-modellen som FriendProfileView tar som parameter -->

From HotAndColdFriends/Models/Friend.swift:
```swift
struct Friend: Codable, Identifiable {
    @DocumentID var id: String?
    var authUid: String?
    var displayName: String
    var photoURL: String?
    var city: String
    var cityLatitude: Double?
    var cityLongitude: Double?
    var isFavorite: Bool
    var isDemo: Bool
    var hasActiveAlert: Bool?
    var alertSummary: String?
    @ServerTimestamp var lastAlertSentAt: Timestamp?
    @ServerTimestamp var createdAt: Timestamp?
}
```

From HotAndColdFriends/Features/FriendList/WeatherDetailSheet.swift (headerSection-strukturen):
```swift
struct WeatherDetailSheet: View {
    let friendWeather: FriendWeather
    // ...
    private var headerSection: some View {
        VStack(spacing: 8) {
            profileImage.frame(width: 80, height: 80).clipShape(Circle())
            Text(friendWeather.friend.displayName).font(.title2.weight(.semibold))
            Text(friendWeather.friend.city).font(.subheadline).foregroundStyle(.secondary)
            HStack(alignment: .center, spacing: 12) { /* temperatur + symbol */ }
            Text(friendWeather.conditionDescription) /* kondition */
        }
    }
}
```

From HotAndColdFriends/Features/Profile/ProfileView.swift (mönster att matcha):
```swift
struct ProfileView: View {
    // Använder .presentationDetents([.medium]) och .presentationDragIndicator(.visible)
    // profileImageView med AsyncImage + initialsCircle fallback
    // initialsCircle(name:size:) med Circle fill + Text initials
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Skapa FriendProfileView och gör WeatherDetailSheet-headern tappbar</name>
  <files>
    HotAndColdFriends/Features/Profile/FriendProfileView.swift,
    HotAndColdFriends/Features/FriendList/WeatherDetailSheet.swift
  </files>
  <action>
**1. Skapa FriendProfileView.swift** i `HotAndColdFriends/Features/Profile/`:

Ny SwiftUI-vy som tar en `Friend` som parameter (INTE AppUser, INTE uid). Ingen ViewModel, inga asynkrona anrop — all data finns i Friend-modellen.

Struktur (matcha ProfileView-mönstret):
- `let friend: Friend`
- `body`: VStack med ScrollView inuti, identisk layout som ProfileView men utan redigera-knapp och utan loading/error-states
- Profilbild: AsyncImage med `friend.photoURL` → fallback till `initialsCircle(name:size:)`. Storlek 100x100 (samma som ProfileView).
- Namn: `Text(friend.displayName)` med `.font(.title2).fontWeight(.semibold)`
- Stad: `Label(friend.city, systemImage: "location.fill")` med `.font(.subheadline).foregroundStyle(.secondary)` — visa bara om `!friend.city.isEmpty`
- Presentation: `.presentationDetents([.medium])` och `.presentationDragIndicator(.visible)` (matchar alla andra sheets i appen)
- Ingen stäng-knapp (matchar appens mönster — bara drag-indikator)
- Kopiera `initialsCircle(name:size:)` och `initials(from:)` från ProfileView (identisk implementation)

**2. Uppdatera WeatherDetailSheet.swift**:

Lägg till state-variabel:
```swift
@State private var showFriendProfile = false
```

Ändra `headerSection`: Wrappa den tappbara delen (profilbild + namn + stad) i en `Button` med `.buttonStyle(.plain)` som sätter `showFriendProfile = true`. Väderdata (temperatur, symbol, konditionsbeskrivning) ska INTE vara del av tappbar yta — de ligger kvar utanför Button.

Konkret ändring i headerSection:
```swift
private var headerSection: some View {
    VStack(spacing: 8) {
        // Tappbar del — öppnar vänprofil
        Button {
            showFriendProfile = true
        } label: {
            VStack(spacing: 8) {
                profileImage
                    .frame(width: 80, height: 80)
                    .clipShape(Circle())

                Text(friendWeather.friend.displayName)
                    .font(.title2.weight(.semibold))
                    .foregroundStyle(.primary)

                Text(friendWeather.friend.city)
                    .font(.subheadline)
                    .foregroundStyle(.secondary)
            }
        }
        .buttonStyle(.plain)

        // Ej tappbar — väderdata (oförändrad)
        HStack(alignment: .center, spacing: 12) {
            // ... temperatur och symbol exakt som idag
        }

        Text(friendWeather.conditionDescription)
            .font(.body)
            .foregroundStyle(.secondary)
    }
}
```

Lägg till `.sheet(isPresented:)` i body, direkt efter `.presentationDragIndicator(.visible)`:
```swift
.sheet(isPresented: $showFriendProfile) {
    FriendProfileView(friend: friendWeather.friend)
}
```

**3. Kör xcodegen** för att registrera nya filen:
```bash
cd /Users/richardsandenskog/Claude/weatherfriends && xcodegen generate
```

**Viktigt:**
- Använd `.buttonStyle(.plain)` på Button — annars får text/ikoner blå tint-färg
- `.foregroundStyle(.primary)` på displayName-texten inuti Button (säkerställer svart text, inte blå)
- `@State` för `showFriendProfile` ska vara i WeatherDetailSheet-structen (inte i sub-vy)
- Stackade sheets fungerar korrekt på iOS 17+ (projektets deployment target)
  </action>
  <verify>
    <automated>cd /Users/richardsandenskog/Claude/weatherfriends && xcodebuild -scheme HotAndColdFriends -destination 'platform=iOS Simulator,name=iPhone 16' build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - FriendProfileView.swift finns i Features/Profile/ och visar Friend-data (namn, bild, stad) utan nätverksanrop
    - WeatherDetailSheet headerSection har tappbar profilbild+namn+stad via Button med .buttonStyle(.plain)
    - .sheet(isPresented: $showFriendProfile) presenterar FriendProfileView med .presentationDetents([.medium])
    - Projektet bygger utan kompileringsfel
  </done>
</task>

<task type="auto">
  <name>Task 2: Fixa dokumentation — SUMMARY-frontmatter och REQUIREMENTS-traceability</name>
  <files>
    .planning/phases/01-foundation/01-02-SUMMARY.md,
    .planning/REQUIREMENTS.md
  </files>
  <action>
**1. Uppdatera 01-02-SUMMARY.md frontmatter:**

Lägg till `requirements-completed`-fältet i YAML-frontmattern, direkt efter `completed: 2026-03-02`-raden:

```yaml
# Metrics
duration: 16min
completed: 2026-03-02
requirements-completed:
  - AUTH-01
  - AUTH-02
  - AUTH-03
```

Använd exakt YAML-format med 2 mellanslags indrag och bindestreck-lista. Fältet ska ligga sist i frontmattern (före `---`-stängaren).

**2. Uppdatera REQUIREMENTS.md traceability-tabell:**

Ändra PROF-03-raden från:
```
| PROF-03 | Phase 4.5 | Pending (gap closure) |
```
till:
```
| PROF-03 | Phase 4.5 | Complete |
```

Ändra CHAT-01 och CHAT-02 från:
```
| CHAT-01 | Phase 4.4 | Pending (gap closure) |
| CHAT-02 | Phase 4.4 | Pending (gap closure) |
```
till:
```
| CHAT-01 | Phase 4.4 | Complete |
| CHAT-02 | Phase 4.4 | Complete |
```
(Fas 4.4 är redan implementerad — authUid-population är klar.)

Uppdatera coverage-summeringen:
- Satisfied: 20 → 23
- Pending (gap closure 4.4-4.5): 3 → 0

**3. Kontrollera REQUIREMENTS.md checkbox-status:**

Säkerställ att PROF-03 redan är ikryssad (`[x]`). Om den visar `[x]` — bra, lämna. Om den visar `[ ]` — kryssa i den (den var markerad som partial i roadmap men complete i requirements — verifiera och gör konsekvent).
  </action>
  <verify>
    <automated>grep -n "requirements-completed" /Users/richardsandenskog/Claude/weatherfriends/.planning/phases/01-foundation/01-02-SUMMARY.md && grep "PROF-03" /Users/richardsandenskog/Claude/weatherfriends/.planning/REQUIREMENTS.md && grep "Pending (gap closure" /Users/richardsandenskog/Claude/weatherfriends/.planning/REQUIREMENTS.md; echo "Exit: $?"</automated>
  </verify>
  <done>
    - 01-02-SUMMARY.md frontmatter innehåller requirements-completed: [AUTH-01, AUTH-02, AUTH-03]
    - REQUIREMENTS.md traceability visar PROF-03 som Complete (inte Pending)
    - REQUIREMENTS.md traceability visar CHAT-01 och CHAT-02 som Complete
    - Coverage: Satisfied 23, Pending (gap closure) 0
    - Inga "Pending (gap closure)" kvar i traceability-tabellen
  </done>
</task>

</tasks>

<verification>
1. `xcodebuild -scheme HotAndColdFriends build` lyckas utan fel
2. FriendProfileView.swift finns i HotAndColdFriends/Features/Profile/
3. WeatherDetailSheet.swift innehåller `showFriendProfile` state och `.sheet(isPresented:)` med FriendProfileView
4. 01-02-SUMMARY.md frontmatter parsas korrekt med requirements-completed
5. REQUIREMENTS.md har inga rader med "Pending (gap closure)" kvar
</verification>

<success_criteria>
- Projektet bygger utan kompileringsfel (xcodebuild BUILD SUCCEEDED)
- FriendProfileView tar Friend som parameter och visar namn, bild, stad utan nätverksanrop
- WeatherDetailSheet-headern (profilbild + namn + stad) öppnar FriendProfileView som stackad sheet
- Väderdata (temperatur, symbol) i headern är INTE tappbar
- 01-02-SUMMARY.md har requirements-completed med AUTH-01, AUTH-02, AUTH-03
- REQUIREMENTS.md traceability visar korrekt Complete-status för PROF-03, CHAT-01, CHAT-02
</success_criteria>

<output>
After completion, create `.planning/phases/04.5-vanprofil-docs/04.5-01-SUMMARY.md`
</output>
