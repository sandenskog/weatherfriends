---
phase: 04.1-onboarding-kontaktimport
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - HotAndColdFriends/Features/ContactImport/ImportReviewView.swift
  - HotAndColdFriends/Features/ContactImport/ContactImportView.swift
  - HotAndColdFriends/Features/Onboarding/OnboardingFavoritesView.swift
autonomous: true
requirements: [FRND-03, FRND-04, WTHR-01]
gap_closure: true

must_haves:
  truths:
    - "Kontakter importerade via onboarding-flödet genomgår AI-platsgissning (guessLocations())"
    - "Användare ser ImportReviewView med konfidens-färger och korrigeringsmöjlighet innan kontakter sparas via onboarding"
    - "Alla sparade vänner (via onboarding) har cityLatitude/cityLongitude om geocoding lyckas"
    - "Auto-geocoding sker i ImportReviewView.buildReviewItems() oavsett flöde (onboarding eller standard)"
    - "Befintligt standard-importflöde via ContactImportView fungerar som förut (regression-fritt)"
  artifacts:
    - path: "HotAndColdFriends/Features/ContactImport/ImportReviewView.swift"
      provides: "ImportReviewMode enum, async buildReviewItems med CLGeocoder, mode-baserad saveAll"
      contains: "enum ImportReviewMode"
    - path: "HotAndColdFriends/Features/ContactImport/ContactImportView.swift"
      provides: "Uppdaterat ImportReviewView-anrop med mode: .standard(...)"
      contains: "mode: .standard"
    - path: "HotAndColdFriends/Features/Onboarding/OnboardingFavoritesView.swift"
      provides: "ContactImportOnboardingWrapper med guessLocations(), isGuessing-state och ImportReviewView-sheet i onboarding-läge"
      contains: "mode: .onboarding"
  key_links:
    - from: "ContactImportOnboardingWrapper"
      to: "ContactImportService.guessLocations()"
      via: "importSelected() async"
      pattern: "contactImportService\\.guessLocations"
    - from: "ContactImportOnboardingWrapper"
      to: "ImportReviewView"
      via: ".sheet(isPresented: $showReview)"
      pattern: "ImportReviewView.*mode.*onboarding"
    - from: "ImportReviewView.buildReviewItems()"
      to: "CLGeocoder.geocodeAddressString()"
      via: "async for-loop i buildReviewItems"
      pattern: "geocoder\\.geocodeAddressString"
    - from: "ImportReviewView.saveAll()"
      to: "PendingFriend callback / FriendService"
      via: "switch mode"
      pattern: "case .onboarding|case .standard"
---

<objective>
Integrera guessLocations() och ImportReviewView i onboarding-kontaktimport, plus fixa auto-geocoding i ImportReviewView som saknas i ALLA flöden.

Purpose: Kontakter importerade via onboarding saknar AI-platsgissning och koordinater (cityLatitude/cityLongitude = nil), vilket gör att WeatherKit inte kan visa väder. Dessutom saknar ImportReviewView.buildReviewItems() geocoding — även standard-flödet via ContactImportView saknar koordinater trots korrekt AI-gissad stad.

Output: Refaktorerad ImportReviewView med mode-enum och auto-geocoding, uppdaterad ContactImportView, och utökad ContactImportOnboardingWrapper med fullt guessLocations()-ImportReviewView-flöde.
</objective>

<execution_context>
@/Users/richardsandenskog/.claude/get-shit-done/workflows/execute-plan.md
@/Users/richardsandenskog/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.1-onboarding-kontaktimport/04.1-CONTEXT.md
@.planning/phases/04.1-onboarding-kontaktimport/04.1-RESEARCH.md

<interfaces>
<!-- Befintliga typer och kontrakt som executor behover. Extraherade fran kodbasen. -->

From HotAndColdFriends/Features/ContactImport/ImportReviewView.swift:
```swift
struct ReviewItem: Identifiable {
    let id: String
    let contact: ImportableContact
    var city: String
    var country: String
    var latitude: Double?
    var longitude: Double?
    var confidence: String  // "high", "medium", "low", "unknown"
    var reason: String
    var isIncluded: Bool
}

struct ImportReviewView: View {
    let uid: String
    let contacts: [ImportableContact]
    let locationGuesses: [LocationGuess]
    let friendService: FriendService
    let contactImportService: ContactImportService
    let onCompleted: () -> Void
    // ... @State properties ...
}
```

From HotAndColdFriends/Features/ContactImport/ContactImportView.swift:
```swift
struct ContactImportView: View {
    let uid: String
    let friendService: FriendService
    let onImported: () -> Void
    // sheet-presentation rad 99-110:
    .sheet(isPresented: $showReview) {
        ImportReviewView(
            uid: uid,
            contacts: selectedContacts,
            locationGuesses: locationGuesses,
            friendService: friendService,
            contactImportService: contactImportService
        ) {
            onImported()
            dismiss()
        }
    }
}
```

From HotAndColdFriends/Features/Onboarding/OnboardingFavoritesView.swift:
```swift
struct PendingFriend: Identifiable {
    let id = UUID()
    var displayName: String
    var city: String
    var cityLatitude: Double?
    var cityLongitude: Double?
}

private struct ContactImportOnboardingWrapper: View {
    @Binding var pendingFriends: [PendingFriend]
    @State private var contactImportService = ContactImportService()
    // ... SAKNAR guessLocations(), ImportReviewView, isGuessing-state ...
    // addSelectedAsPending() satter alltid cityLatitude: nil, cityLongitude: nil
}
```

From HotAndColdFriends/Services/ContactImportService.swift:
```swift
struct LocationGuess: Decodable, Identifiable { ... }
struct ReviewedContact { ... }
func guessLocations(for contacts: [ImportableContact]) async throws -> [LocationGuess]
func saveReviewedContacts(uid: String, reviewedContacts: [ReviewedContact], friendService: FriendService) async throws
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refaktorera ImportReviewView med mode-enum och auto-geocoding + uppdatera ContactImportView</name>
  <files>HotAndColdFriends/Features/ContactImport/ImportReviewView.swift, HotAndColdFriends/Features/ContactImport/ContactImportView.swift</files>
  <action>
**ImportReviewView.swift — tre andringar:**

1. **Lagg till ImportReviewMode enum** (ovanfor ImportReviewView struct):
```swift
enum ImportReviewMode {
    case standard(uid: String, friendService: FriendService)
    case onboarding(onPendingFriendsReady: ([PendingFriend]) -> Void)
}
```

2. **Refaktorera ImportReviewView initializer:**
- Ersatt `let uid: String` och `let friendService: FriendService` med `let mode: ImportReviewMode`
- Behall `let contacts`, `let locationGuesses`, `let contactImportService`, `let onCompleted`

3. **Gor buildReviewItems() async med CLGeocoder auto-geocoding:**
- Andra signatur till `private func buildReviewItems() async`
- Uppdatera call-site fran `.task { buildReviewItems() }` till `.task { await buildReviewItems() }`
- Lagg till `import CoreLocation` overst i filen
- Efter att items-arrayen byggts (befintlig map-logik), kör sekventiell geocoding:
```swift
// Auto-geocoda alla items med stad men utan koordinater
for index in items.indices {
    let item = items[index]
    guard !item.city.isEmpty, item.latitude == nil else { continue }
    let addressString = item.country.isEmpty ? item.city : "\(item.city), \(item.country)"
    let geocoder = CLGeocoder()  // Ny instans per anrop (CLGeocoder tillater inte parallella anrop)
    if let placemark = try? await geocoder.geocodeAddressString(addressString).first,
       let location = placemark.location {
        items[index].latitude = location.coordinate.latitude
        items[index].longitude = location.coordinate.longitude
    }
    // Misslyckad geocoding: koordinater forblir nil — rod prick i review-vyn, anvandaren kan manuellt söka
}
reviewItems = items
```
- VIKTIGT: Lagg till `@State private var isLoadingReview = true` och satt `isLoadingReview = false` efter geocoding-loopen. Visa `ProgressView("Löser upp platser...")` i body medan isLoadingReview ar true, istallet for listan.

4. **Uppdatera saveAll() med mode-switch:**
```swift
private func saveAll() async {
    isSaving = true
    defer { isSaving = false }
    let included = reviewItems.filter { $0.isIncluded }

    switch mode {
    case .standard(let uid, let friendService):
        // Befintligt Firestore-flode (oforandrat)
        let reviewedContacts = included.map { item in
            ReviewedContact(contact: item.contact, city: item.city, country: item.country,
                          latitude: item.latitude, longitude: item.longitude, confidence: item.confidence)
        }
        do {
            try await contactImportService.saveReviewedContacts(uid: uid, reviewedContacts: reviewedContacts, friendService: friendService)
            onCompleted()
            dismiss()
        } catch {
            errorMessage = "Kunde inte spara kontakter: \(error.localizedDescription)"
        }

    case .onboarding(let onPendingFriendsReady):
        let pendingFriends = included.map { item -> PendingFriend in
            let cityDisplay = item.city.isEmpty
                ? "Okand plats"
                : (item.country.isEmpty ? item.city : "\(item.city), \(item.country)")
            return PendingFriend(displayName: item.contact.fullName, city: cityDisplay,
                              cityLatitude: item.latitude, cityLongitude: item.longitude)
        }
        onPendingFriendsReady(pendingFriends)
        onCompleted()
        dismiss()
    }
}
```

**ContactImportView.swift — en andring:**

Uppdatera sheet-anropet (rad 99-110) fran:
```swift
ImportReviewView(uid: uid, contacts: ..., locationGuesses: ..., friendService: friendService, contactImportService: ...) { ... }
```
till:
```swift
ImportReviewView(mode: .standard(uid: uid, friendService: friendService), contacts: selectedContacts, locationGuesses: locationGuesses, contactImportService: contactImportService) {
    onImported()
    dismiss()
}
```

**Inga andra andringar i ContactImportView.**
  </action>
  <verify>
    <automated>cd /Users/richardsandenskog/Claude/weatherfriends && xcodebuild build -project HotAndColdFriends.xcodeproj -scheme HotAndColdFriends -destination 'platform=iOS Simulator,name=iPhone 17' -quiet 2>&1 | tail -5</automated>
  </verify>
  <done>
    - ImportReviewMode enum definierad med .standard och .onboarding cases
    - ImportReviewView tar mode istallet for uid/friendService
    - buildReviewItems() ar async med CLGeocoder auto-geocoding per kontakt
    - saveAll() branschar pa mode — standard sparar via Firestore, onboarding returnerar PendingFriends via callback
    - ProgressView visas medan geocoding pagar
    - ContactImportView uppdaterad med mode: .standard(...) — befintligt flode oforandrat
    - Projektet kompilerar utan fel
  </done>
</task>

<task type="auto">
  <name>Task 2: Utoka ContactImportOnboardingWrapper med guessLocations() och ImportReviewView</name>
  <files>HotAndColdFriends/Features/Onboarding/OnboardingFavoritesView.swift</files>
  <action>
**ContactImportOnboardingWrapper** (rad 351 i OnboardingFavoritesView.swift) — utoka till att spegla ContactImportView:s importSelected()-flode:

1. **Lagg till ny state:**
```swift
@State private var isGuessing = false
@State private var showReview = false
@State private var selectedContacts: [ImportableContact] = []
@State private var locationGuesses: [LocationGuess] = []
```

2. **Ersatt toolbar-knappen** (nuvarande "Lagg till (\(selectedIds.count))") med:
```swift
ToolbarItem(placement: .confirmationAction) {
    Button {
        Task { await importSelected() }
    } label: {
        if isGuessing {
            HStack(spacing: 4) {
                ProgressView().controlSize(.small)
                Text("Analyserar...")
            }
        } else {
            Text("Importera (\(selectedIds.count))")
                .fontWeight(.semibold)
        }
    }
    .disabled(selectedIds.isEmpty || isGuessing)
}
```

3. **Lagg till importSelected() async-metod** (speglar ContactImportView exakt):
```swift
private func importSelected() async {
    isGuessing = true
    defer { isGuessing = false }
    selectedContacts = contacts.filter { selectedIds.contains($0.id) }
    do {
        locationGuesses = try await contactImportService.guessLocations(for: selectedContacts)
    } catch {
        locationGuesses = []  // Visa review anda, utan gissningar
    }
    showReview = true
}
```

4. **Lagg till .sheet(isPresented: $showReview)** pa NavigationStack (bredvid befintlig .alert):
```swift
.sheet(isPresented: $showReview) {
    ImportReviewView(
        mode: .onboarding { newPending in
            pendingFriends.append(contentsOf: newPending)
        },
        contacts: selectedContacts,
        locationGuesses: locationGuesses,
        contactImportService: contactImportService
    ) {
        dismiss()  // Stang ContactImportOnboardingWrapper nar review ar klar
    }
}
```

5. **Ta bort addSelectedAsPending()** — den ersatts helt av importSelected() + ImportReviewView-callback.

**Sammanfattning av forandringar:**
- `addSelectedAsPending()` BORTTAGEN
- `importSelected()` TILLAGD (async, anropar guessLocations())
- Toolbar-knapp andrad fran synkron "Lagg till" till async "Importera" med spinner
- `.sheet(isPresented: $showReview)` TILLAGD med ImportReviewView i onboarding-mode
- 4 nya @State-properties tillagda

OBS: ContactImportOnboardingWrapper ar markerad `private struct` — den behover INTE vara publik. ImportReviewView ar dock publik (internal) och kan nass fran OnboardingFavoritesView.swift utan problem.
  </action>
  <verify>
    <automated>cd /Users/richardsandenskog/Claude/weatherfriends && xcodebuild build -project HotAndColdFriends.xcodeproj -scheme HotAndColdFriends -destination 'platform=iOS Simulator,name=iPhone 17' -quiet 2>&1 | tail -5</automated>
  </verify>
  <done>
    - ContactImportOnboardingWrapper anropar guessLocations() via importSelected()
    - Toolbar-knapp visar "Analyserar..." spinner under AI-gissning
    - ImportReviewView visas som sheet i onboarding-mode med callback
    - Godkanda kontakter returneras som PendingFriend med stad och koordinater
    - addSelectedAsPending() borttagen (ersatt av nytt flode)
    - Projektet kompilerar utan fel
  </done>
</task>

</tasks>

<verification>
1. Projektet kompilerar: `xcodebuild build -scheme HotAndColdFriends -destination 'platform=iOS Simulator,name=iPhone 17' -quiet`
2. ImportReviewMode enum finns: `grep -n "enum ImportReviewMode" HotAndColdFriends/Features/ContactImport/ImportReviewView.swift`
3. Auto-geocoding finns: `grep -n "geocodeAddressString" HotAndColdFriends/Features/ContactImport/ImportReviewView.swift`
4. Onboarding-wrapper anropar guessLocations: `grep -n "guessLocations" HotAndColdFriends/Features/Onboarding/OnboardingFavoritesView.swift`
5. ContactImportView anvander mode: .standard: `grep -n "mode: .standard" HotAndColdFriends/Features/ContactImport/ContactImportView.swift`
6. addSelectedAsPending borttagen: `grep -c "addSelectedAsPending" HotAndColdFriends/Features/Onboarding/OnboardingFavoritesView.swift` returnerar 0
</verification>

<success_criteria>
- ImportReviewView stodjer bade .standard (Firestore) och .onboarding (PendingFriend callback) lage
- buildReviewItems() geocodar automatiskt alla AI-gissade stader till koordinater (CLGeocoder)
- ContactImportOnboardingWrapper kör guessLocations() och visar ImportReviewView innan kontakter laggs till
- Befintligt standard-flode via ContactImportView fungerar som innan (mode: .standard)
- Alla vanner importerade via onboarding far cityLatitude/cityLongitude om geocoding lyckas
- Projektet kompilerar utan fel
</success_criteria>

<output>
After completion, create `.planning/phases/04.1-onboarding-kontaktimport/04.1-01-SUMMARY.md`
</output>
