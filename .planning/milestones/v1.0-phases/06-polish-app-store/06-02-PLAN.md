---
phase: 06-polish-app-store
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - project.yml
  - HotAndColdFriends/Resources/HotAndColdFriends.entitlements
  - HotAndColdFriends/Resources/Info.plist
  - HotAndColdFriends/Resources/PrivacyInfo.xcprivacy
  - HotAndColdFriends/Services/AppWeatherService.swift
  - HotAndColdFriends/Features/FriendList/FriendListViewModel.swift
  - HotAndColdFriends/App/HotAndColdFriendsApp.swift
  - HotAndColdFriends/Core/Navigation/AppRouter.swift
  - HotAndColdFriendsWidget/HotAndColdFriendsWidget.swift
  - HotAndColdFriendsWidget/WidgetViews.swift
  - HotAndColdFriendsWidget/HotAndColdFriendsWidget.entitlements
  - HotAndColdFriendsWidget/Info.plist
  - HotAndColdFriends/Models/WidgetFriendEntry.swift
autonomous: false
requirements:
  - WDGT-01

user_setup:
  - service: Apple Developer Portal
    why: "App Group-entitlement kräver registrering i Developer Portal"
    dashboard_config:
      - task: "Registrera App Group ID: group.se.sandenskog.hotandcoldfriends"
        location: "Apple Developer Portal → Certificates, Identifiers & Profiles → Identifiers → App Groups → + → group.se.sandenskog.hotandcoldfriends"
      - task: "Lägg till App Group-capability på App ID se.sandenskog.hotandcoldfriends"
        location: "Apple Developer Portal → Identifiers → se.sandenskog.hotandcoldfriends → Capabilities → App Groups → kryssruta → Edit → välj group.se.sandenskog.hotandcoldfriends"
      - task: "Skapa nytt App ID för widget-extension: se.sandenskog.hotandcoldfriends.widget"
        location: "Apple Developer Portal → Identifiers → + → App IDs → App → Bundle ID: se.sandenskog.hotandcoldfriends.widget → Capabilities: App Groups (välj group.se.sandenskog.hotandcoldfriends)"
      - task: "Generera nya provisioning profiles för båda App IDs"
        location: "Apple Developer Portal → Profiles → + → iOS App Development → välj respektive App ID → välj device/certifikat"

must_haves:
  truths:
    - "iOS hemskärmswidget visar favoriters väder med profilbild, temperatur och SF Symbol"
    - "Widget finns i tre storlekar: small (1 favorit), medium (3-4 favoriter), large (alla 6)"
    - "Tapp på vän i widget öppnar den vännens väderdetalj i appen"
    - "Widget-data uppdateras var 30:e minut (synkat med AppWeatherService cache)"
    - "Privacy manifest (PrivacyInfo.xcprivacy) deklarerar UserDefaults API-användning"
  artifacts:
    - path: "HotAndColdFriendsWidget/HotAndColdFriendsWidget.swift"
      provides: "WeatherTimelineProvider + HotAndColdFriendsWidget @main entry point"
      contains: "TimelineProvider"
    - path: "HotAndColdFriendsWidget/WidgetViews.swift"
      provides: "SmallWidgetView, MediumWidgetView, LargeWidgetView"
      contains: "SmallWidgetView"
    - path: "HotAndColdFriends/Models/WidgetFriendEntry.swift"
      provides: "WidgetFriendEntry Codable struct delad mellan app och widget"
      contains: "WidgetFriendEntry"
    - path: "project.yml"
      provides: "HotAndColdFriendsWidget target med App Group-entitlements"
      contains: "HotAndColdFriendsWidget"
    - path: "HotAndColdFriends/Resources/PrivacyInfo.xcprivacy"
      provides: "Privacy manifest med UserDefaults API-deklaration"
      contains: "NSPrivacyAccessedAPICategoryUserDefaults"
  key_links:
    - from: "HotAndColdFriends/Services/AppWeatherService.swift"
      to: "UserDefaults(suiteName: group.se.sandenskog.hotandcoldfriends)"
      via: "Skriver WidgetFriendEntry-array till delade UserDefaults efter väderuppdatering"
      pattern: "widgetFavorites"
    - from: "HotAndColdFriendsWidget/HotAndColdFriendsWidget.swift"
      to: "UserDefaults(suiteName: group.se.sandenskog.hotandcoldfriends)"
      via: "Läser WidgetFriendEntry-array synkront i TimelineProvider.getTimeline()"
      pattern: "widgetFavorites"
    - from: "HotAndColdFriendsWidget/WidgetViews.swift"
      to: "hotandcold://friend/{id}"
      via: "widgetURL/Link(destination:) deep links"
      pattern: "hotandcold://friend"
    - from: "HotAndColdFriends/App/HotAndColdFriendsApp.swift"
      to: "AppRouter"
      via: ".onOpenURL { url in } → parsar hotandcold://friend/{id}"
      pattern: "onOpenURL"
---

<objective>
Implementera iOS hemskärmswidget (WDGT-01) som visar favoriters väder i tre storlekar, med deep link-navigation och privacy manifest.

Purpose: Widgeten gör vännernas väder tillgängligt utan att öppna appen — detta är en av de tre kvarvarande v1-kraven och en central differentiator.

Output: WidgetKit-extension med small/medium/large-storlekar, datadelning via App Group UserDefaults, deep link-navigation till väderdetalj, och PrivacyInfo.xcprivacy.
</objective>

<execution_context>
@/Users/richardsandenskog/.claude/get-shit-done/workflows/execute-plan.md
@/Users/richardsandenskog/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-polish-app-store/06-RESEARCH.md

@project.yml
@HotAndColdFriends/Resources/HotAndColdFriends.entitlements
@HotAndColdFriends/Resources/Info.plist
@HotAndColdFriends/App/HotAndColdFriendsApp.swift
@HotAndColdFriends/Core/Navigation/AppRouter.swift
@HotAndColdFriends/Services/AppWeatherService.swift
@HotAndColdFriends/Features/FriendList/FriendListViewModel.swift
@HotAndColdFriends/Models/FriendWeather.swift
@HotAndColdFriends/Models/Friend.swift

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From HotAndColdFriends/Models/FriendWeather.swift:
```swift
struct FriendWeather: Identifiable {
    let friend: Friend
    let weather: CurrentWeather?
    var id: String { friend.id ?? UUID().uuidString }
    var temperatureCelsius: Double?
    var symbolName: String  // SF Symbol-namn: "sun.max.fill", "cloud.rain.fill" etc.
}
```

From HotAndColdFriends/Models/Friend.swift:
```swift
struct Friend: Codable, Identifiable {
    @DocumentID var id: String?
    var authUid: String?
    var displayName: String
    var photoURL: String?
    var city: String
    var cityLatitude: Double?
    var cityLongitude: Double?
    var isFavorite: Bool
    var isDemo: Bool
    // ...
}
```

From HotAndColdFriends/Services/AppWeatherService.swift:
```swift
@Observable @MainActor
class AppWeatherService {
    func currentWeather(latitude: Double, longitude: Double) async throws -> CurrentWeather
    func hourlyForecast(...) async throws -> [HourWeather]
    func dailyForecast(...) async throws -> [DayWeather]
}
```

From HotAndColdFriends/Features/FriendList/FriendListViewModel.swift:
```swift
@Observable @MainActor
class FriendListViewModel {
    var favorites: [FriendWeather] = []
    var others: [FriendWeather] = []
    func load(uid: String, friendService: FriendService, weatherService: AppWeatherService) async
}
```

From HotAndColdFriends/Features/FriendList/FriendRowView.swift:
```swift
extension Color {
    static func temperatureColor(celsius: Double) -> Color
}
```

From HotAndColdFriends/Core/Navigation/AppRouter.swift:
```swift
struct AppRouter: View {
    @State private var openConversationId: String? = nil
    @State private var openWeatherAlertFriendId: String? = nil
    // .onReceive(NotificationCenter) pattern for deep links
}
extension Notification.Name {
    static let openChat, static let openWeatherAlert
}
```

From HotAndColdFriends/App/HotAndColdFriendsApp.swift:
```swift
@main struct HotAndColdFriendsApp: App {
    var body: some Scene {
        WindowGroup {
            AppRouter()
                .environment(authManager)
                .environment(userService)
                // ... alla @Environment-services
                .task { ... }
        }
    }
}
```

From project.yml:
```yaml
targets:
  HotAndColdFriends:
    type: application
    entitlements:
      path: HotAndColdFriends/Resources/HotAndColdFriends.entitlements
      properties:
        com.apple.developer.weatherkit: true
        com.apple.developer.applesignin: [Default]
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: WidgetKit-infrastruktur, datadelning och privacy manifest</name>
  <files>
    project.yml,
    HotAndColdFriends/Resources/HotAndColdFriends.entitlements,
    HotAndColdFriends/Resources/Info.plist,
    HotAndColdFriends/Resources/PrivacyInfo.xcprivacy,
    HotAndColdFriends/Models/WidgetFriendEntry.swift,
    HotAndColdFriends/Services/AppWeatherService.swift,
    HotAndColdFriends/Features/FriendList/FriendListViewModel.swift,
    HotAndColdFriends/App/HotAndColdFriendsApp.swift,
    HotAndColdFriends/Core/Navigation/AppRouter.swift,
    HotAndColdFriendsWidget/HotAndColdFriendsWidget.entitlements,
    HotAndColdFriendsWidget/Info.plist,
    HotAndColdFriendsWidget/HotAndColdFriendsWidget.swift,
    HotAndColdFriendsWidget/WidgetViews.swift
  </files>
  <action>
**1. Skapa WidgetFriendEntry i huvudappen** (`HotAndColdFriends/Models/WidgetFriendEntry.swift`):

```swift
import Foundation

/// Codable-modell delad mellan app och widget via App Group UserDefaults.
/// Innehåller all data widgeten behöver — inga nätverksanrop i widget-extension.
struct WidgetFriendEntry: Codable {
    let id: String
    let displayName: String
    let city: String
    let temperatureCelsius: Double?
    let symbolName: String
    let temperatureColorRGB: [Double]  // [r, g, b] 0...1 — från Color.temperatureColor

    /// Konverterar en FriendWeather till WidgetFriendEntry
    static func from(friendWeather fw: FriendWeather) -> WidgetFriendEntry? {
        guard let id = fw.friend.id else { return nil }
        let temp = fw.temperatureCelsius
        // Beräkna RGB-komponenter för temperaturfärg
        let rgb: [Double]
        if let celsius = temp {
            // Samma logik som Color.temperatureColor — hårdkodad för Codable-transport
            if celsius < 0 {
                rgb = [0.6, 0.85, 1.0]   // isblå
            } else if celsius < 10 {
                rgb = [0.4, 0.7, 0.95]   // kylig blå
            } else if celsius < 20 {
                rgb = [0.3, 0.75, 0.45]  // grön
            } else if celsius < 28 {
                rgb = [1.0, 0.65, 0.2]   // orange
            } else {
                rgb = [1.0, 0.3, 0.2]    // röd
            }
        } else {
            rgb = [0.6, 0.6, 0.6]        // grå fallback
        }
        return WidgetFriendEntry(
            id: id,
            displayName: fw.friend.displayName,
            city: fw.friend.city,
            temperatureCelsius: temp,
            symbolName: fw.symbolName,
            temperatureColorRGB: rgb
        )
    }
}
```

**OBS:** Profilbilder exkluderas från widget-data i denna version. AsyncImage fungerar inte i WidgetKit, och att serialisera bilddata till UserDefaults är memory-intensivt. Widgeten visar initialer istället. Detta kan utökas i v2 med FileManager-baserad bildcache till App Group container.

**2. Lägg till dataskrivning i FriendListViewModel** efter att väderdata laddats:

I `FriendListViewModel.swift`, lägg till efter raden `others = sorted.filter { !$0.friend.isFavorite }` (i slutet av `load()`-metoden):

```swift
// Skriv favoriter till delad UserDefaults för widget
updateWidgetData(favorites: self.favorites)
```

Lägg till privat metod i FriendListViewModel:

```swift
import WidgetKit  // Lägg till överst i filen

private func updateWidgetData(favorites: [FriendWeather]) {
    let entries = favorites.compactMap { WidgetFriendEntry.from(friendWeather: $0) }
    if let data = try? JSONEncoder().encode(entries) {
        let defaults = UserDefaults(suiteName: "group.se.sandenskog.hotandcoldfriends")
        defaults?.set(data, forKey: "widgetFavorites")
    }
    WidgetCenter.shared.reloadTimelines(ofKind: "HotAndColdFriendsWidget")
}
```

**3. Uppdatera project.yml** — lägg till App Group-entitlement i befintligt target och nytt widget-target:

I befintliga `HotAndColdFriends`-targetets entitlements, lägg till App Group:
```yaml
    entitlements:
      path: HotAndColdFriends/Resources/HotAndColdFriends.entitlements
      properties:
        com.apple.developer.weatherkit: true
        com.apple.developer.applesignin:
          - Default
        com.apple.security.application-groups:
          - group.se.sandenskog.hotandcoldfriends
```

Lägg till nytt widget-target UNDER befintliga HotAndColdFriends-target:

```yaml
  HotAndColdFriendsWidgetExtension:
    type: app-extension
    platform: iOS
    deploymentTarget: "17.0"
    sources:
      - path: HotAndColdFriendsWidget
        excludes:
          - "**/@eaDir"
      - path: HotAndColdFriends/Models/WidgetFriendEntry.swift
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: se.sandenskog.hotandcoldfriends.widget
        INFOPLIST_FILE: HotAndColdFriendsWidget/Info.plist
        SWIFT_VERSION: "5.9"
        IPHONEOS_DEPLOYMENT_TARGET: "17.0"
        DEVELOPMENT_TEAM: "A473BQKT8M"
        MARKETING_VERSION: "1.0.0"
        CURRENT_PROJECT_VERSION: "1"
        ASSETCATALOG_COMPILER_WIDGET_BACKGROUND_COLOR_NAME: "WidgetBackground"
    entitlements:
      path: HotAndColdFriendsWidget/HotAndColdFriendsWidget.entitlements
      properties:
        com.apple.security.application-groups:
          - group.se.sandenskog.hotandcoldfriends
    dependencies:
      - target: HotAndColdFriends
        embed: false
```

Dessutom: i HotAndColdFriends-targetets dependencies, lägg till:
```yaml
      - target: HotAndColdFriendsWidgetExtension
        embed:
          - sign
```

**OBS om target dependency:** Widget-extension embeddar inte app-targettet — det är tvärtom: app-targettet embeddar widget-extension. Kontrollera att xcodegen-konfigurationen reflekterar detta korrekt. Om xcodegen-versionen inte stöder `embed` med lista, använd `embed: true` istället. Alternativt: widget-target dependency listas inte under HotAndColdFriends.dependencies — xcodegen hanterar extension-embedding automatiskt. Testa med `xcodegen generate` och verifiera.

**4. Skapa widget-extension-filer:**

**a) `HotAndColdFriendsWidget/HotAndColdFriendsWidget.entitlements`:**
```xml
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>com.apple.security.application-groups</key>
    <array>
        <string>group.se.sandenskog.hotandcoldfriends</string>
    </array>
</dict>
</plist>
```

**b) `HotAndColdFriendsWidget/Info.plist`:**
```xml
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>CFBundleDevelopmentRegion</key>
    <string>$(DEVELOPMENT_LANGUAGE)</string>
    <key>CFBundleDisplayName</key>
    <string>Hot &amp; Cold Friends</string>
    <key>CFBundleExecutable</key>
    <string>$(EXECUTABLE_NAME)</string>
    <key>CFBundleIdentifier</key>
    <string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
    <key>CFBundleInfoDictionaryVersion</key>
    <string>6.0</string>
    <key>CFBundleName</key>
    <string>$(PRODUCT_NAME)</string>
    <key>CFBundlePackageType</key>
    <string>$(PRODUCT_BUNDLE_PACKAGE_TYPE)</string>
    <key>CFBundleShortVersionString</key>
    <string>$(MARKETING_VERSION)</string>
    <key>CFBundleVersion</key>
    <string>$(CURRENT_PROJECT_VERSION)</string>
    <key>NSExtension</key>
    <dict>
        <key>NSExtensionPointIdentifier</key>
        <string>com.apple.widgetkit-extension</string>
    </dict>
</dict>
</plist>
```

**c) `HotAndColdFriendsWidget/HotAndColdFriendsWidget.swift`** — TimelineProvider + Widget @main:

```swift
import WidgetKit
import SwiftUI

// MARK: - Timeline Entry

struct WeatherEntry: TimelineEntry {
    let date: Date
    let friends: [WidgetFriendEntry]
}

// MARK: - Timeline Provider

struct WeatherTimelineProvider: TimelineProvider {
    func placeholder(in context: Context) -> WeatherEntry {
        WeatherEntry(date: Date(), friends: Self.sampleEntries)
    }

    func getSnapshot(in context: Context, completion: @escaping (WeatherEntry) -> Void) {
        let entry: WeatherEntry
        if context.isPreview {
            entry = WeatherEntry(date: Date(), friends: Self.sampleEntries)
        } else {
            entry = WeatherEntry(date: Date(), friends: loadFriends())
        }
        completion(entry)
    }

    func getTimeline(in context: Context, completion: @escaping (Timeline<WeatherEntry>) -> Void) {
        let friends = loadFriends()
        let entry = WeatherEntry(date: Date(), friends: friends)
        // Uppdatera var 30:e minut — synkat med AppWeatherService 30-min TTL-cache
        let nextUpdate = Calendar.current.date(byAdding: .minute, value: 30, to: Date())!
        let timeline = Timeline(entries: [entry], policy: .after(nextUpdate))
        completion(timeline)
    }

    // MARK: - Data Loading

    private func loadFriends() -> [WidgetFriendEntry] {
        guard let defaults = UserDefaults(suiteName: "group.se.sandenskog.hotandcoldfriends"),
              let data = defaults.data(forKey: "widgetFavorites"),
              let decoded = try? JSONDecoder().decode([WidgetFriendEntry].self, from: data) else {
            return []
        }
        return decoded
    }

    // MARK: - Sample Data (för previews och placeholder)

    static let sampleEntries: [WidgetFriendEntry] = [
        WidgetFriendEntry(id: "1", displayName: "Anna", city: "Stockholm", temperatureCelsius: 22, symbolName: "sun.max.fill", temperatureColorRGB: [1.0, 0.65, 0.2]),
        WidgetFriendEntry(id: "2", displayName: "Erik", city: "Oslo", temperatureCelsius: 14, symbolName: "cloud.fill", temperatureColorRGB: [0.3, 0.75, 0.45]),
        WidgetFriendEntry(id: "3", displayName: "Sofia", city: "Barcelona", temperatureCelsius: 30, symbolName: "sun.max.fill", temperatureColorRGB: [1.0, 0.3, 0.2]),
        WidgetFriendEntry(id: "4", displayName: "Marcus", city: "London", temperatureCelsius: 12, symbolName: "cloud.rain.fill", temperatureColorRGB: [0.4, 0.7, 0.95]),
        WidgetFriendEntry(id: "5", displayName: "Lisa", city: "Reykjavik", temperatureCelsius: -2, symbolName: "cloud.snow.fill", temperatureColorRGB: [0.6, 0.85, 1.0]),
        WidgetFriendEntry(id: "6", displayName: "Johan", city: "Bangkok", temperatureCelsius: 34, symbolName: "sun.max.fill", temperatureColorRGB: [1.0, 0.3, 0.2]),
    ]
}

// MARK: - Widget Configuration

@main
struct HotAndColdFriendsWidget: Widget {
    let kind = "HotAndColdFriendsWidget"

    var body: some WidgetConfiguration {
        StaticConfiguration(kind: kind, provider: WeatherTimelineProvider()) { entry in
            WeatherWidgetEntryView(entry: entry)
                .containerBackground(.fill.tertiary, for: .widget)
        }
        .configurationDisplayName("Hot & Cold Friends")
        .description("Se vädret hos dina vänner.")
        .supportedFamilies([.systemSmall, .systemMedium, .systemLarge])
    }
}
```

**d) `HotAndColdFriendsWidget/WidgetViews.swift`** — Layouter per storlek:

```swift
import SwiftUI
import WidgetKit

// MARK: - Entry View (delegerar till rätt storlek)

struct WeatherWidgetEntryView: View {
    let entry: WeatherEntry
    @Environment(\.widgetFamily) var family

    var body: some View {
        switch family {
        case .systemSmall:
            SmallWidgetView(friend: entry.friends.first)
        case .systemMedium:
            MediumWidgetView(friends: Array(entry.friends.prefix(4)))
        default:
            LargeWidgetView(friends: Array(entry.friends.prefix(6)))
        }
    }
}

// MARK: - Small Widget (1 favorit)

struct SmallWidgetView: View {
    let friend: WidgetFriendEntry?

    var body: some View {
        if let friend {
            VStack(spacing: 6) {
                initialsCircle(for: friend, size: 36)
                Text(friend.displayName)
                    .font(.caption.weight(.semibold))
                    .lineLimit(1)
                HStack(spacing: 4) {
                    Image(systemName: friend.symbolName)
                        .font(.caption2)
                        .foregroundStyle(temperatureColor(for: friend))
                    if let temp = friend.temperatureCelsius {
                        Text(String(format: "%.0f°", temp))
                            .font(.title3.weight(.bold))
                            .foregroundStyle(temperatureColor(for: friend))
                    }
                }
                Text(friend.city)
                    .font(.caption2)
                    .foregroundStyle(.secondary)
                    .lineLimit(1)
            }
            .widgetURL(URL(string: "hotandcold://friend/\(friend.id)")!)
        } else {
            emptyState
        }
    }
}

// MARK: - Medium Widget (3-4 favoriter)

struct MediumWidgetView: View {
    let friends: [WidgetFriendEntry]

    var body: some View {
        if friends.isEmpty {
            emptyState
        } else {
            HStack(spacing: 0) {
                ForEach(friends, id: \.id) { friend in
                    Link(destination: URL(string: "hotandcold://friend/\(friend.id)")!) {
                        FriendWidgetCell(friend: friend)
                    }
                    if friend.id != friends.last?.id {
                        Divider()
                    }
                }
            }
        }
    }
}

// MARK: - Large Widget (alla 6 favoriter)

struct LargeWidgetView: View {
    let friends: [WidgetFriendEntry]

    var body: some View {
        if friends.isEmpty {
            emptyState
        } else {
            VStack(spacing: 4) {
                Text("Hot & Cold Friends")
                    .font(.caption.weight(.semibold))
                    .foregroundStyle(.secondary)
                    .frame(maxWidth: .infinity, alignment: .leading)
                    .padding(.bottom, 2)

                LazyVGrid(columns: [
                    GridItem(.flexible()),
                    GridItem(.flexible()),
                    GridItem(.flexible())
                ], spacing: 8) {
                    ForEach(friends, id: \.id) { friend in
                        Link(destination: URL(string: "hotandcold://friend/\(friend.id)")!) {
                            FriendWidgetCell(friend: friend)
                        }
                    }
                }
            }
        }
    }
}

// MARK: - Shared Components

struct FriendWidgetCell: View {
    let friend: WidgetFriendEntry

    var body: some View {
        VStack(spacing: 4) {
            initialsCircle(for: friend, size: 32)
            Text(friend.displayName)
                .font(.caption2.weight(.medium))
                .lineLimit(1)
            HStack(spacing: 2) {
                Image(systemName: friend.symbolName)
                    .font(.system(size: 10))
                if let temp = friend.temperatureCelsius {
                    Text(String(format: "%.0f°", temp))
                        .font(.caption.weight(.bold))
                }
            }
            .foregroundStyle(temperatureColor(for: friend))
            Text(friend.city)
                .font(.system(size: 9))
                .foregroundStyle(.secondary)
                .lineLimit(1)
        }
        .frame(maxWidth: .infinity)
        .padding(.vertical, 4)
    }
}

// MARK: - Helpers

private func initialsCircle(for friend: WidgetFriendEntry, size: CGFloat) -> some View {
    let initials = friend.displayName.components(separatedBy: " ")
        .prefix(2)
        .compactMap { $0.first.map(String.init) }
        .joined()
    return ZStack {
        Circle()
            .fill(temperatureColor(for: friend).opacity(0.15))
        Text(initials)
            .font(.system(size: size * 0.4, weight: .semibold))
            .foregroundStyle(temperatureColor(for: friend))
    }
    .frame(width: size, height: size)
}

private func temperatureColor(for friend: WidgetFriendEntry) -> Color {
    Color(
        red: friend.temperatureColorRGB[safe: 0] ?? 0.6,
        green: friend.temperatureColorRGB[safe: 1] ?? 0.6,
        blue: friend.temperatureColorRGB[safe: 2] ?? 0.6
    )
}

private var emptyState: some View {
    VStack(spacing: 8) {
        Image(systemName: "person.3")
            .font(.title2)
            .foregroundStyle(.secondary)
        Text("Inga favoriter")
            .font(.caption)
            .foregroundStyle(.secondary)
        Text("Markera vänner som favoriter i appen")
            .font(.caption2)
            .foregroundStyle(.tertiary)
            .multilineTextAlignment(.center)
    }
}

// MARK: - Array Safe Subscript

private extension Array {
    subscript(safe index: Index) -> Element? {
        indices.contains(index) ? self[index] : nil
    }
}

// MARK: - Previews

#Preview("Small", as: .systemSmall) {
    HotAndColdFriendsWidget()
} timeline: {
    WeatherEntry(date: Date(), friends: WeatherTimelineProvider.sampleEntries)
}

#Preview("Medium", as: .systemMedium) {
    HotAndColdFriendsWidget()
} timeline: {
    WeatherEntry(date: Date(), friends: WeatherTimelineProvider.sampleEntries)
}

#Preview("Large", as: .systemLarge) {
    HotAndColdFriendsWidget()
} timeline: {
    WeatherEntry(date: Date(), friends: WeatherTimelineProvider.sampleEntries)
}
```

**5. Uppdatera HotAndColdFriends.entitlements** — lägg till App Group:

Lägg till i befintliga entitlements-filen:
```xml
<key>com.apple.security.application-groups</key>
<array>
    <string>group.se.sandenskog.hotandcoldfriends</string>
</array>
```

**6. Uppdatera Info.plist** — lägg till URL-schema `hotandcold` för widget deep links:

Lägg till en ny `<dict>` i befintliga `CFBundleURLTypes`-arrayen:
```xml
<dict>
    <key>CFBundleURLSchemes</key>
    <array>
        <string>hotandcold</string>
    </array>
</dict>
```

**7. Lägg till `.onOpenURL` i HotAndColdFriendsApp.swift** för att hantera widget deep links:

I HotAndColdFriendsApp body, lägg till `.onOpenURL` modifier på AppRouter:
```swift
AppRouter()
    // ... befintliga .environment() ...
    .onOpenURL { url in
        // Parsa hotandcold://friend/<id>
        guard url.scheme == "hotandcold",
              url.host == "friend",
              let friendId = url.pathComponents.dropFirst().first else {
            return
        }
        // Posta notifikation som AppRouter redan lyssnar på
        NotificationCenter.default.post(
            name: .openWeatherAlert,
            object: friendId
        )
    }
    .task { ... }  // befintlig .task
```

**OBS:** Återanvänder `.openWeatherAlert` Notification.Name som redan finns i AppRouter — den navigerar till väderdetalj för given friend-ID, exakt vad widget-deep-linken behöver. Ingen ny NotificationCenter-hantering behövs.

**8. Skapa PrivacyInfo.xcprivacy** (`HotAndColdFriends/Resources/PrivacyInfo.xcprivacy`):

```xml
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN"
  "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>NSPrivacyTracking</key>
    <false/>
    <key>NSPrivacyTrackingDomains</key>
    <array/>
    <key>NSPrivacyCollectedDataTypes</key>
    <array/>
    <key>NSPrivacyAccessedAPITypes</key>
    <array>
        <dict>
            <key>NSPrivacyAccessedAPIType</key>
            <string>NSPrivacyAccessedAPICategoryUserDefaults</string>
            <key>NSPrivacyAccessedAPITypeReasons</key>
            <array>
                <string>CA92.1</string>
            </array>
        </dict>
    </array>
</dict>
</plist>
```

CA92.1 = "Access info from same app or app group" — korrekt reason för widget-datadelning via App Group UserDefaults.

**9. Kör xcodegen:**
```bash
cd /Users/richardsandenskog/Claude/weatherfriends && xcodegen generate
```
  </action>
  <verify>
    <automated>cd /Users/richardsandenskog/Claude/weatherfriends && xcodegen generate 2>&1 && xcodebuild build -project HotAndColdFriends.xcodeproj -scheme HotAndColdFriends -destination 'platform=iOS Simulator,name=iPhone 16' -quiet 2>&1 | tail -10</automated>
  </verify>
  <done>
    - project.yml har HotAndColdFriendsWidgetExtension target med App Group-entitlement
    - HotAndColdFriends.entitlements innehåller App Group
    - WidgetFriendEntry.swift finns och är delad mellan app och widget
    - FriendListViewModel skriver favoriter till delade UserDefaults efter load()
    - HotAndColdFriendsWidget.swift har TimelineProvider med 30-min uppdatering
    - WidgetViews.swift har small/medium/large-layouter
    - Widget deep links via hotandcold://friend/{id} URL-schema
    - .onOpenURL i HotAndColdFriendsApp parsar widget-URL och navigerar
    - PrivacyInfo.xcprivacy deklarerar UserDefaults API
    - Projektet kompilerar utan fel
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verifiera widget på simulator</name>
  <files>Ingen filändring — manuell verifiering</files>
  <action>
Verifiera att widgeten fungerar korrekt på simulator. Se how-to-verify nedan.
  </action>
  <what-built>
    iOS hemskärmswidget med tre storlekar (small/medium/large) som visar favoriters väder. Widget deep links öppnar vännens väderdetalj i appen. Privacy manifest inkluderat.
  </what-built>
  <how-to-verify>
    **Förutsättning:** App Group måste vara registrerad i Apple Developer Portal (se user_setup ovan). Om detta inte gjorts ännu, kompileras projektet men widgeten kan inte dela data med appen.

    1. **Bygg och kör i simulator** (Xcode → Product → Run med HotAndColdFriends-schemat)
    2. **Öppna appen** och logga in — vänta tills vännerlistan laddats (favoriter syns)
    3. **Gå till hemskärmen** i simulatorn (Cmd+Shift+H)
    4. **Lägg till widget:**
       - Håll ner fingret på hemskärmen → "+" i övre vänstra hörnet
       - Sök efter "Hot & Cold Friends"
       - Lägg till small, medium och large — verifiera att favoriters namn, temperatur och väderikon visas
    5. **Tappa på en vän i widgeten** — verifiera att appen öppnas och navigerar till vännens väderdetalj
    6. **Kontrollera tom widget:** Om inga favoriter finns, ska widgeten visa "Inga favoriter" med instruktion
    7. **Kontrollera att widgeten visar initialer** (bokstavscirkel) istället för profilbild — detta är korrekt v1-beteende
  </how-to-verify>
  <verify>
    <automated>cd /Users/richardsandenskog/Claude/weatherfriends && xcodebuild build -project HotAndColdFriends.xcodeproj -scheme HotAndColdFriends -destination 'platform=iOS Simulator,name=iPhone 16' -quiet 2>&1 | tail -5</automated>
  </verify>
  <done>Widgeten visas korrekt i alla tre storlekar och deep links fungerar</done>
  <resume-signal>Skriv "approved" om widgeten fungerar, eller beskriv eventuella problem</resume-signal>
</task>

</tasks>

<verification>
1. `xcodegen generate` lyckas utan fel
2. `xcodebuild build` kompilerar båda targets (app + widget) utan fel
3. Widget-target finns i Xcode-projektet med korrekt bundle ID (se.sandenskog.hotandcoldfriends.widget)
4. App Group-entitlement finns i båda targets
5. FriendListViewModel skriver WidgetFriendEntry-data till delade UserDefaults
6. Widget visar favoriters namn, temperatur, stad och väderikon i alla tre storlekar
7. Widget deep link öppnar väderdetalj i appen
8. PrivacyInfo.xcprivacy finns med UserDefaults API-deklaration
9. Tom widget visar "Inga favoriter"-meddelande
</verification>

<success_criteria>
- WDGT-01 uppfyllt: iOS hemskärmswidget visar favoriters väder
- Tre storlekar stöds: small (1 favorit), medium (3-4), large (6)
- Deep link-navigation: tap → appen öppnas → väderdetalj
- Widget-data synkas via App Group UserDefaults
- Privacy manifest inkluderat för App Store-submission
- Projektet kompilerar utan fel
</success_criteria>

<output>
After completion, create `.planning/phases/06-polish-app-store/06-02-SUMMARY.md`
</output>
