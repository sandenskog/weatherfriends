---
phase: 14-phase10-verify-avatar-fix
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - HotAndColdFriends/Features/Profile/ProfileView.swift
  - HotAndColdFriends/Features/Animations/MotionReducer.swift
autonomous: true
requirements:
  - COMP-06
gap_closure: true

must_haves:
  truths:
    - "ProfileView visar gradient-avatar med temperaturzon-farg (arctic fallback) istallet for gra cirkel"
    - "MotionReducer.swift innehaller bara MotionReducedModifier och CrossfadeIfReducedModifier structs — inga oanvanda View extensions"
  artifacts:
    - path: "HotAndColdFriends/Features/Profile/ProfileView.swift"
      provides: "AvatarView integration replacing initialsCircle()"
      contains: "AvatarView("
    - path: "HotAndColdFriends/Features/Animations/MotionReducer.swift"
      provides: "Only used modifiers remain"
  key_links:
    - from: "HotAndColdFriends/Features/Profile/ProfileView.swift"
      to: "HotAndColdFriends/DesignSystem/AvatarView.swift"
      via: "AvatarView component usage"
      pattern: "AvatarView\\(displayName:"
---

<objective>
Fix ProfileView to use AvatarView (gradient avatar) instead of custom initialsCircle(), and remove dead code from MotionReducer.

Purpose: Close COMP-06 integration gap (ProfileView missing Bubble Pop gradient avatar) and clean up unused MotionReducer convenience methods identified in milestone audit.
Output: Two files updated, build succeeds.
</objective>

<execution_context>
@/Users/richardsandenskog/.claude/get-shit-done/workflows/execute-plan.md
@/Users/richardsandenskog/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v2.0-MILESTONE-AUDIT.md

<interfaces>
<!-- AvatarView interface from DesignSystem -->
From HotAndColdFriends/DesignSystem/AvatarView.swift:
```swift
struct AvatarView: View {
    let displayName: String
    let temperatureCelsius: Double?
    var size: CGFloat = 52
    var photoURL: String? = nil
}
```
Note: When temperatureCelsius is nil, AvatarView falls back to .arctic zone gradient.

From HotAndColdFriends/Models/AppUser.swift:
```swift
struct AppUser: Codable, Identifiable {
    @DocumentID var id: String?
    var displayName: String
    var photoURL: String?
    var city: String
    var cityLatitude: Double?
    var cityLongitude: Double?
    var authProvider: String
    var fcmToken: String?
    var profileImageURL: URL? { ... }  // computed
}
```
Note: AppUser has NO temperature field. Pass nil to AvatarView for temperatureCelsius.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace ProfileView initialsCircle() with AvatarView</name>
  <files>HotAndColdFriends/Features/Profile/ProfileView.swift</files>
  <action>
Replace the `profileImageView(user:)` method to use `AvatarView` instead of the custom `initialsCircle()`.

1. Replace the entire `profileImageView(user:)` method body:
   - Remove the AsyncImage/initialsCircle logic
   - Replace with: `AvatarView(displayName: user.displayName, temperatureCelsius: nil, size: 100, photoURL: user.photoURL)`
   - AvatarView already handles photoURL with AsyncImage fallback internally, so ProfileView no longer needs its own AsyncImage logic

2. Delete the `initialsCircle(name:size:)` method (lines 186-195) — no longer needed.

3. Delete the `initials(from:)` method (lines 197-206) — no longer needed (AvatarView has its own initials computation).

The resulting `profileImageView(user:)` should be a simple one-liner returning AvatarView.
  </action>
  <verify>
    <automated>cd /Users/richardsandenskog/Claude/weatherfriends && xcodebuild build -scheme HotAndColdFriends -destination 'platform=iOS Simulator,name=iPhone 16' -quiet 2>&1 | tail -5</automated>
  </verify>
  <done>ProfileView uses AvatarView with gradient instead of gray initialsCircle(). No initialsCircle or initials methods remain in the file. Build succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: Remove MotionReducer dead code</name>
  <files>HotAndColdFriends/Features/Animations/MotionReducer.swift</files>
  <action>
Remove the unused convenience View extensions and the empty MotionReducer enum:

1. Delete the `enum MotionReducer` block (lines 40-44) — it provides no functionality (just a comment).

2. Delete the entire `extension View` block (lines 48-72) containing:
   - `func motionReduced(...)` — never called anywhere in codebase
   - `func crossfadeIfReduced(...)` — never called anywhere in codebase

3. Keep the two modifier structs that ARE used:
   - `MotionReducedModifier` (lines 8-19) — used by animation code
   - `CrossfadeIfReducedModifier` (lines 25-34) — used by animation code

4. Update the file's top comment if needed to reflect that consumers apply modifiers directly via `modifier(MotionReducedModifier(...))` rather than through convenience extensions.
  </action>
  <verify>
    <automated>cd /Users/richardsandenskog/Claude/weatherfriends && xcodebuild build -scheme HotAndColdFriends -destination 'platform=iOS Simulator,name=iPhone 16' -quiet 2>&1 | tail -5 && grep -c "motionReduced\|crossfadeIfReduced\|enum MotionReducer" HotAndColdFriends/Features/Animations/MotionReducer.swift || echo "0 matches — dead code removed"</automated>
  </verify>
  <done>MotionReducer.swift contains only MotionReducedModifier and CrossfadeIfReducedModifier structs. No unused enum or View extensions. Build succeeds.</done>
</task>

</tasks>

<verification>
- `xcodebuild build` succeeds without errors
- ProfileView.swift contains `AvatarView(` and does NOT contain `initialsCircle` or `Color(.systemGray5)`
- MotionReducer.swift does NOT contain `func motionReduced` or `func crossfadeIfReduced` or `enum MotionReducer`
- grep confirms no other files reference the removed methods
</verification>

<success_criteria>
- ProfileView displays gradient avatar via AvatarView component (nil temperature = arctic gradient)
- MotionReducer has zero dead code
- Full project builds successfully
</success_criteria>

<output>
After completion, create `.planning/phases/14-phase10-verify-avatar-fix/14-01-SUMMARY.md`
</output>
