---
phase: 11-animationer
plan: 02
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - HotAndColdFriends/Features/Animations/ConfettiOverlay.swift
  - HotAndColdFriends/Features/Animations/CloudRefreshModifier.swift
  - HotAndColdFriends/Features/FriendList/FriendsTabView.swift
  - HotAndColdFriends/Features/FriendList/FriendListView.swift
  - HotAndColdFriends/Features/FriendList/AddFriendSheet.swift
  - HotAndColdFriends/Features/ContactImport/ImportReviewView.swift
autonomous: true
requirements:
  - ANIM-02
  - ANIM-04
  - ANIM-05
  - ANIM-06

must_haves:
  truths:
    - "Lagga till en ny van triggar konfetti i temperaturzon-farger"
    - "Byta tab animeras med scale + glow pa aktiv tab"
    - "Sortera vanlistan animeras med staggerad slide (50ms delay per item)"
    - "Pull-to-refresh har custom moln-animation istallet for standard spinner"
  artifacts:
    - path: "HotAndColdFriends/Features/Animations/ConfettiOverlay.swift"
      provides: "Konfetti-overlay med temperaturzon-farger och vaederikon-partiklar"
      min_lines: 60
    - path: "HotAndColdFriends/Features/Animations/CloudRefreshModifier.swift"
      provides: "Custom pull-to-refresh med moln-animation"
      min_lines: 40
  key_links:
    - from: "HotAndColdFriends/Features/FriendList/AddFriendSheet.swift"
      to: "ConfettiOverlay"
      via: "Overlay som triggras nar van skapas"
      pattern: "confetti|Confetti"
    - from: "HotAndColdFriends/Features/FriendList/FriendsTabView.swift"
      to: "shadowGlow"
      via: "Glow-shadow pa aktiv tab som foljer matchedGeometryEffect"
      pattern: "shadowGlow|glowShadow"
    - from: "HotAndColdFriends/Features/FriendList/FriendListView.swift"
      to: "staggered animation"
      via: "ForEach med .transition och delay per index"
      pattern: "stagger|delay.*index"
---

<objective>
Implementera konfetti vid ny van, tab-glow-animation, staggerad listsortering och custom pull-to-refresh moln-animation.

Purpose: Alla kvarvarande animations-requirements som ger appen sin levande, lekfulla kaensla.
Output: Konfetti-overlay, moln-refresh, tab-glow och staggerad lista — alla med Reduce Motion-stod.
</objective>

<execution_context>
@/Users/richardsandenskog/.claude/get-shit-done/workflows/execute-plan.md
@/Users/richardsandenskog/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-animationer/11-CONTEXT.md
@.planning/phases/11-animationer/11-01-SUMMARY.md

@HotAndColdFriends/Features/FriendList/FriendsTabView.swift
@HotAndColdFriends/Features/FriendList/FriendListView.swift
@HotAndColdFriends/Features/FriendList/FriendListViewModel.swift
@HotAndColdFriends/Features/FriendList/AddFriendSheet.swift
@HotAndColdFriends/Features/ContactImport/ImportReviewView.swift
@HotAndColdFriends/DesignSystem/TemperatureZone.swift
@HotAndColdFriends/Features/Animations/MotionReducer.swift

<interfaces>
<!-- Fran plan 11-01 -->
From HotAndColdFriends/Features/Animations/MotionReducer.swift:
```swift
// Central Reduce Motion ViewModifier — anvands av alla animationer
extension View {
    func motionReduced(animation:reducedAnimation:value:) -> some View
    func crossfadeIfReduced(value:) -> some View
}
```

From HotAndColdFriends/DesignSystem/TemperatureZone.swift:
```swift
enum TemperatureZone: String, CaseIterable {
    case tropical, warm, cool, cold, arctic
    var color: Color { ... }
    var gradient: LinearGradient { ... }
}
```

From HotAndColdFriends/Features/FriendList/FriendsTabView.swift:
```swift
// Befintlig matchedGeometryEffect + spring pa tab-switcher:
.matchedGeometryEffect(id: "activeTab", in: tabNamespace)
.shadowGlowPrimary()
// Spring: .spring(response: 0.35, dampingFraction: 0.7)
```

From HotAndColdFriends/Features/FriendList/AddFriendSheet.swift:
```swift
// onAdded callback som kallas nar van skapas:
let onAdded: () -> Void
// I addFriend(): onAdded() + dismiss()
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ConfettiOverlay + tab-glow animation</name>
  <files>
    HotAndColdFriends/Features/Animations/ConfettiOverlay.swift,
    HotAndColdFriends/Features/FriendList/FriendsTabView.swift,
    HotAndColdFriends/Features/FriendList/AddFriendSheet.swift,
    HotAndColdFriends/Features/ContactImport/ImportReviewView.swift
  </files>
  <action>
1. Skapa `ConfettiOverlay.swift` med ren SwiftUI (INTE ConfettiSwiftUI-biblioteket — AnimationKit-paketet ar ej integrerat i projektet):
   - En `View` som tar `isActive: Binding&lt;Bool&gt;` och `zone: TemperatureZone`.
   - Nar `isActive` andras till `true`: spawna ~40-50 partiklar med randomiserade positioner, rotationer och hastigheter.
   - Partiklar: mix av cirklar (8pt), rektanglar (6x10pt) och 3 vaederikoner (sol, snoeflinga, droppe) renderade via `Image("weather_clear_day")`, `Image("weather_snow")`, `Image("weather_rain")` fran asset catalog. Om assets inte finns, anvand SF Symbols som fallback: "sun.max.fill", "snowflake", "drop.fill".
   - Faerger: haemta fran `zone.color` plus en ljusare och moerkare variant (opacity 0.7, 1.0, saturerade).
   - Animation: partiklarna faller nedat med gravity-effekt, fade out efter ~1.5-2s. Anvand TimelineView + Canvas for partikelrendering (samma monster som befintliga WeatherAnimationView-partiklar).
   - Overlay: `.allowsHitTesting(false)` sa det inte blockerar interaktion.
   - Med Reduce Motion: doelj helt (visa ingenting).
   - Extension: `.confettiOverlay(isActive:zone:)` pa View.

2. Integrera konfetti i `AddFriendSheet.swift`:
   - Lagg till `@State private var showConfetti = false` och en fallback-zon `@State private var confettiZone: TemperatureZone = .warm`.
   - I `addFriend()`: efter lyckad `friendService.addFriend()`, satt `showConfetti = true` med vaennens temperaturzon (baserat pa stadens latitud — anvand `.warm` som default om temperatur okand).
   - Laeg `.confettiOverlay(isActive: $showConfetti, zone: confettiZone)` pa NavigationStack.
   - Foerdroej `dismiss()` med ~1.8s sa att konfetti hinner synas: `DispatchQueue.main.asyncAfter(deadline: .now() + 1.8) { dismiss() }`.

3. Integrera konfetti i `ImportReviewView.swift`:
   - Lagg till samma konfetti-pattern. Trigga nar importen lyckas (efter att vanner sparats).
   - Anvand `.warm` som default-zon (blandade vanner).

4. Foerstaerk tab-glow i `FriendsTabView.swift`:
   - Tab-switchern har redan `.shadowGlowPrimary()` pa den aktiva Capsule-bakgrunden. Lagg till en subtil scale-animation pa det aktiva tab-textlagret:
     ```swift
     .scaleEffect(selectedTab == tab ? 1.02 : 1.0)
     ```
   - Lagg till animation pa glow-shadowen sa den foljer tab-bytet mjukt (redan hanterat av matchedGeometryEffect + .spring).
   - Med Reduce Motion: ta bort scale-effekten, behall matchedGeometryEffect men med .easeInOut(duration: 0.25) istallet for spring.
  </action>
  <verify>
    <automated>cd /Users/richardsandenskog/Claude/weatherfriends && xcodebuild build -project HotAndColdFriends.xcodeproj -scheme HotAndColdFriends -destination 'platform=iOS Simulator,name=iPhone 16' -quiet 2>&1 | tail -5</automated>
  </verify>
  <done>
    - ConfettiOverlay.swift existerar med temperaturzon-faergade partiklar och vaederikoner
    - AddFriendSheet visar konfetti nar van laggs till
    - ImportReviewView visar konfetti nar import lyckas
    - Tab-switcher har subtil scale + glow pa aktiv tab
    - Konfetti doeljs helt med Reduce Motion
    - Build SUCCEEDED
  </done>
</task>

<task type="auto">
  <name>Task 2: Staggerad listanimation + custom pull-to-refresh moln</name>
  <files>
    HotAndColdFriends/Features/Animations/CloudRefreshModifier.swift,
    HotAndColdFriends/Features/FriendList/FriendListView.swift
  </files>
  <action>
1. Skapa `CloudRefreshModifier.swift` med:
   - En custom pull-to-refresh-vy som ersatter standard `.refreshable` spinner.
   - Visuellt: ett litet moln (ritat med SwiftUI Shapes — tva overlappande ellipser, liknande CloudDriftView i WeatherAnimationView) som foljer drag-gesten nedat.
   - Under laddning: molnet "regnar" — smaa droppar (3-4 st) faller nedat fran molnet med spring-animation i loop.
   - Nar klart: molnet tonar ut med scale 1.0->0.8 + opacity 1.0->0.
   - Implementera som en ViewModifier som tar en `@Sendable () async -> Void` closure (samma signatur som `.refreshable`).
   - Extension: `.cloudRefreshable(action:)` pa View.
   - Med Reduce Motion: anvand standard `.refreshable` istallet (fall tillbaka till iOS-default).
   - VIKTIGT: `.refreshable` pa List ar djupt integrerat med SwiftUI — om custom implementation ar for komplex, anvand `.refreshable` med en custom ProgressView-style i `.overlay` som visar molnet. Prioritera att det fungerar framfor perfekt custom gesture-handling.

2. Lagg till staggerad listanimation i `FriendListView.swift`:
   - Nar listan laddas (`viewModel.favorites` / `viewModel.others` andras), animera varje rad med staggerad slide-in:
     ```swift
     ForEach(Array(viewModel.others.enumerated()), id: \.element.id) { index, fw in
         FriendRowView(friendWeather: fw)
             .transition(.asymmetric(
                 insertion: .move(edge: .trailing).combined(with: .opacity),
                 removal: .opacity
             ))
             .animation(
                 .spring(response: 0.35, dampingFraction: 0.7).delay(Double(index) * 0.05),
                 value: viewModel.refreshToken
             )
     }
     ```
   - Anvand `viewModel.refreshToken` (redan UUID i FriendListViewModel) som animationsvarde — det andras vid varje load/refresh.
   - Samma monster for `favoritesSection`.
   - Med Reduce Motion: ta bort delay och anvand enkel crossfade (.opacity transition).
   - Ersatt `.refreshable` med `.cloudRefreshable(action:)` (eller behall `.refreshable` om custom pull-to-refresh ar for komplex att integrera med List).
  </action>
  <verify>
    <automated>cd /Users/richardsandenskog/Claude/weatherfriends && xcodebuild build -project HotAndColdFriends.xcodeproj -scheme HotAndColdFriends -destination 'platform=iOS Simulator,name=iPhone 16' -quiet 2>&1 | tail -5</automated>
  </verify>
  <done>
    - CloudRefreshModifier.swift existerar med moln + regn-animation
    - FriendListView har staggerad slide-animation pa listrader med 50ms delay per item
    - FriendListView anvander cloudRefreshable eller visar moln-animation under refresh
    - Staggerad animation respekterar Reduce Motion (crossfade istallet)
    - Build SUCCEEDED
  </done>
</task>

</tasks>

<verification>
- Alla animationsfiler existerar under Features/Animations/
- Konfetti triggas i AddFriendSheet och ImportReviewView
- Tab-switcher har glow + scale pa aktiv tab
- Listanimation ar staggerad med 50ms delay
- Pull-to-refresh har moln-animation
- Alla animationer har Reduce Motion-fallback
- Projektet bygger utan errors
</verification>

<success_criteria>
- `xcodebuild build` SUCCEEDED
- Konfetti-overlay ar kopplat till AddFriendSheet och ImportReviewView
- Tab-glow foljer pill-markern med scale-effekt
- Listrader animeras med staggerad slide vid sortering/refresh
- Pull-to-refresh visar moln istallet for standard spinner
- Reduce Motion: crossfade overallt, konfetti doljs helt
</success_criteria>

<output>
After completion, create `.planning/phases/11-animationer/11-02-SUMMARY.md`
</output>
