---
phase: 11-animationer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - HotAndColdFriends/Features/Animations/MotionReducer.swift
  - HotAndColdFriends/Features/Animations/HeartPopModifier.swift
  - HotAndColdFriends/Features/Animations/StickerBounceModifier.swift
  - HotAndColdFriends/Features/Animations/WeatherAnimationView.swift
  - HotAndColdFriends/Features/FriendList/FriendListView.swift
  - HotAndColdFriends/Features/Chat/ChatBubbleView.swift
autonomous: true
requirements:
  - ANIM-01
  - ANIM-03
  - ANIM-07

must_haves:
  truths:
    - "Trycka pa favorit-hjartat ger en tydlig pop-animation (shrink -> overshoot -> settle)"
    - "Skicka en sticker animeras med bounce-in (fade + slide upp -> overshoot -> settle)"
    - "Med Reduce Motion aktiverat visas crossfade istallet for slide/bounce i alla animationer"
  artifacts:
    - path: "HotAndColdFriends/Features/Animations/MotionReducer.swift"
      provides: "Central ViewModifier som alla animerade vyer anvander for Reduce Motion fallback"
      min_lines: 20
    - path: "HotAndColdFriends/Features/Animations/HeartPopModifier.swift"
      provides: "Heart pop spring animation (0.6x -> 1.3x -> 1.0x)"
      min_lines: 25
    - path: "HotAndColdFriends/Features/Animations/StickerBounceModifier.swift"
      provides: "Bounce-in animation for stickers i chatt"
      min_lines: 25
  key_links:
    - from: "HotAndColdFriends/Features/FriendList/FriendListView.swift"
      to: "HeartPopModifier"
      via: "ViewModifier pa favorit-toggle swipe action"
      pattern: "heartPop|HeartPop"
    - from: "HotAndColdFriends/Features/Chat/ChatBubbleView.swift"
      to: "StickerBounceModifier"
      via: "ViewModifier pa WeatherStickerView"
      pattern: "stickerBounce|StickerBounce"
    - from: "HotAndColdFriends/Features/Animations/WeatherAnimationView.swift"
      to: "MotionReducer"
      via: "Migrering fran lokal reduceMotion-check till central modifier"
      pattern: "motionReduced|MotionReducer"
---

<objective>
Skapa central MotionReducer-modifier, heart-pop favorit-animation och sticker bounce-in animation.

Purpose: Etablera animationssprak och Reduce Motion-infrastruktur som alla efterfoljande animationer bygger pa.
Output: Tre nya ViewModifiers + integrering i FriendListView och ChatBubbleView.
</objective>

<execution_context>
@/Users/richardsandenskog/.claude/get-shit-done/workflows/execute-plan.md
@/Users/richardsandenskog/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-animationer/11-CONTEXT.md

@HotAndColdFriends/Features/Animations/WeatherAnimationView.swift
@HotAndColdFriends/Features/FriendList/FriendListView.swift
@HotAndColdFriends/Features/FriendList/FriendListViewModel.swift
@HotAndColdFriends/Features/Chat/ChatBubbleView.swift
@HotAndColdFriends/Features/Chat/ChatView.swift
@HotAndColdFriends/Features/Chat/WeatherStickerView.swift
@HotAndColdFriends/DesignSystem/BubblePopButton.swift

<interfaces>
<!-- BubblePopButton spring-referens for animationssprak -->
From HotAndColdFriends/DesignSystem/BubblePopButton.swift:
```swift
.animation(.spring(response: 0.25, dampingFraction: 0.6), value: isPressed)
```

From HotAndColdFriends/Features/Animations/WeatherAnimationView.swift:
```swift
@Environment(\.accessibilityReduceMotion) private var reduceMotion
// Befintlig Reduce Motion-check som ska migreras till central MotionReducer
```

From HotAndColdFriends/Features/FriendList/FriendListView.swift:
```swift
// Favorit-toggle via swipe actions i favoritesSection och othersSection
.swipeActions(edge: .trailing, allowsFullSwipe: true) {
    Button { Task { await viewModel.toggleFavorite(...) } }
}
```

From HotAndColdFriends/Features/Chat/ChatBubbleView.swift:
```swift
// Sticker renderas i bubbleContent:
if message.type == .weatherSticker, let weatherData = message.weatherData {
    WeatherStickerView(weatherData: weatherData)
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: MotionReducer ViewModifier + HeartPopModifier</name>
  <files>
    HotAndColdFriends/Features/Animations/MotionReducer.swift,
    HotAndColdFriends/Features/Animations/HeartPopModifier.swift,
    HotAndColdFriends/Features/Animations/WeatherAnimationView.swift,
    HotAndColdFriends/Features/FriendList/FriendListView.swift
  </files>
  <action>
1. Skapa `MotionReducer.swift` med:
   - En `ViewModifier` som laser `@Environment(\.accessibilityReduceMotion)` och exponerar det som en conditional animation helper.
   - Extension pa `View`: `.motionReduced(animation:reducedAnimation:value:)` som valjer spring-animation eller crossfade (.opacity med ~0.25s) baserat pa Reduce Motion-installning.
   - En convenience `.crossfadeIfReduced(value:)` for enklaste fallet.
   - En statisk property `MotionReducer.isReduced` via `@Environment` for manuell branching (t.ex. konfetti som ska doljas helt).

2. Skapa `HeartPopModifier.swift` med:
   - En `ViewModifier` som tar `isActive: Bool` binding.
   - Nar `isActive` andras till `true`: scale 1.0 -> 0.6 -> 1.3 -> 1.0 med spring (.spring response: 0.3, dampingFraction: 0.5).
   - Implementera med `@State private var scale: CGFloat = 1.0` och en `.onChange(of: isActive)` som trigger sequensen via `withAnimation`.
   - Med Reduce Motion: enkel crossfade (opacity pulse) istallet for scale.
   - Extension: `.heartPop(isActive:)` pa View.

3. Migrera `WeatherAnimationView.swift`:
   - Ersatt den lokala `@Environment(\.accessibilityReduceMotion)` med MotionReducer-patternen. Anvand `@Environment(\.accessibilityReduceMotion)` fortfarande (det ar standard SwiftUI) men gaa genom `.motionReduced()` modifieren eller den centrala metoden for konsistens.
   - Behall all befintlig animationslogik (SunPulseView, CloudDriftView, ParticleAnimationView, ThunderFlashView).

4. Integrera hjart-pop i `FriendListView.swift`:
   - Lagg till `@State private var heartPopFriendId: String?` i FriendListView.
   - I bade `favoritesSection` och `othersSection`, wrappa FriendRowView med `.heartPop(isActive: heartPopFriendId == fw.friend.id)`.
   - I toggleFavorite-callbacken: satt `heartPopFriendId` till vannens ID INNAN toggle-anropet, och nollstall efter ~0.6s med DispatchQueue.main.asyncAfter.
   - Animationen ska triggas bade nar man LAGGAR TILL och TAR BORT favorit.
  </action>
  <verify>
    <automated>cd /Users/richardsandenskog/Claude/weatherfriends && xcodebuild build -project HotAndColdFriends.xcodeproj -scheme HotAndColdFriends -destination 'platform=iOS Simulator,name=iPhone 16' -quiet 2>&1 | tail -5</automated>
  </verify>
  <done>
    - MotionReducer.swift existerar med .motionReduced() och .crossfadeIfReduced() extensions
    - HeartPopModifier.swift existerar med .heartPop(isActive:) extension
    - WeatherAnimationView anvander MotionReducer istallet for lokal reduceMotion-check
    - FriendListView triggar heart-pop vid favorit-toggle
    - Build SUCCEEDED
  </done>
</task>

<task type="auto">
  <name>Task 2: StickerBounceModifier + integration i ChatBubbleView</name>
  <files>
    HotAndColdFriends/Features/Animations/StickerBounceModifier.swift,
    HotAndColdFriends/Features/Chat/ChatBubbleView.swift
  </files>
  <action>
1. Skapa `StickerBounceModifier.swift` med:
   - En `ViewModifier` som animerar bounce-in for stickers.
   - `@State private var appeared = false` — triggras av `.onAppear`.
   - Animation: opacity 0->1 + offset.y 20->0 + scale 0.8->1.05->1.0 med spring (.spring response: 0.4, dampingFraction: 0.55).
   - Med Reduce Motion: enbart opacity 0->1 med .easeInOut(duration: 0.25).
   - Extension: `.stickerBounce()` pa View.

2. Integrera i `ChatBubbleView.swift`:
   - I `bubbleContent`, wrappa WeatherStickerView med `.stickerBounce()`:
     ```swift
     if message.type == .weatherSticker, let weatherData = message.weatherData {
         WeatherStickerView(weatherData: weatherData)
             .stickerBounce()
     }
     ```
   - Ingen annan andring i ChatBubbleView — textbubblor paaverkas INTE.
  </action>
  <verify>
    <automated>cd /Users/richardsandenskog/Claude/weatherfriends && xcodebuild build -project HotAndColdFriends.xcodeproj -scheme HotAndColdFriends -destination 'platform=iOS Simulator,name=iPhone 16' -quiet 2>&1 | tail -5</automated>
  </verify>
  <done>
    - StickerBounceModifier.swift existerar med .stickerBounce() extension
    - ChatBubbleView applicerar .stickerBounce() pa WeatherStickerView
    - Animationen ar fade + slide upp + overshoot med spring, crossfade med Reduce Motion
    - Build SUCCEEDED
  </done>
</task>

</tasks>

<verification>
- Alla tre nya filer (MotionReducer.swift, HeartPopModifier.swift, StickerBounceModifier.swift) existerar under Features/Animations/
- WeatherAnimationView anvander central MotionReducer-pattern
- FriendListView har heart-pop pa favorit-toggle
- ChatBubbleView har sticker-bounce pa vaeder-stickers
- Projektet bygger utan errors
</verification>

<success_criteria>
- `xcodebuild build` SUCCEEDED
- Heart pop-animation ar kopplad till favorit-toggle i bade favoriter- och oevriga-sektionen
- Sticker bounce-animation ar kopplad till WeatherStickerView i ChatBubbleView
- Alla animationer har Reduce Motion-fallback via MotionReducer
</success_criteria>

<output>
After completion, create `.planning/phases/11-animationer/11-01-SUMMARY.md`
</output>
