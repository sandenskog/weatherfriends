---
phase: 10-komponenter
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - HotAndColdFriends/Features/FriendList/FriendsTabView.swift
  - HotAndColdFriendsWidget/WidgetViews.swift
autonomous: true
requirements:
  - COMP-05
  - COMP-07

must_haves:
  truths:
    - "Tab-switcher i FriendsTabView är en custom pill-header med bubblePrimary-glow på aktiv tab"
    - "Small widget har gradient-bakgrund baserad på den visade vännens temperaturzon"
    - "Medium/large widgets har gradient per vän-cell — varje cell visar sin temperaturzon-gradient"
    - "Widget-avatarerna visar initialer på gradient-bakgrund (inte grå opacity-cirkel)"
  artifacts:
    - path: "HotAndColdFriends/Features/FriendList/FriendsTabView.swift"
      provides: "Custom pill-tab-switcher med glow-shadow och matchedGeometryEffect"
      contains: "matchedGeometryEffect"
    - path: "HotAndColdFriendsWidget/WidgetViews.swift"
      provides: "Widget-vyer med temperaturzon-gradient bakgrunder och per-cell gradients"
      contains: "TemperatureZone"
  key_links:
    - from: "FriendsTabView pill-tab"
      to: "selectedTab state"
      via: "matchedGeometryEffect + animation"
      pattern: "matchedGeometryEffect"
    - from: "SmallWidgetView"
      to: "TemperatureZone.gradient"
      via: "ZStack bakgrundslager"
      pattern: "TemperatureZone.*gradient"
---

<objective>
Ersätt den segmenterade standardpickern i FriendsTabView med en custom pill-tab-switcher med glow-shadow, och ge widget-vyerna temperaturzon-gradient som bakgrund.

Purpose: Tab-switcher är det mest synliga navigationselementet i appen — ett gradient-pill med glow-skugga ger direkt Bubble Pop-identitet. Widgets är den synligaste ytan utanför appen.
Output: Uppdaterad FriendsTabView.swift + uppdaterad WidgetViews.swift
</objective>

<execution_context>
@/Users/richardsandenskog/.claude/get-shit-done/workflows/execute-plan.md
@/Users/richardsandenskog/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md

<interfaces>
<!-- Key types executorn behöver. Extraherade från kodbasen. -->

Från HotAndColdFriends/Features/FriendList/FriendsTabView.swift:
```swift
enum FriendsTab: String, CaseIterable {
    case list = "Lista"
    case map = "Karta"
    case categories = "Kategorier"
}

struct FriendsTabView: View {
    @State private var selectedTab: FriendsTab = .list
    // body har Picker("Vy", selection: $selectedTab).pickerStyle(.segmented)
    // detta ska ersättas med custom pill-switcher
}
```

Från HotAndColdFriends/DesignSystem/BubblePopColors.swift:
```swift
static let bubblePrimary = Color(hex: 0xFF6B8A)
static let bubbleSecondary = Color(hex: 0xFF8E6B)
static let bubbleBg = Color(hex: 0xF0F4FF)
static let bubbleSurface = Color(hex: 0xFFFFFF)
static let bubbleTextSecondary = Color(hex: 0x8892A8)
```

Från HotAndColdFriends/DesignSystem/BubblePopShadows.swift:
```swift
func shadowGlowPrimary() -> some View  // brand-glow för aktiv tab
func shadowSm() -> some View
```

Från HotAndColdFriends/DesignSystem/BubblePopSpacing.swift:
```swift
enum Spacing { xs = 4; sm = 8; md = 16; lg = 24 }
enum CornerRadius { sm = 12; md = 20; xl = 50; round = 9999 }
```

Från HotAndColdFriends/DesignSystem/BubblePopTypography.swift:
```swift
static var bubbleButton: Font  // Baloo 2 SemiBold 15pt — används för tab-labels
```

Från HotAndColdFriendsWidget/WidgetViews.swift — befintliga helpers att ersätta:
```swift
// initialsCircle(for:size:) — nuläge: Circle().fill(temperatureColor.opacity(0.15)) + Text(initials)
// temperatureColor(for:) — returnerar Color från RGB-array
// Ska ersättas med TemperatureZone-gradient direkt
```

Från HotAndColdFriends/Models/WidgetFriendEntry.swift:
```swift
struct WidgetFriendEntry: Codable {
    let id: String
    let displayName: String
    let city: String
    let temperatureCelsius: Double?
    let symbolName: String
    let temperatureColorRGB: [Double]  // [r, g, b] — behålls för bakåtkompatibilitet
}
```

Widget-kontext: WidgetViews.swift är i HotAndColdFriendsWidget-target som INTE har tillgång till
DesignSystem-filer direkt (de är i main app target). Lösningen: Definiera `TemperatureZone` inline
i WidgetViews.swift (eller som private extension) — duplicera gradient-logiken för de 5 zonerna
baserat på `temperatureCelsius` från `WidgetFriendEntry`. Alternativt: kontrollera om TemperatureZone.swift
är tillagd i widget-target via project.pbxproj (check: `grep "TemperatureZone" HotAndColdFriends.xcodeproj/project.pbxproj`).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Custom pill-tab-switcher i FriendsTabView med glow-shadow på aktiv tab</name>
  <files>HotAndColdFriends/Features/FriendList/FriendsTabView.swift</files>
  <action>
Ersätt `Picker("Vy", selection: $selectedTab).pickerStyle(.segmented)`-blocket i `FriendsTabView.body` med en custom pill-tab-switcher.

Implementera en privat computed property `tabSwitcher: some View`:

```swift
@Namespace private var tabNamespace

private var tabSwitcher: some View {
    HStack(spacing: Spacing.xs) {
        ForEach(FriendsTab.allCases, id: \.self) { tab in
            Button {
                withAnimation(.spring(response: 0.35, dampingFraction: 0.7)) {
                    selectedTab = tab
                }
            } label: {
                Text(tab.rawValue)
                    .font(.bubbleButton)
                    .foregroundStyle(selectedTab == tab ? .white : .bubbleTextSecondary)
                    .padding(.horizontal, Spacing.md)
                    .padding(.vertical, Spacing.sm)
                    .background {
                        if selectedTab == tab {
                            Capsule()
                                .fill(
                                    LinearGradient(
                                        colors: [.bubblePrimary, .bubbleSecondary],
                                        startPoint: .leading,
                                        endPoint: .trailing
                                    )
                                )
                                .matchedGeometryEffect(id: "activeTab", in: tabNamespace)
                                .shadowGlowPrimary()
                        }
                    }
            }
            .buttonStyle(.plain)
        }
    }
    .padding(Spacing.xs)
    .background(Color.bubbleBg)
    .clipShape(Capsule())
}
```

Ersätt Picker-blocket i body med:
```swift
tabSwitcher
    .padding(.horizontal, Spacing.md)
    .padding(.top, Spacing.sm)
    .padding(.bottom, Spacing.xs)
```

Ta bort `@Namespace`-deklarationen om den redan finns, annars lägg till den som `@Namespace private var tabNamespace` i struct-kroppen.

Viktig notering: `matchedGeometryEffect` kräver att `@Namespace` deklareras i samma View-struct. Om FriendsTabView är en `@Observable`-klass eller har komplexare struktur, anpassa platsen för `@Namespace`.
  </action>
  <verify>
    <automated>xcodebuild -project /Users/richardsandenskog/Claude/weatherfriends/HotAndColdFriends.xcodeproj -scheme HotAndColdFriends -destination 'generic/platform=iOS Simulator' build 2>&1 | tail -5</automated>
  </verify>
  <done>FriendsTabView bygger utan fel, ingen Picker-komponent kvar, custom pill-switcher med gradient och matchedGeometryEffect implementerad</done>
</task>

<task type="auto">
  <name>Task 2: Widget-bakgrunder med temperaturzon-gradient per cell (small/medium/large)</name>
  <files>HotAndColdFriendsWidget/WidgetViews.swift</files>
  <action>
Uppdatera WidgetViews.swift för att visa temperaturzon-gradient som bakgrund.

**Steg 1 — Kontrollera om TemperatureZone är tillgänglig i widget-target:**
```bash
grep -i "TemperatureZone" /Users/richardsandenskog/Claude/weatherfriends/HotAndColdFriends.xcodeproj/project.pbxproj | grep -i "widget"
```

Om TemperatureZone.swift är registrerad i widget-target: Importera och använd direkt.
Om INTE: Definiera en privat helper i WidgetViews.swift:

```swift
// Widget-lokal TemperatureZone gradient helper
private func zoneGradient(celsius: Double?) -> LinearGradient {
    guard let c = celsius else {
        return LinearGradient(colors: [Color(hex: 0x4A6CF7), Color(hex: 0x7B61FF)], startPoint: .topLeading, endPoint: .bottomTrailing)
    }
    let colors: [Color]
    switch c {
    case let t where t > 28: colors = [Color(hex: 0xFF6B6B), Color(hex: 0xFF8E6B)]
    case 20...28:            colors = [Color(hex: 0xFFB347), Color(hex: 0xFFD93D)]
    case 10..<20:            colors = [Color(hex: 0x6BCB77), Color(hex: 0x6B9FE8)]
    case 0..<10:             colors = [Color(hex: 0x6B9FE8), Color(hex: 0x4A6CF7)]
    default:                 colors = [Color(hex: 0x4A6CF7), Color(hex: 0x7B61FF)]
    }
    return LinearGradient(colors: colors, startPoint: .topLeading, endPoint: .bottomTrailing)
}

extension Color {
    fileprivate init(hex: UInt) {
        self.init(red: Double((hex >> 16) & 0xFF)/255, green: Double((hex >> 8) & 0xFF)/255, blue: Double(hex & 0xFF)/255)
    }
}
```

Notera: Om `Color(hex:)` redan är definierad i main target och TemperatureZone-filen inte är i widget-target, behövs en lokal kopia i widget-filen. Om `Color(hex:)` är i widget-target via shared file, hoppa över den definitionen.

**Steg 2 — SmallWidgetView: hel gradient-bakgrund:**
Lägg till gradient-bakgrund på hela vyn. Ersätt den befintliga `VStack`-strukturen med:
```swift
var body: some View {
    if let friend {
        ZStack {
            // Gradient-bakgrund för hela small widget
            zoneGradient(celsius: friend.temperatureCelsius)
                .ignoresSafeArea()

            VStack(spacing: 6) {
                // befintligt innehåll...
            }
        }
        .widgetURL(...)
    } else {
        emptyState
    }
}
```
Uppdatera textfärger till `.white` (namn, stad, temperatur) eftersom bakgrunden nu är färgad.

**Steg 3 — FriendWidgetCell: gradient per cell:**
Ersätt `.frame(maxWidth: .infinity).padding(.vertical, 4)` med ett gradient-bakgrundslager:
```swift
.background(
    RoundedRectangle(cornerRadius: 12)
        .fill(zoneGradient(celsius: friend.temperatureCelsius).opacity(0.85))
)
.padding(2)
```
Uppdatera textfärger i FriendWidgetCell till `.white`.

**Steg 4 — initialsCircle: gradient-bakgrund istället för opacity-cirkel:**
Ersätt `Circle().fill(temperatureColor(for: friend).opacity(0.15))` med:
```swift
Circle().fill(
    LinearGradient(
        colors: [Color.white.opacity(0.3), Color.white.opacity(0.1)],
        startPoint: .topLeading,
        endPoint: .bottomTrailing
    )
)
```
(Vit glasmorphism-cirkel på gradient-bakgrund — initialer i `.white`)

Behåll `temperatureColor(for:)` helper för bakåtkompatibilitet om den används på andra ställen, men initialsCircle och cell-bakgrund ska nu använda gradient-logiken.
  </action>
  <verify>
    <automated>xcodebuild -project /Users/richardsandenskog/Claude/weatherfriends/HotAndColdFriends.xcodeproj -scheme HotAndColdFriendsWidget -destination 'generic/platform=iOS Simulator' build 2>&1 | tail -5</automated>
  </verify>
  <done>Widget-target bygger utan fel, SmallWidgetView har gradient-bakgrund, FriendWidgetCell har gradient per cell, initialer syns vita mot gradient-bakgrund</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Custom pill-tab-switcher i FriendsTabView (gradient pill + glow + matchedGeometryEffect-animation) och widget-vyer med temperaturzon-gradient (small: hel bakgrund, medium/large: gradient per cell). Kör i simulator och kontrollera widget-preview i Xcode.</what-built>
  <how-to-verify>
    1. Öppna appen — Friends-fliken
    2. Tab-switcher ska vara en pill-form med gradient-bakgrund, inte standardsegmenterad kontroll
    3. Tryck på "Karta" och "Kategorier" — aktiv pill ska animeras smidigt med gradient och glow-skugga
    4. För widgets: Kör widget-target i Xcode Preview (välj SmallWidgetView/MediumWidgetView preview)
    5. Small widget: hela bakgrunden ska ha temperaturzon-gradient
    6. Medium/large: varje vän-cell ska ha sin egen gradient i temperaturzonfärg
  </how-to-verify>
  <resume-signal>Skriv "approved" om det ser rätt ut, eller beskriv problem att åtgärda</resume-signal>
</task>

</tasks>

<verification>
- Main app bygger utan fel: `xcodebuild -scheme HotAndColdFriends` → BUILD SUCCEEDED
- Widget bygger utan fel: `xcodebuild -scheme HotAndColdFriendsWidget` → BUILD SUCCEEDED
- FriendsTabView.swift innehåller `matchedGeometryEffect` och inget `pickerStyle(.segmented)`
- WidgetViews.swift innehåller gradient-logik per vän (ej bara solid `temperatureColor`)
</verification>

<success_criteria>
Tab-switcher är en distinkt Bubble Pop-komponent med gradient och glow. Widgets visar temperaturzon-gradient som den primära visuella signaturen — användare ser direkt vilken zon deras vän befinner sig i utan att läsa text.
</success_criteria>

<output>
After completion, create `.planning/phases/10-komponenter/10-03-SUMMARY.md`
</output>
