---
phase: 16-invite-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - HotAndColdFriends/Services/InviteService.swift
  - HotAndColdFriends/App/HotAndColdFriendsApp.swift
  - HotAndColdFriends/Services/ClipboardInviteService.swift
  - HotAndColdFriends/Features/Profile/ProfileView.swift
  - HotAndColdFriends/Features/Friends/AddFriendSheet.swift
  - HotAndColdFriends/Features/FriendList/FriendsTabView.swift
  - project.yml
autonomous: false
requirements:
  - INVT-01
  - INVT-03
  - INVT-04
user_setup:
  - service: apple-developer
    why: "Associated Domains capability maste aktiveras i Apple Developer Portal"
    dashboard_config:
      - task: "Aktivera Associated Domains capability for app ID se.sandenskog.hotandcoldfriends"
        location: "developer.apple.com -> Certificates, Identifiers & Profiles -> Identifiers -> se.sandenskog.hotandcoldfriends -> Capabilities -> Associated Domains (checkmark)"

must_haves:
  truths:
    - "Invite-kod ar permanent — raderas INTE efter redemption"
    - "Flera personer kan anvanda samma invite-kod"
    - "Invite-lank anvander HTTPS Universal Link (apps.sandenskog.se/invite/<token>)"
    - "Appen hanterar bade Universal Links och gamla custom scheme-lankar"
    - "Ny anvandare som installerar via invite-lank blir automatiskt van efter signup (deferred deep link)"
    - "Clipboard-check sker bara en gang (inte vid varje app-start)"
    - "Toast visar 'You are now friends with [name]!' efter auto-redeem"
    - "Invite-kod ar tillganglig pa tre stallen: Profilvyn, AddFriendSheet, och share-knapp i FriendsTabView header"
  artifacts:
    - path: "HotAndColdFriends/Services/InviteService.swift"
      provides: "Refaktorerad invite-service med persistent koder och HTTPS URL"
      contains: "redeemedBy"
    - path: "HotAndColdFriends/Services/ClipboardInviteService.swift"
      provides: "Clipboard-check for deferred deep link"
      contains: "friendscast-invite"
    - path: "HotAndColdFriends/App/HotAndColdFriendsApp.swift"
      provides: "Universal Links-hantering i onOpenURL"
      contains: "apps.sandenskog.se"
    - path: "project.yml"
      provides: "Associated Domains entitlement"
      contains: "associated-domains"
    - path: "HotAndColdFriends/Features/FriendList/FriendsTabView.swift"
      provides: "Invite share-knapp i header toolbar"
      contains: "ShareLink"
  key_links:
    - from: "HotAndColdFriends/App/HotAndColdFriendsApp.swift"
      to: "HotAndColdFriends/Services/InviteService.swift"
      via: "onOpenURL -> redeemInvite"
      pattern: "redeemInvite.*token"
    - from: "HotAndColdFriends/Services/ClipboardInviteService.swift"
      to: "HotAndColdFriends/Services/InviteService.swift"
      via: "clipboard token -> redeemInvite"
      pattern: "redeemInvite"
    - from: "project.yml"
      to: "apps.sandenskog.se AASA"
      via: "Associated Domains entitlement"
      pattern: "applinks:apps\\.sandenskog\\.se"
    - from: "HotAndColdFriends/Features/FriendList/FriendsTabView.swift"
      to: "HotAndColdFriends/Services/InviteService.swift"
      via: "ShareLink with invite URL"
      pattern: "getOrCreateInviteToken"
---

<objective>
Refaktorera iOS invite-systemet: persistent invite-koder (multi-use), Universal Links-hantering, deferred deep link via clipboard, och uppdaterade UI-integrationer for att driva viralitet.

Purpose: Gor invite-floden komplett fran lank till vanskap — bade for anvandare med och utan appen installerad.
Output: Fungerande invite-system med Universal Links, persistent koder, clipboard deferred deep link och uppdaterade UI-platser (Profil, AddFriend, header share-knapp).
</objective>

<execution_context>
@/Users/richardsandenskog/.claude/get-shit-done/workflows/execute-plan.md
@/Users/richardsandenskog/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-invite-foundation/16-CONTEXT.md
@.planning/phases/16-invite-foundation/16-RESEARCH.md

@HotAndColdFriends/Services/InviteService.swift
@HotAndColdFriends/App/HotAndColdFriendsApp.swift
@HotAndColdFriends/Features/FriendList/FriendsTabView.swift
@project.yml

<interfaces>
From HotAndColdFriends/Services/InviteService.swift:
```swift
// Current InviteDocument — will be extended with redeemedBy
struct InviteDocument: Codable {
    var senderUid: String
    var senderDisplayName: String
    var senderCity: String
    @ServerTimestamp var createdAt: Timestamp?
}

// Current InviteError — alreadyRedeemed case should be removed/repurposed
enum InviteError: LocalizedError {
    case invalidToken, alreadyRedeemed, selfInvite, tokenCreationFailed
}

// Current InviteService — @Observable @MainActor class
// Key methods to refactor:
// - createInviteToken(for:userService:) -> String — creates NEW token each time, should be lazy-permanent
// - inviteURL(token:) -> URL — currently returns hotandcold:// scheme, must return HTTPS
// - redeemInvite(token:redeemerUid:friendService:userService:) — deletes doc, must keep + add to redeemedBy
```

From HotAndColdFriends/App/HotAndColdFriendsApp.swift:
```swift
// Current onOpenURL handler — only handles hotandcold:// scheme
.onOpenURL { url in
    guard url.scheme == "hotandcold" else { return }
    // Handles invite and friend deep links
}

// Services available in environment:
// authManager, userService, friendService, inviteService, chatService, etc.
```

From HotAndColdFriends/Features/FriendList/FriendsTabView.swift:
```swift
// Current toolbar structure — has plus-menu (add friend, import contacts) and profile button
.toolbar {
    ToolbarItem(placement: .topBarTrailing) {
        HStack(spacing: 12) {
            Menu { /* add friend, import contacts */ } label: { Image(systemName: "plus") }
            Button { showProfile = true } label: { /* profile icon */ }
        }
    }
}
// Environment services: @Environment(AuthManager.self), @Environment(FriendService.self), etc.
// Invite share button should be added to this toolbar HStack
```

From project.yml entitlements (current):
```yaml
entitlements:
  properties:
    com.apple.developer.weatherkit: true
    com.apple.developer.applesignin: [Default]
    com.apple.security.application-groups: [group.se.sandenskog.hotandcoldfriends]
    # Missing: com.apple.developer.associated-domains
```

Token format used by web clipboard: `friendscast-invite:<token>:<unix_timestamp>`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refaktorera InviteService for persistenta koder och Universal Links</name>
  <files>HotAndColdFriends/Services/InviteService.swift, project.yml</files>
  <action>
1. Uppdatera `InviteDocument` struct:
   - Lagg till `var redeemedBy: [String]` (default = tom array)
   - Behall alla befintliga falt (senderUid, senderDisplayName, senderCity, createdAt)

2. Uppdatera `InviteError`:
   - Ta bort `alreadyRedeemed` case (koder ar nu multi-use)
   - Lagg till `alreadyFriends` case med meddelande "You are already friends with this person."

3. Refaktorera `createInviteToken(for:userService:)` till lazy-permanent modell:
   - Ny signatur: `func getOrCreateInviteToken(for uid: String, userService: UserService) async throws -> String`
   - Forst: sok efter befintligt invite-dokument dar senderUid == uid (query pa invites-collection)
   - Om finns: returnera det dokumentets ID (token)
   - Om inte finns: skapa nytt token (samma 12-char UUID-prefix-logik), spara med `redeemedBy: []`, returnera token
   - Behall gammal `createInviteToken` som private/internal for bakatkompabilitet under migrering

4. Uppdatera `inviteURL(token:)`:
   - Andra fran `hotandcold://invite/\(token)` till `https://apps.sandenskog.se/invite/\(token)`

5. Refaktorera `redeemInvite(...)`:
   - Ta bort den sista raden som gor `db.collection("invites").document(token).delete()`
   - Ersatt med: `try await db.collection("invites").document(token).updateData(["redeemedBy": FieldValue.arrayUnion([redeemerUid])])`
   - Lagg till check: om `redeemerUid` redan finns i `redeemedBy` ELLER redan ar van med sender -> kasta `alreadyFriends`
   - Behall all befintlig logik for mutual friendship creation och contact-merge

6. Uppdatera `project.yml` entitlements — lagg till Associated Domains:
   ```yaml
   com.apple.developer.associated-domains:
     - applinks:apps.sandenskog.se
   ```
   Placera under befintliga entitlement-properties for HotAndColdFriends target.
  </action>
  <verify>
    <automated>cd /Users/richardsandenskog/Claude/weatherfriends && grep -q "redeemedBy" HotAndColdFriends/Services/InviteService.swift && grep -q "apps.sandenskog.se" HotAndColdFriends/Services/InviteService.swift && grep -q "associated-domains" project.yml && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>InviteDocument har redeemedBy-array, invite-koder raderas inte vid redemption, URL ar HTTPS Universal Link, Associated Domains entitlement finns i project.yml</done>
</task>

<task type="auto">
  <name>Task 2: Universal Links-handler, clipboard deferred deep link och UI-uppdateringar</name>
  <files>HotAndColdFriends/App/HotAndColdFriendsApp.swift, HotAndColdFriends/Services/ClipboardInviteService.swift, HotAndColdFriends/Features/Profile/ProfileView.swift, HotAndColdFriends/Features/Friends/AddFriendSheet.swift, HotAndColdFriends/Features/FriendList/FriendsTabView.swift</files>
  <action>
1. Skapa `HotAndColdFriends/Services/ClipboardInviteService.swift`:
   - `@Observable @MainActor class ClipboardInviteService`
   - Metod `checkClipboardForInviteToken() -> String?`:
     - Las `UIPasteboard.general.string`
     - Kontrollera prefix `friendscast-invite:`
     - Parsa format: `friendscast-invite:<token>:<unix_timestamp>`
     - Validera att timestamp ar inom 7 dagar (604800 sekunder)
     - Returnera token om giltigt, nil annars
   - Metod `clearClipboardInvite()`:
     - Om clipboard innehaller `friendscast-invite:` prefix, rensa den (satt till tom strang)
   - Property `hasCheckedClipboard: Bool` — lases/sparas i UserDefaults med nyckel `hasCheckedClipboardInvite`
   - Metod `checkAndRedeemIfNeeded(authUid:inviteService:friendService:userService:) async`:
     - Guard: `!hasCheckedClipboard` — annars return
     - Guard: `let token = checkClipboardForInviteToken()` — annars markera som checked och return
     - Forsk redeem via `inviteService.redeemInvite(...)`
     - Vid lyckas: satt `showFriendAddedToast = true`, `friendAddedName = invite.senderDisplayName`
     - Markera `hasCheckedClipboard = true` (UserDefaults)
     - Rensa clipboard via `clearClipboardInvite()`
   - Published properties for toast: `var showFriendAddedToast = false`, `var friendAddedName = ""`

2. Uppdatera `HotAndColdFriendsApp.swift`:
   - Lagg till `@State private var clipboardInviteService: ClipboardInviteService` (init i init())
   - Lagg till `.environment(clipboardInviteService)` i body
   - Utoka `onOpenURL` handler:
     - Ta BORT `guard url.scheme == "hotandcold" else { return }`
     - Lagg till ny branch for Universal Links:
       ```swift
       if url.host == "apps.sandenskog.se",
          url.pathComponents.count >= 3,
          url.pathComponents[1] == "invite" {
           let token = url.pathComponents[2]
           // Redeem via inviteService (samma logik som befintlig)
       }
       ```
     - Behall befintlig `hotandcold://` hantering for bakatkompabilitet (om url.scheme == "hotandcold")
     - Behall befintlig `hotandcold://friend/` hantering for widget deep links
   - I `.task { }` blocket (efter push notification registration):
     - Lagg till clipboard-check: `await clipboardInviteService.checkAndRedeemIfNeeded(authUid: uid, inviteService: inviteService, friendService: friendService, userService: userService)` — BARA om `authManager.currentUser?.id` finns
   - Lagg till toast-overlay langst ner i body (eller anvand `.overlay`):
     - Visa "You are now friends with [name]!" som en icke-blockerande toast nar `clipboardInviteService.showFriendAddedToast` ar true
     - Auto-dismiss efter 3 sekunder
     - Enkel SwiftUI-implementation: Text i en rundad rektangel med `.transition(.move(edge: .top))` och `.animation`

3. Uppdatera `ProfileView.swift`:
   - Andra fran `createInviteToken` till `getOrCreateInviteToken`
   - ShareLink URL ar redan HTTPS tack vare Task 1-andringen i InviteService

4. Uppdatera `AddFriendSheet.swift`:
   - Lagg till en Section eller knapp for invite-lank:
     - "Invite via Link" med ShareLink som delar invite-URL
     - Anvand `inviteService.getOrCreateInviteToken(for: uid, userService: userService)` for att hamta/skapa token
     - Visa ShareLink med invite-URL nar token ar tillgangligt

5. Lagg till invite share-knapp i `FriendsTabView.swift` header (per user decision i CONTEXT.md):
   - Lagg till `@Environment(InviteService.self) private var inviteService` och `@Environment(UserService.self) private var userService`
   - Lagg till `@State private var inviteToken: String?` och `@State private var inviteURL: URL?`
   - I toolbar HStack (bredvid befintliga plus-menu och profil-knapp), lagg till en ShareLink-knapp med `square.and.arrow.up` systemImage
   - ShareLink ska dela invite-URL:en (HTTPS Universal Link)
   - Ladda invite-token via `.task { }` modifier: `inviteToken = try? await inviteService.getOrCreateInviteToken(for: authManager.currentUser!.id, userService: userService)` och bygg URL via `inviteService.inviteURL(token:)`
   - Placera knappen till vanster om plus-menyn for synlighet (viralitet kraver minimal friktion per CONTEXT.md)
  </action>
  <verify>
    <automated>cd /Users/richardsandenskog/Claude/weatherfriends && grep -q "apps.sandenskog.se" HotAndColdFriends/App/HotAndColdFriendsApp.swift && grep -q "friendscast-invite" HotAndColdFriends/Services/ClipboardInviteService.swift && grep -q "getOrCreateInviteToken" HotAndColdFriends/Features/Profile/ProfileView.swift && grep -q "ShareLink" HotAndColdFriends/Features/FriendList/FriendsTabView.swift && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>onOpenURL hanterar bade Universal Links och custom scheme, ClipboardInviteService checkar clipboard en gang efter signup, toast visar vanskapsbekraftelse, ProfileView, AddFriendSheet och FriendsTabView header anvander permanent invite-kod med HTTPS URL</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verifiera invite-system i Xcode</name>
  <files>N/A</files>
  <action>
    Human verification of the complete invite system. Claude has automated:
    1. InviteService refaktorerad for persistenta koder (multi-use, ej raderade)
    2. Universal Links-hantering i onOpenURL (HTTPS + bakatkompatibel custom scheme)
    3. ClipboardInviteService for deferred deep link (clipboard-check vid forsta start)
    4. Uppdaterade UI-platser (ProfileView, AddFriendSheet, FriendsTabView header) med HTTPS invite-lankar
    5. Associated Domains entitlement i project.yml

    Verification steps:
    1. Kor `xcodegen generate` och oppna projektet i Xcode
    2. Verifiera att projektet bygger utan fel
    3. I Xcode: kontrollera att Signing and Capabilities visar "Associated Domains" med `applinks:apps.sandenskog.se`
    4. Kor appen i simulator:
       a. Ga till profilen — verifiera att invite-lanken ar HTTPS (apps.sandenskog.se/invite/...)
       b. Ga till Add Friend — verifiera att "Invite via Link" finns och genererar HTTPS-lank
       c. Ga till vanlistan (FriendsTabView) — verifiera att share-knapp finns i headern (bredvid plus-menyn)
    5. OBS: Universal Links fungerar INTE i simulator — kraver fysisk enhet + deployed AASA-fil.
  </action>
  <verify>Human confirms build succeeds and invite URLs are HTTPS on all three locations</verify>
  <done>Projektet bygger, invite-lankar ar HTTPS, alla tre UI-platser (Profil, AddFriend, header) visar invite-funktionalitet</done>
</task>

</tasks>

<verification>
1. InviteDocument har redeemedBy-falt och invite-dokument raderas inte vid redemption
2. Invite-URL returnerar HTTPS Universal Link (apps.sandenskog.se/invite/<token>)
3. onOpenURL hanterar bade Universal Links och custom scheme
4. ClipboardInviteService parsar friendscast-invite:<token>:<timestamp> format med 7-dagars TTL
5. Clipboard-check sker en gang (UserDefaults-flagga) — inte vid varje app-start
6. Associated Domains entitlement finns i project.yml
7. ProfileView, AddFriendSheet och FriendsTabView header anvander getOrCreateInviteToken (permanent kod)
8. Invite share-knapp ar synlig i FriendsTabView toolbar
</verification>

<success_criteria>
- Invite-koder ar permanenta och multi-use (redeemedBy-array istallet for delete)
- Invite-URL ar HTTPS Universal Link
- Appen hanterar Universal Links via onOpenURL
- Deferred deep link via clipboard fungerar (check en gang efter signup)
- Toast visar vanskapsbekraftelse vid auto-redeem
- Projektet bygger med Associated Domains entitlement
- Invite-kod tillganglig pa alla tre stallen: Profilvyn, AddFriendSheet, header share-knapp
</success_criteria>

<output>
After completion, create `.planning/phases/16-invite-foundation/16-02-SUMMARY.md`
</output>
