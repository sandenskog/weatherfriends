---
phase: 16-invite-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - website/server.js
  - website/views/invite.ejs
  - website/.well-known/apple-app-site-association
  - website/package.json
  - website/Dockerfile
  - website/.gitignore
  - website/.dockerignore
autonomous: true
requirements:
  - INVT-01
  - INVT-02
user_setup:
  - service: firebase
    why: "Server-side Firestore access for invite token lookup"
    env_vars:
      - name: GOOGLE_APPLICATION_CREDENTIALS
        source: "Firebase Console -> Project Settings -> Service accounts -> Generate new private key"
    dashboard_config:
      - task: "Create a service account with Firestore read-only access"
        location: "Firebase Console -> Project Settings -> Service accounts"

must_haves:
  truths:
    - "AASA-filen serveras korrekt pa apps.sandenskog.se/.well-known/apple-app-site-association med application/json content-type"
    - "Invite-lank med giltig token visar personlig sida med inbjudarens namn och stad"
    - "Invite-lank med ogiltig token visar generisk 404-sida"
    - "OpenGraph-meta-taggar renderas server-side per token for snygga link previews"
    - "iOS-besokare ser App Store-knapp, Android/desktop ser 'only iPhone'-meddelande"
    - "Clipboard-copy av invite-token sker innan App Store-redirect"
  artifacts:
    - path: "website/server.js"
      provides: "Express server med AASA-route, invite-route, statiska filer"
      min_lines: 40
    - path: "website/views/invite.ejs"
      provides: "Dynamisk invite-sida med OG-tags, plattformsdetektering, clipboard-copy"
      min_lines: 50
    - path: "website/.well-known/apple-app-site-association"
      provides: "Apple App Site Association fil for Universal Links"
      contains: "applinks"
    - path: "website/Dockerfile"
      provides: "Node.js Docker-image istallet for nginx"
      contains: "node:20-alpine"
    - path: "website/package.json"
      provides: "Dependencies: express, firebase-admin, ejs"
  key_links:
    - from: "website/server.js"
      to: "Firestore invites collection"
      via: "firebase-admin SDK"
      pattern: "collection.*invites.*doc.*get"
    - from: "website/server.js"
      to: "website/views/invite.ejs"
      via: "res.render"
      pattern: "res\\.render.*invite"
    - from: "website/.well-known/apple-app-site-association"
      to: "iOS app"
      via: "appIDs matching bundle ID"
      pattern: "A473BQKT8M\\.se\\.sandenskog\\.hotandcoldfriends"
---

<objective>
Uppgradera apps.sandenskog.se fran statisk nginx till en Node.js Express-server som servar AASA-filen for Universal Links och en dynamisk invite-fallback-sida med personliga OpenGraph-taggar, plattformsdetektering och clipboard-copy av invite-token.

Purpose: Gor invite-lankar klickbara overallt (iMessage, WhatsApp, etc.) med snygga link previews och leder nya anvandare till App Store med deferred deep link-token i clipboard.
Output: Fungerande Express-server i Docker med AASA-fil, dynamisk invite-sida, och bevarade statiska sidor.
</objective>

<execution_context>
@/Users/richardsandenskog/.claude/get-shit-done/workflows/execute-plan.md
@/Users/richardsandenskog/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-invite-foundation/16-CONTEXT.md
@.planning/phases/16-invite-foundation/16-RESEARCH.md

@website/Dockerfile
@website/index.html
@website/privacy.html
@website/support.html
@HotAndColdFriends/Services/InviteService.swift

<interfaces>
<!-- Existing InviteDocument structure in Firestore (collection: "invites", doc ID = token) -->
<!-- Server needs to read these fields for OG tags -->
From HotAndColdFriends/Services/InviteService.swift:
```swift
struct InviteDocument: Codable {
    var senderUid: String
    var senderDisplayName: String
    var senderCity: String
    @ServerTimestamp var createdAt: Timestamp?
    // Plan 02 will add: var redeemedBy: [String]
}
```

<!-- Token format: 12-char lowercase UUID prefix -->
<!-- URL format: https://apps.sandenskog.se/invite/<token> -->
<!-- App Store URL: https://apps.apple.com/app/id6760045281 -->
<!-- Team ID: A473BQKT8M -->
<!-- Bundle ID: se.sandenskog.hotandcoldfriends -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Express server med AASA-fil och statiska sidor</name>
  <files>website/server.js, website/package.json, website/.well-known/apple-app-site-association, website/Dockerfile, website/.gitignore, website/.dockerignore</files>
  <action>
1. Skapa `website/package.json` med dependencies: express (4.x), firebase-admin (12.x), ejs (3.x). Script: `"start": "node server.js"`.

2. Skapa `website/.well-known/apple-app-site-association` (JSON-fil utan .json-andelse):
```json
{
  "applinks": {
    "details": [
      {
        "appIDs": ["A473BQKT8M.se.sandenskog.hotandcoldfriends"],
        "components": [
          { "/": "/invite/*", "comment": "Invite deep links" }
        ]
      }
    ]
  }
}
```

3. Skapa `website/server.js` med Express:
   - Initiera firebase-admin med `applicationDefault()` credentials (laster fran GOOGLE_APPLICATION_CREDENTIALS env var)
   - Konfigurera ejs som view engine, views-mapp = `views/`
   - Serva AASA-filen pa `/.well-known/apple-app-site-association` med Content-Type `application/json` (INTE redirect, direkt 200)
   - Serva statiska filer fran `public/` (for index.html, privacy.html, support.html)
   - Port: 80 (samma som nuvarande nginx)

4. Flytta befintliga HTML-filer till `website/public/`: index.html, privacy.html, support.html. Behall originalen tills flytten ar verifierad.

5. Uppdatera `website/Dockerfile`:
   - Byt fran `nginx:alpine` till `node:20-alpine`
   - WORKDIR /app
   - COPY package*.json, RUN npm ci --production
   - COPY server.js, views/, public/, .well-known/
   - EXPOSE 80, CMD ["node", "server.js"]

6. Skapa `website/.gitignore` med: node_modules/, firebase-service-account.json, *.log
7. Skapa `website/.dockerignore` med: node_modules/, .git, @eaDir, firebase-service-account.json
  </action>
  <verify>
    <automated>cd /Users/richardsandenskog/Claude/weatherfriends/website && cat .well-known/apple-app-site-association | python3 -m json.tool && node -e "require('./package.json')" && echo "OK"</automated>
  </verify>
  <done>Express server-fil existerar med AASA-route, statiska filer i public/, Dockerfile uppgraderad till Node.js, AASA-fil validerar som korrekt JSON med ratt appID</done>
</task>

<task type="auto">
  <name>Task 2: Dynamisk invite-sida med OG-tags, plattformsdetektering och clipboard</name>
  <files>website/server.js, website/views/invite.ejs</files>
  <action>
1. Lagg till invite-route i `website/server.js`:
   - GET `/invite/:token` — Gor Firestore-uppslag i `invites`-collection pa doc ID = token
   - Om doc INTE finns: render invite.ejs med `{ valid: false }`
   - Om doc finns: extrahera senderDisplayName, senderCity. Detektera plattform via User-Agent (iPhone/iPad/iPod = iOS, Android = Android, ovriga = desktop). Render invite.ejs med `{ valid: true, senderName, senderCity, token, isIOS, isAndroid, appStoreUrl: 'https://apps.apple.com/app/id6760045281' }`
   - Felhantering: catch -> render med `{ valid: false }`

2. Skapa `website/views/invite.ejs`:
   - OpenGraph meta-tags (server-renderade, INTE JavaScript-injicerade):
     - `og:title`: "[senderName] invited you to FriendsCast"
     - `og:description`: "See the weather where [senderName] is in [senderCity]"
     - `og:type`: "website"
     - `og:url`: "https://apps.sandenskog.se/invite/[token]"
   - For ogiltig token: generisk OG-tags ("FriendsCast - Weather + Friends")
   - Body-innehall:
     - Varm, social design (FriendsCast branding, INTE minimalistisk/monokromatisk per MEMORY.md)
     - Visa inbjudarens namn och stad: "[Name] fran [City] bjuder in dig till FriendsCast"
     - Kort app-beskrivning
     - iOS: Stor "Get FriendsCast" App Store-knapp som anropar `redirectToAppStore(token, appStoreUrl)` JavaScript-funktion
     - Android/Desktop: Text "FriendsCast is currently available for iPhone only" med App Store-lank for framtida referens
     - Ogiltig token: "This invite link is no longer valid" med generisk App Store-lank

3. JavaScript i invite.ejs for clipboard-copy:
   - `redirectToAppStore(token, appStoreUrl)` funktion:
     - Kopiera `friendscast-invite:[token]:[unix_timestamp]` till clipboard med `navigator.clipboard.writeText()` (fallback: textarea + execCommand)
     - timestamp = `Math.floor(Date.now() / 1000)` (Unix seconds)
     - Kort delay (500ms) efter clipboard-write for att sakerstalla att det gick igenom
     - Redirect till appStoreUrl via `window.location.href`
   - Prefixet `friendscast-invite:` anvands av iOS-appen for att identifiera invite-tokens i clipboard

4. Responsiv design med inline CSS (ingen extern CSS-fil behovs for en sida):
   - Centrerad layout, max-width 480px
   - Varm, social kansla — anvand ljusa, varma farger (mjuk orange/peach), rundade knappar
   - Mobilvanligh (viewport meta-tag redan satt)
  </action>
  <verify>
    <automated>cd /Users/richardsandenskog/Claude/weatherfriends/website && node -e "const ejs = require('ejs'); const fs = require('fs'); const tpl = fs.readFileSync('views/invite.ejs','utf8'); const html = ejs.render(tpl, {valid:true,senderName:'Test',senderCity:'Stockholm',token:'abc123',isIOS:true,isAndroid:false,appStoreUrl:'https://apps.apple.com/app/id6760045281'}); console.log(html.includes('og:title') && html.includes('Test') && html.includes('clipboard') ? 'PASS' : 'FAIL')"</automated>
  </verify>
  <done>Invite-sidan renderar personliga OG-tags med inbjudarens namn, visar plattformsanpassat innehall, och kopierar invite-token med timestamp till clipboard innan App Store-redirect</done>
</task>

</tasks>

<verification>
1. AASA-filen ar giltig JSON med korrekt appID (A473BQKT8M.se.sandenskog.hotandcoldfriends)
2. Express-servern startar utan fel (node server.js — kraver firebase credentials vid Firestore-anrop)
3. Statiska sidor (index.html, privacy.html, support.html) serveras korrekt
4. Invite-sidan renderar dynamiska OG-tags baserat pa Firestore-data
5. Clipboard-copy-logiken inkluderar token + timestamp med friendscast-invite: prefix
</verification>

<success_criteria>
- AASA-fil serveras pa /.well-known/apple-app-site-association med application/json content-type
- Invite-route renderar personlig sida med OG-tags for giltig token
- Ogiltig token ger 404-sida med generiskt innehall
- iOS-besokare ser App Store-knapp, ovriga ser "iPhone only"-meddelande
- Clipboard-copy sker med friendscast-invite:[token]:[timestamp] format innan redirect
- Dockerfile bygger en fungerande Node.js-container
</success_criteria>

<output>
After completion, create `.planning/phases/16-invite-foundation/16-01-SUMMARY.md`
</output>
