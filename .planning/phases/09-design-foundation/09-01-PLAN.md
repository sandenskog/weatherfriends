---
phase: 09-design-foundation
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - HotAndColdFriends/DesignSystem/BubblePopColors.swift
  - HotAndColdFriends/DesignSystem/BubblePopTypography.swift
  - HotAndColdFriends/DesignSystem/BubblePopSpacing.swift
  - HotAndColdFriends/DesignSystem/BubblePopShadows.swift
  - HotAndColdFriends/DesignSystem/TemperatureZone.swift
  - HotAndColdFriends/Resources/Info.plist
  - HotAndColdFriends/Resources/Fonts/Baloo2-Regular.ttf
  - HotAndColdFriends/Resources/Fonts/Baloo2-Medium.ttf
  - HotAndColdFriends/Resources/Fonts/Baloo2-SemiBold.ttf
  - HotAndColdFriends/Resources/Fonts/Baloo2-Bold.ttf
  - HotAndColdFriends/Resources/Fonts/Baloo2-ExtraBold.ttf
  - HotAndColdFriends/Features/FriendList/FriendRowView.swift
  - HotAndColdFriendsWidget/WidgetViews.swift
  - HotAndColdFriends.xcodeproj/project.pbxproj
autonomous: true
requirements:
  - DSGN-01
  - DSGN-02
  - DSGN-03
  - DSGN-04
  - DSGN-05

must_haves:
  truths:
    - "Alla Bubble Pop färger (temperaturzoner, brand, UI, semantiska, chat) finns som SwiftUI Color-extensions med prefix 'bubble' — inga hårdkodade Color(red:green:blue:) i token-filer"
    - "Baloo 2 fontfiler (.ttf) finns bundlade i Resources/Fonts/ och registrerade i Info.plist UIAppFonts — custom font renderas korrekt"
    - "TemperatureZone-enum returnerar korrekt zon för alla 5 intervall: Tropical (>28°), Warm (20-28°), Cool (10-20°), Cold (0-10°), Arctic (<0°) — med gradient-stöd"
    - "Spacing-enum har xs=4, sm=8, md=16, lg=24, xl=32 och CornerRadius-enum har sm=12, md=20, lg=28, xl=50, round=9999"
    - "Shadow-skala har sm, md, lg, glowPrimary, glowAccent som ViewModifier-extensions"
    - "FriendRowView.swift använder TemperatureZone-färger istället för den gamla Color.temperatureColor(celsius:)"
  artifacts:
    - path: "HotAndColdFriends/DesignSystem/BubblePopColors.swift"
      provides: "Komplett Bubble Pop färgpalett som Color-extensions"
      contains: "Color.bubblePrimary"
    - path: "HotAndColdFriends/DesignSystem/BubblePopTypography.swift"
      provides: "Font-helpers med Baloo 2 för rubriker/knappar och SF Pro för body"
      contains: "Baloo2"
    - path: "HotAndColdFriends/DesignSystem/BubblePopSpacing.swift"
      provides: "8pt grid spacing-konstanter och border radius-skala"
      contains: "Spacing"
    - path: "HotAndColdFriends/DesignSystem/BubblePopShadows.swift"
      provides: "Shadow-skala som ViewModifier-extensions"
      contains: "shadowSm"
    - path: "HotAndColdFriends/DesignSystem/TemperatureZone.swift"
      provides: "TemperatureZone-enum med 5 zoner, färger och gradienter"
      contains: "case tropical"
    - path: "HotAndColdFriends/Resources/Info.plist"
      provides: "UIAppFonts med Baloo 2 font-filnamn"
      contains: "UIAppFonts"
    - path: "HotAndColdFriends/Features/FriendList/FriendRowView.swift"
      provides: "Migrerad FriendRowView med TemperatureZone-färger"
      contains: "TemperatureZone"
  key_links:
    - from: "TemperatureZone.swift TemperatureZone(celsius:)"
      to: "FriendRowView.swift temperaturvisning"
      via: "TemperatureZone ersätter Color.temperatureColor(celsius:)"
      pattern: "TemperatureZone.*celsius"
    - from: "BubblePopTypography.swift Font.bubbleHeading"
      to: "Info.plist UIAppFonts"
      via: "Baloo 2 måste registreras i Info.plist för att custom font ska fungera"
      pattern: "UIAppFonts"
---

<objective>
Etablera det kompletta Bubble Pop designsystemets fundament: färgtokens, Baloo 2 typsnitt, 8pt spacing grid, shadow-skala och TemperatureZone-modell — allt som SwiftUI-extensions och helpers tillgängliga för hela appen (inklusive widget-target).

Purpose: Skapa en enda källa till sanning för alla visuella tokens så att fas 10 kan bygga komponenter utan att hårdkoda färger, storlekar eller typsnitt. Migrera FriendRowView som proof-of-concept.
Output: 5 nya Swift-filer i DesignSystem/, Baloo 2 fontfiler bundlade, Info.plist uppdaterad, FriendRowView migrerad.
</objective>

<execution_context>
@/Users/richardsandenskog/.claude/get-shit-done/workflows/execute-plan.md
@/Users/richardsandenskog/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-design-foundation/09-CONTEXT.md
@Design/friendscast-design-pack/specs/ui-spec-and-components.html

<interfaces>
<!-- Befintliga typer och mönster som executor behöver. Extraherade från kodbas och design-spec. -->

From Design/friendscast-design-pack/specs/ui-spec-and-components.html (CSS variables → Swift tokens):
```css
/* Temperature Scale */
--tropical: #FF6B6B;
--warm: #FFB347;
--cool: #6BCB77;
--cold: #6B9FE8;
--arctic: #4A6CF7;

/* Brand */
--primary: #FF6B8A;
--primary-dark: #E85577;
--secondary: #FF8E6B;
--accent: #FFD93D;

/* UI */
--bg: #F0F4FF;
--surface: #FFFFFF;
--surface-hover: #F7F9FF;
--border: #E8ECF4;
--border-strong: #C0C8D8;
--text-primary: #2D3142;
--text-secondary: #8892A8;
--text-muted: #B0BFD6;

/* Chat */
--chat-mine: linear-gradient(135deg, #FF6B8A, #FF8E6B);
--chat-other-bg: #FFFFFF;
--chat-other-border: #E8ECF4;

/* Semantic */
--success: #6BCB77;
--warning: #FFB347;
--error: #FF6B6B;
--favorite: #FF6B8A;

/* Radius */
--radius-sm: 12px; --radius-md: 20px; --radius-lg: 28px; --radius-xl: 50px; --radius-round: 50%;

/* Shadows */
--shadow-sm: 0 2px 8px rgba(45,49,66,0.06);
--shadow-md: 0 8px 24px rgba(45,49,66,0.08);
--shadow-lg: 0 16px 48px rgba(45,49,66,0.1);
--shadow-glow-primary: 0 8px 24px rgba(255,107,138,0.25);
--shadow-glow-accent: 0 8px 24px rgba(255,217,61,0.25);
```

From HotAndColdFriends/Features/FriendList/FriendRowView.swift:
```swift
// Befintlig Color.temperatureColor(celsius:) — SKA ERSÄTTAS med TemperatureZone:
extension Color {
    static func temperatureColor(celsius: Double) -> Color {
        switch celsius {
        case ..<0:     return Color(red: 0.2, green: 0.4, blue: 1.0)
        case 0..<10:   return Color(red: 0.4, green: 0.6, blue: 0.9)
        case 10..<20:  return Color(red: 0.5, green: 0.7, blue: 0.5)
        case 20..<28:  return Color(red: 1.0, green: 0.6, blue: 0.2)
        default:       return Color(red: 0.9, green: 0.2, blue: 0.2)
        }
    }
}
```

From HotAndColdFriends/Resources/Info.plist:
```xml
<!-- UIAppFonts saknas — måste läggas till för Baloo 2 -->
<!-- Sista entry före </dict></plist> är CFBundleURLTypes -->
```

Temperature zones (09-CONTEXT.md beslut):
- Tropical: >28° (#FF6B6B)
- Warm: 20-28° (#FFB347)
- Cool: 10-20° (#6BCB77)
- Cold: 0-10° (#6B9FE8)
- Arctic: <0° (#4A6CF7)

Typography mapping (09-CONTEXT.md beslut):
- Baloo 2 Bold/ExtraBold → rubriker (H1=.largeTitle, H2=.title2, H3=.title3)
- Baloo 2 SemiBold → knappar, tab-labels, temperaturvärden
- SF Pro (.body, .caption, .footnote) → brödtext, captions
- Inter bundlas INTE — SF Pro ersätter Inter
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ladda ner Baloo 2 fontfiler och registrera i projekt</name>
  <files>
    HotAndColdFriends/Resources/Fonts/Baloo2-Regular.ttf,
    HotAndColdFriends/Resources/Fonts/Baloo2-Medium.ttf,
    HotAndColdFriends/Resources/Fonts/Baloo2-SemiBold.ttf,
    HotAndColdFriends/Resources/Fonts/Baloo2-Bold.ttf,
    HotAndColdFriends/Resources/Fonts/Baloo2-ExtraBold.ttf,
    HotAndColdFriends/Resources/Info.plist,
    HotAndColdFriends.xcodeproj/project.pbxproj
  </files>
  <action>
**Ladda ner Baloo 2 fontfiler:**

1. Skapa katalog `HotAndColdFriends/Resources/Fonts/`
2. Ladda ner Baloo 2 från Google Fonts (OFL-licensierad). Använd `curl` för att hämta TTF-filer direkt:
   - Baloo2-Regular.ttf (400)
   - Baloo2-Medium.ttf (500)
   - Baloo2-SemiBold.ttf (600)
   - Baloo2-Bold.ttf (700)
   - Baloo2-ExtraBold.ttf (800)

   Google Fonts URL-format: `https://github.com/nicholasgross/google-fonts/raw/main/ofl/baloo2/` eller ladda ner via Google Fonts API. Alternativ: `pip3 install gftools` och ladda ner, eller `curl -L "https://fonts.google.com/download?family=Baloo+2"` som zip.

3. Registrera fontfilerna i `Info.plist` — lägg till UIAppFonts-array med alla 5 filnamn:
```xml
<key>UIAppFonts</key>
<array>
    <string>Baloo2-Regular.ttf</string>
    <string>Baloo2-Medium.ttf</string>
    <string>Baloo2-SemiBold.ttf</string>
    <string>Baloo2-Bold.ttf</string>
    <string>Baloo2-ExtraBold.ttf</string>
</array>
```

4. Lägg till fontfilerna i Xcode-projektets Build Phases → Copy Bundle Resources via `project.pbxproj`. Skapa en Fonts-grupp i Xcode-projektet under Resources.

VIKTIGT: Verifiera att fontfilernas PostScript-namn matchar det som används i Swift-koden (troligen "Baloo2-Regular", "Baloo2-Bold" etc). Kör `fc-scan --format "%{postscriptname}\n" <fil>` eller liknande för att verifiera.
  </action>
  <verify>
    <automated>ls /Users/richardsandenskog/Claude/weatherfriends/HotAndColdFriends/Resources/Fonts/Baloo2-*.ttf | wc -l | grep -q "5" && grep -q "UIAppFonts" /Users/richardsandenskog/Claude/weatherfriends/HotAndColdFriends/Resources/Info.plist && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>
    1. 5 Baloo 2 TTF-filer (Regular, Medium, SemiBold, Bold, ExtraBold) finns i HotAndColdFriends/Resources/Fonts/.
    2. Info.plist har UIAppFonts-array med alla 5 filnamn.
    3. Fontfiler är tillagda i project.pbxproj (Copy Bundle Resources).
  </done>
</task>

<task type="auto">
  <name>Task 2: Skapa BubblePopColors.swift med komplett färgpalett</name>
  <files>
    HotAndColdFriends/DesignSystem/BubblePopColors.swift
  </files>
  <action>
Skapa ny fil `HotAndColdFriends/DesignSystem/BubblePopColors.swift` med en `Color`-extension som definierar hela Bubble Pop färgpaletten.

**Alla färger som static computed properties med hex-initialisering:**

Temperature Scale:
- `Color.tempTropical` = #FF6B6B
- `Color.tempWarm` = #FFB347
- `Color.tempCool` = #6BCB77
- `Color.tempCold` = #6B9FE8
- `Color.tempArctic` = #4A6CF7

Brand:
- `Color.bubblePrimary` = #FF6B8A
- `Color.bubblePrimaryDark` = #E85577
- `Color.bubbleSecondary` = #FF8E6B
- `Color.bubbleAccent` = #FFD93D

UI:
- `Color.bubbleBg` = #F0F4FF
- `Color.bubbleSurface` = #FFFFFF
- `Color.bubbleSurfaceHover` = #F7F9FF
- `Color.bubbleBorder` = #E8ECF4
- `Color.bubbleBorderStrong` = #C0C8D8
- `Color.bubbleTextPrimary` = #2D3142
- `Color.bubbleTextSecondary` = #8892A8
- `Color.bubbleTextMuted` = #B0BFD6

Chat:
- `Color.chatMineStart` = #FF6B8A (gradient start)
- `Color.chatMineEnd` = #FF8E6B (gradient end)
- `Color.chatOtherBg` = #FFFFFF
- `Color.chatOtherBorder` = #E8ECF4

Semantic:
- `Color.bubbleSuccess` = #6BCB77
- `Color.bubbleWarning` = #FFB347
- `Color.bubbleError` = #FF6B6B
- `Color.bubbleFavorite` = #FF6B8A

**Inkludera hex Color-initializer** som `private` convenience init:
```swift
private init(hex: UInt, opacity: Double = 1) {
    self.init(
        red: Double((hex >> 16) & 0xFF) / 255,
        green: Double((hex >> 8) & 0xFF) / 255,
        blue: Double(hex & 0xFF) / 255,
        opacity: opacity
    )
}
```

**Inkludera convenience LinearGradient:**
```swift
extension LinearGradient {
    static var chatMine: LinearGradient {
        LinearGradient(colors: [.chatMineStart, .chatMineEnd],
                       startPoint: .topLeading, endPoint: .bottomTrailing)
    }
}
```
  </action>
  <verify>
    <automated>grep -c "Color.bubblePrimary\|Color.tempTropical\|Color.chatMineStart\|Color.bubbleSuccess" /Users/richardsandenskog/Claude/weatherfriends/HotAndColdFriends/DesignSystem/BubblePopColors.swift | grep -q "4" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>
    1. BubblePopColors.swift har alla 26 färgkonstanter (5 temp + 4 brand + 8 UI + 4 chat + 4 semantic + hex init).
    2. LinearGradient.chatMine convenience property finns.
    3. Alla hex-värden matchar design-specen exakt.
  </done>
</task>

<task type="auto">
  <name>Task 3: Skapa TemperatureZone-enum med färger och gradienter</name>
  <files>
    HotAndColdFriends/DesignSystem/TemperatureZone.swift
  </files>
  <action>
Skapa ny fil `HotAndColdFriends/DesignSystem/TemperatureZone.swift`.

**TemperatureZone-enum:**
```swift
import SwiftUI

enum TemperatureZone: String, CaseIterable {
    case tropical
    case warm
    case cool
    case cold
    case arctic

    init(celsius: Double) {
        switch celsius {
        case _ where celsius > 28: self = .tropical
        case 20...28:              self = .warm
        case 10..<20:              self = .cool
        case 0..<10:               self = .cold
        default:                   self = .arctic
        }
    }

    var color: Color {
        switch self {
        case .tropical: return .tempTropical
        case .warm:     return .tempWarm
        case .cool:     return .tempCool
        case .cold:     return .tempCold
        case .arctic:   return .tempArctic
        }
    }

    var gradient: LinearGradient {
        // Tvåfärgs-gradient för avatarer, widgets och bakgrunder
        let colors: [Color]
        switch self {
        case .tropical: colors = [.tempTropical, Color(hex: 0xFF8E6B)]
        case .warm:     colors = [.tempWarm, Color(hex: 0xFFD93D)]
        case .cool:     colors = [.tempCool, Color(hex: 0x6B9FE8)]
        case .cold:     colors = [.tempCold, Color(hex: 0x4A6CF7)]
        case .arctic:   colors = [.tempArctic, Color(hex: 0x7B61FF)]
        }
        return LinearGradient(colors: colors,
                              startPoint: .topLeading,
                              endPoint: .bottomTrailing)
    }

    var label: String {
        rawValue.capitalized
    }
}
```

OBS: `Color(hex:)` init skapas i BubblePopColors.swift. Om den är `private` — gör den `internal` alternativt duplicera hex-värden med `Color(red:green:blue:)` i TemperatureZone. Bäst: gör hex-init `internal` (ej private).
  </action>
  <verify>
    <automated>grep -c "case tropical\|case warm\|case cool\|case cold\|case arctic" /Users/richardsandenskog/Claude/weatherfriends/HotAndColdFriends/DesignSystem/TemperatureZone.swift | grep -q "5" && grep -q "init(celsius:" /Users/richardsandenskog/Claude/weatherfriends/HotAndColdFriends/DesignSystem/TemperatureZone.swift && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>
    1. TemperatureZone-enum med 5 cases (tropical, warm, cool, cold, arctic).
    2. init(celsius:) mappar korrekt: >28=tropical, 20-28=warm, 10-20=cool, 0-10=cold, <0=arctic.
    3. Varje zon har .color och .gradient property.
  </done>
</task>

<task type="auto">
  <name>Task 4: Skapa BubblePopTypography.swift med Baloo 2 och SF Pro helpers</name>
  <files>
    HotAndColdFriends/DesignSystem/BubblePopTypography.swift
  </files>
  <action>
Skapa ny fil `HotAndColdFriends/DesignSystem/BubblePopTypography.swift`.

**Font-extension med Baloo 2 för rubriker och SF Pro för body:**

```swift
import SwiftUI

extension Font {
    // MARK: - Headings (Baloo 2)

    /// H1 — stora rubriker, kartvyns stad, etc.
    static var bubbleH1: Font {
        .custom("Baloo2-ExtraBold", size: 28, relativeTo: .largeTitle)
    }

    /// H2 — sektionstitlar
    static var bubbleH2: Font {
        .custom("Baloo2-Bold", size: 22, relativeTo: .title2)
    }

    /// H3 — korttitlar, dialogtitlar
    static var bubbleH3: Font {
        .custom("Baloo2-Bold", size: 18, relativeTo: .title3)
    }

    // MARK: - Interactive (Baloo 2)

    /// Knappar, tab-labels
    static var bubbleButton: Font {
        .custom("Baloo2-SemiBold", size: 15, relativeTo: .body)
    }

    /// Temperaturvärden
    static var bubbleTemperature: Font {
        .custom("Baloo2-ExtraBold", size: 24, relativeTo: .title2)
    }

    /// Liten temperaturtext (widget, badge)
    static var bubbleTemperatureSmall: Font {
        .custom("Baloo2-Bold", size: 16, relativeTo: .callout)
    }

    // MARK: - Body text (SF Pro / system)

    /// Brödtext
    static var bubbleBody: Font { .body }

    /// Sekundär text
    static var bubbleCaption: Font { .caption }

    /// Liten text (timestamps etc.)
    static var bubbleFootnote: Font { .footnote }
}
```

VIKTIGT: Custom font-namnen i `.custom()` MÅSTE matcha fonternas PostScript-namn. Verifiera med:
```bash
for f in HotAndColdFriends/Resources/Fonts/Baloo2-*.ttf; do
    fc-scan --format "%{postscriptname}\n" "$f" 2>/dev/null || python3 -c "
from fontTools.ttLib import TTFont
t = TTFont('$f')
print(t['name'].getDebugName(6))
"
done
```

Typiska PostScript-namn: "Baloo2-Regular", "Baloo2-Medium", "Baloo2-SemiBold", "Baloo2-Bold", "Baloo2-ExtraBold". Om de avviker (t.ex. med mellanslag), justera `.custom()`-strängarna.
  </action>
  <verify>
    <automated>grep -c "bubbleH1\|bubbleH2\|bubbleH3\|bubbleButton\|bubbleTemperature\b" /Users/richardsandenskog/Claude/weatherfriends/HotAndColdFriends/DesignSystem/BubblePopTypography.swift | grep -q "5" && grep -q "Baloo2" /Users/richardsandenskog/Claude/weatherfriends/HotAndColdFriends/DesignSystem/BubblePopTypography.swift && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>
    1. Font-extensions för headings: bubbleH1 (ExtraBold 28), bubbleH2 (Bold 22), bubbleH3 (Bold 18).
    2. Font-extensions för interactive: bubbleButton (SemiBold 15), bubbleTemperature (ExtraBold 24), bubbleTemperatureSmall (Bold 16).
    3. Font-extensions för body: bubbleBody, bubbleCaption, bubbleFootnote (system fonts).
    4. Alla headings/knappar/temp använder .custom("Baloo2-*") med relativeTo: för Dynamic Type.
  </done>
</task>

<task type="auto">
  <name>Task 5: Skapa BubblePopSpacing.swift med 8pt grid och corner radius</name>
  <files>
    HotAndColdFriends/DesignSystem/BubblePopSpacing.swift
  </files>
  <action>
Skapa ny fil `HotAndColdFriends/DesignSystem/BubblePopSpacing.swift`.

```swift
import SwiftUI

// MARK: - Spacing (8pt grid)

enum Spacing {
    static let xs: CGFloat = 4
    static let sm: CGFloat = 8
    static let md: CGFloat = 16
    static let lg: CGFloat = 24
    static let xl: CGFloat = 32
}

// MARK: - Corner Radius

enum CornerRadius {
    static let sm: CGFloat = 12
    static let md: CGFloat = 20
    static let lg: CGFloat = 28
    static let xl: CGFloat = 50
    static let round: CGFloat = 9999
}
```
  </action>
  <verify>
    <automated>grep -q "Spacing" /Users/richardsandenskog/Claude/weatherfriends/HotAndColdFriends/DesignSystem/BubblePopSpacing.swift && grep -q "CornerRadius" /Users/richardsandenskog/Claude/weatherfriends/HotAndColdFriends/DesignSystem/BubblePopSpacing.swift && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>
    1. Spacing-enum: xs=4, sm=8, md=16, lg=24, xl=32.
    2. CornerRadius-enum: sm=12, md=20, lg=28, xl=50, round=9999.
  </done>
</task>

<task type="auto">
  <name>Task 6: Skapa BubblePopShadows.swift med shadow-skala som ViewModifiers</name>
  <files>
    HotAndColdFriends/DesignSystem/BubblePopShadows.swift
  </files>
  <action>
Skapa ny fil `HotAndColdFriends/DesignSystem/BubblePopShadows.swift`.

```swift
import SwiftUI

extension View {
    /// Liten shadow — kort, subtila element
    func shadowSm() -> some View {
        self.shadow(color: Color(red: 0.176, green: 0.192, blue: 0.259).opacity(0.06),
                    radius: 4, x: 0, y: 2)
    }

    /// Medium shadow — kort, knappar, aktiva element
    func shadowMd() -> some View {
        self.shadow(color: Color(red: 0.176, green: 0.192, blue: 0.259).opacity(0.08),
                    radius: 12, x: 0, y: 8)
    }

    /// Stor shadow — modaler, popovers
    func shadowLg() -> some View {
        self.shadow(color: Color(red: 0.176, green: 0.192, blue: 0.259).opacity(0.1),
                    radius: 24, x: 0, y: 16)
    }

    /// Glow primary — aktiva knappar, favoriter
    func shadowGlowPrimary() -> some View {
        self.shadow(color: Color.bubblePrimary.opacity(0.25),
                    radius: 12, x: 0, y: 8)
    }

    /// Glow accent — speciella interaktioner
    func shadowGlowAccent() -> some View {
        self.shadow(color: Color.bubbleAccent.opacity(0.25),
                    radius: 12, x: 0, y: 8)
    }
}
```

Shadow-värdena översätter CSS `box-shadow: x y blur rgba()` till SwiftUI `.shadow(color:radius:x:y:)` där:
- CSS blur ≈ SwiftUI radius / 2 (CSS blur = 8px → SwiftUI radius = 4, CSS blur = 24px → SwiftUI radius = 12, CSS blur = 48px → SwiftUI radius = 24)
  </action>
  <verify>
    <automated>grep -c "shadowSm\|shadowMd\|shadowLg\|shadowGlowPrimary\|shadowGlowAccent" /Users/richardsandenskog/Claude/weatherfriends/HotAndColdFriends/DesignSystem/BubblePopShadows.swift | grep -q "5" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>
    1. 5 shadow-modifiers: shadowSm, shadowMd, shadowLg, shadowGlowPrimary, shadowGlowAccent.
    2. Alla värden baserade på design-specens CSS shadows.
  </done>
</task>

<task type="auto">
  <name>Task 7: Migrera FriendRowView till TemperatureZone-färger</name>
  <files>
    HotAndColdFriends/Features/FriendList/FriendRowView.swift
  </files>
  <action>
Uppdatera FriendRowView.swift:

1. **Ta bort** den gamla `Color.temperatureColor(celsius:)` extension (rad 3-20).

2. **Ersätt** alla användningar av `Color.temperatureColor(celsius:)` med `TemperatureZone(celsius:).color`.

Specifikt, ändra temperaturvisningens `.foregroundStyle`:
```swift
// Från:
.foregroundStyle(
    friendWeather.temperatureCelsius.map { Color.temperatureColor(celsius: $0) } ?? .secondary
)
// Till:
.foregroundStyle(
    friendWeather.temperatureCelsius.map { TemperatureZone(celsius: $0).color } ?? .bubbleTextSecondary
)
```

3. Sök igenom HELA filen efter eventuella andra användningar av `temperatureColor` och byt ut dem.

4. Om andra filer i projektet refererar till `Color.temperatureColor(celsius:)` — notera dem men migrera dem INTE i denna plan (det görs i fas 10). Kontrollera med: `grep -r "temperatureColor" HotAndColdFriends/`

VIKTIGT: Behåll all befintlig funktionalitet. Ändra BARA färgreferenserna, inte layout eller logik.
  </action>
  <verify>
    <automated>! grep -q "temperatureColor" /Users/richardsandenskog/Claude/weatherfriends/HotAndColdFriends/Features/FriendList/FriendRowView.swift && grep -q "TemperatureZone" /Users/richardsandenskog/Claude/weatherfriends/HotAndColdFriends/Features/FriendList/FriendRowView.swift && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>
    1. Color.temperatureColor(celsius:) extension borttagen från FriendRowView.swift.
    2. Alla färgreferenser i FriendRowView använder TemperatureZone(celsius:).color.
    3. Befintlig layout och funktionalitet oförändrad.
  </done>
</task>

<task type="auto">
  <name>Task 8: Registrera DesignSystem-filer i Xcode-projektet och verifiera kompilering</name>
  <files>
    HotAndColdFriends.xcodeproj/project.pbxproj
  </files>
  <action>
1. Lägg till alla 5 nya Swift-filer i `HotAndColdFriends/DesignSystem/` till Xcode-projektets main target:
   - BubblePopColors.swift
   - BubblePopTypography.swift
   - BubblePopSpacing.swift
   - BubblePopShadows.swift
   - TemperatureZone.swift

2. Lägg ÄVEN till filerna i Widget-target (`HotAndColdFriendsWidget`) så att widget-koden kan använda tokens. Alternativt: skapa en shared framework. Enklast: lägg till filerna i båda targets target membership.

3. Verifiera att projektet kompilerar:
```bash
xcodebuild -project HotAndColdFriends.xcodeproj -scheme HotAndColdFriends -destination 'platform=iOS Simulator,name=iPhone 16' build 2>&1 | tail -20
```

4. Om kompileringsfel uppstår — fixa dem. Vanliga problem:
   - Font PostScript-namn stämmer inte
   - `Color(hex:)` initializer scope (private vs internal)
   - Missing import statements
   - Widget target saknar file references
  </action>
  <verify>
    <automated>cd /Users/richardsandenskog/Claude/weatherfriends && xcodebuild -project HotAndColdFriends.xcodeproj -scheme HotAndColdFriends -destination 'platform=iOS Simulator,name=iPhone 16' build 2>&1 | tail -5 | grep -q "BUILD SUCCEEDED" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>
    1. Alla 5 DesignSystem-filer registrerade i main target.
    2. DesignSystem-filer tillgängliga för widget-target.
    3. Fontfiler i Copy Bundle Resources.
    4. Projektet kompilerar utan fel (BUILD SUCCEEDED).
  </done>
</task>

</tasks>

<verification>
1. `ls HotAndColdFriends/Resources/Fonts/Baloo2-*.ttf | wc -l` returnerar 5
2. `grep "UIAppFonts" HotAndColdFriends/Resources/Info.plist` matchar
3. `grep -c "bubblePrimary\|tempTropical\|chatMineStart" HotAndColdFriends/DesignSystem/BubblePopColors.swift` returnerar minst 3
4. `grep "case tropical" HotAndColdFriends/DesignSystem/TemperatureZone.swift` matchar
5. `grep "Baloo2" HotAndColdFriends/DesignSystem/BubblePopTypography.swift` matchar
6. `grep "Spacing" HotAndColdFriends/DesignSystem/BubblePopSpacing.swift` matchar
7. `grep "shadowSm\|shadowMd\|shadowLg" HotAndColdFriends/DesignSystem/BubblePopShadows.swift` matchar
8. `grep "TemperatureZone" HotAndColdFriends/Features/FriendList/FriendRowView.swift` matchar
9. `! grep "temperatureColor" HotAndColdFriends/Features/FriendList/FriendRowView.swift` — inga gamla referenserna kvar
10. `xcodebuild -project HotAndColdFriends.xcodeproj -scheme HotAndColdFriends -destination 'platform=iOS Simulator,name=iPhone 16' build 2>&1 | tail -5` visar BUILD SUCCEEDED
</verification>

<success_criteria>
- Alla Bubble Pop färgtokens tillgängliga som Color-extensions — konsekvent namngivning med bubble/temp-prefix
- Baloo 2 font fungerar runtime — rubriker, knappar och temperaturvärden renderas i Baloo 2
- 8pt spacing grid och corner radius-skala tillgängliga som CGFloat-konstanter
- Shadow-skala implementerad som ViewModifier-extensions (5 nivåer)
- TemperatureZone-enum mappar celsiusgrader till rätt zon med färg och gradient
- FriendRowView använder nya tokens istället för hårdkodade Color(red:green:blue:)
- Projektet kompilerar utan fel
</success_criteria>

<output>
After completion, create `.planning/phases/09-design-foundation/09-01-SUMMARY.md`
</output>
