---
phase: 09-design-foundation
plan: "02"
type: execute
wave: 2
depends_on:
  - "09-01"
files_modified:
  - HotAndColdFriends/Resources/Assets.xcassets/WeatherIcons/sun-clear.imageset/Contents.json
  - HotAndColdFriends/Resources/Assets.xcassets/WeatherIcons/cloud-sun.imageset/Contents.json
  - HotAndColdFriends/Resources/Assets.xcassets/WeatherIcons/cloud-overcast.imageset/Contents.json
  - HotAndColdFriends/Resources/Assets.xcassets/WeatherIcons/cloud-moon.imageset/Contents.json
  - HotAndColdFriends/Resources/Assets.xcassets/WeatherIcons/moon-clear.imageset/Contents.json
  - HotAndColdFriends/Resources/Assets.xcassets/WeatherIcons/rain.imageset/Contents.json
  - HotAndColdFriends/Resources/Assets.xcassets/WeatherIcons/heavy-rain.imageset/Contents.json
  - HotAndColdFriends/Resources/Assets.xcassets/WeatherIcons/drizzle.imageset/Contents.json
  - HotAndColdFriends/Resources/Assets.xcassets/WeatherIcons/snow.imageset/Contents.json
  - HotAndColdFriends/Resources/Assets.xcassets/WeatherIcons/sleet.imageset/Contents.json
  - HotAndColdFriends/Resources/Assets.xcassets/WeatherIcons/hail.imageset/Contents.json
  - HotAndColdFriends/Resources/Assets.xcassets/WeatherIcons/thunderstorm.imageset/Contents.json
  - HotAndColdFriends/Resources/Assets.xcassets/WeatherIcons/fog.imageset/Contents.json
  - HotAndColdFriends/Resources/Assets.xcassets/WeatherIcons/wind.imageset/Contents.json
  - HotAndColdFriends/Resources/Assets.xcassets/AppIcon.appiconset/Contents.json
  - HotAndColdFriends/Resources/Assets.xcassets/LogoHorizontal.imageset/Contents.json
  - HotAndColdFriends/Resources/Assets.xcassets/EmptyStateFriends.imageset/Contents.json
  - HotAndColdFriends/Resources/Assets.xcassets/EmptyStateChat.imageset/Contents.json
  - HotAndColdFriends/DesignSystem/WeatherIconMapper.swift
  - HotAndColdFriends/Features/Login/LoginView.swift
  - HotAndColdFriends/Features/FriendList/FriendListView.swift
  - HotAndColdFriends/Features/Chat/ConversationListView.swift
  - HotAndColdFriends/Features/FriendList/FriendRowView.swift
  - HotAndColdFriends.xcodeproj/project.pbxproj
autonomous: true
requirements:
  - ASSET-01
  - ASSET-02
  - ASSET-03
  - ASSET-04

must_haves:
  truths:
    - "14 SVG väderikoner finns i Assets.xcassets/WeatherIcons/ och renderas korrekt i alla vyer — inga SF Symbols systemikoner för väder"
    - "App-ikon genererad från SVG som 1024x1024 PNG utan alfakanal i AppIcon.appiconset"
    - "Horisontell logotyp (SVG) visas på login-vy eller onboarding-vy"
    - "Empty state-illustrationer visas i FriendListView (tom vänlista) och ConversationListView (tom chattlista)"
    - "WeatherIconMapper mappar WeatherKit symbolnamn till custom asset-namn — central helper som alla vyer använder"
  artifacts:
    - path: "HotAndColdFriends/Resources/Assets.xcassets/WeatherIcons/"
      provides: "14 SVG väderikoner som asset catalog imagesets"
      contains: "sun-clear.imageset"
    - path: "HotAndColdFriends/Resources/Assets.xcassets/AppIcon.appiconset/"
      provides: "Ny app-ikon från design-pack SVG"
      contains: "AppIcon.png"
    - path: "HotAndColdFriends/Resources/Assets.xcassets/LogoHorizontal.imageset/"
      provides: "Horisontell logotyp SVG"
      contains: "logo-horizontal.svg"
    - path: "HotAndColdFriends/Resources/Assets.xcassets/EmptyStateFriends.imageset/"
      provides: "Empty state illustration för tom vänlista"
      contains: "empty-state-no-friends.svg"
    - path: "HotAndColdFriends/Resources/Assets.xcassets/EmptyStateChat.imageset/"
      provides: "Empty state illustration för tom chattlista"
      contains: "empty-state-no-chat.svg"
    - path: "HotAndColdFriends/DesignSystem/WeatherIconMapper.swift"
      provides: "Central mapping WeatherKit symbolName → custom asset"
      contains: "weatherIconName"
    - path: "HotAndColdFriends/Features/Login/LoginView.swift"
      provides: "Horisontell logotyp på login-vy"
      contains: "LogoHorizontal"
    - path: "HotAndColdFriends/Features/FriendList/FriendListView.swift"
      provides: "Empty state illustration vid tom vänlista"
      contains: "EmptyStateFriends"
    - path: "HotAndColdFriends/Features/Chat/ConversationListView.swift"
      provides: "Empty state illustration vid tom chattlista"
      contains: "EmptyStateChat"
  key_links:
    - from: "WeatherIconMapper.weatherIconName(for:)"
      to: "FriendRowView Image(weatherIconName)"
      via: "WeatherKit symbolName mappas till custom asset-namn"
      pattern: "WeatherIconMapper"
    - from: "Design/friendscast-design-pack/svg-icons/*.svg"
      to: "Assets.xcassets/WeatherIcons/*.imageset/"
      via: "SVG-filer kopieras direkt till asset catalog"
      pattern: "svg"
---

<objective>
Importera alla grafiska assets från design-paketet till iOS asset catalog och integrera dem i appen: 14 väderikoner med central WeatherKit-mapping, ny app-ikon, horisontell logotyp på login-vy, och empty state-illustrationer i tomma listor.

Purpose: Appen ska visuellt använda custom design-assets istället för generiska SF Symbols och visa relevant visuellt innehåll i alla tomma states.
Output: 18 nya asset catalog imagesets, 1 ny Swift-fil (WeatherIconMapper), 3 befintliga vyer uppdaterade, ny app-ikon.
</objective>

<execution_context>
@/Users/richardsandenskog/.claude/get-shit-done/workflows/execute-plan.md
@/Users/richardsandenskog/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-design-foundation/09-CONTEXT.md
@.planning/phases/09-design-foundation/09-01-PLAN.md
@Design/friendscast-design-pack/specs/ui-spec-and-components.html

<interfaces>
<!-- SVG-filer som ska importeras och etablerade mönster i kodbasen. -->

Available SVG icons (Design/friendscast-design-pack/svg-icons/):
- cloud-moon.svg, cloud-overcast.svg, cloud-sun.svg, drizzle.svg
- fog.svg, hail.svg, heavy-rain.svg, moon-clear.svg
- rain.svg, sleet.svg, snow.svg, sun-clear.svg
- thunderstorm.svg, wind.svg

Available SVG UI assets (Design/friendscast-design-pack/svg-ui/):
- app-icon.svg → 1024x1024 PNG (ingen alfakanal)
- logo-horizontal.svg → asset catalog
- empty-state-no-chat.svg → asset catalog
- empty-state-no-friends.svg → asset catalog

From HotAndColdFriends/Features/FriendList/FriendRowView.swift (rad 50-54):
```swift
// Befintlig användning — ska byta till WeatherIconMapper:
Image(systemName: friendWeather.symbolName)
    .font(.body)
    .foregroundStyle(.secondary)
    .frame(width: 24, height: 24)
```

From HotAndColdFriends/Models/FriendWeather.swift (symbolName property):
```swift
// symbolName kommer från WeatherKit — t.ex. "sun.max.fill", "cloud.rain.fill", "snowflake"
var symbolName: String { ... }
```

WeatherKit common symbol names that need mapping:
- "sun.max.fill" / "sun.max" → sun-clear
- "cloud.sun.fill" / "cloud.sun" → cloud-sun
- "cloud.fill" / "cloud" → cloud-overcast
- "cloud.moon.fill" / "cloud.moon" → cloud-moon
- "moon.fill" / "moon.stars.fill" → moon-clear
- "cloud.rain.fill" / "cloud.rain" → rain
- "cloud.heavyrain.fill" / "cloud.heavyrain" → heavy-rain
- "cloud.drizzle.fill" / "cloud.drizzle" → drizzle
- "cloud.snow.fill" / "snowflake" → snow
- "cloud.sleet.fill" / "cloud.sleet" → sleet
- "cloud.hail.fill" / "cloud.hail" → hail
- "cloud.bolt.fill" / "cloud.bolt.rain.fill" → thunderstorm
- "cloud.fog.fill" / "cloud.fog" → fog
- "wind" / "wind.circle" → wind

Asset catalog imageset Contents.json for SVG:
```json
{
  "images": [
    {
      "filename": "<name>.svg",
      "idiom": "universal"
    }
  ],
  "info": { "author": "xcode", "version": 1 },
  "properties": {
    "preserves-vector-representation": true
  }
}
```

From HotAndColdFriends/Features/FriendList/FriendListView.swift (empty state pattern):
```swift
// Appen har troligen en "no friends" text — ersätt med illustration + text
// Sök efter ContentUnavailableView eller liknande empty state
```

From HotAndColdFriends/Features/Chat/ConversationListView.swift:
```swift
// Sök efter empty state / "no conversations" text
```

From HotAndColdFriends/Features/Login/LoginView.swift:
```swift
// Sök efter app-titel eller branding — ersätt/komplettera med horisontell logotyp
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Importera 14 SVG väderikoner till Assets.xcassets</name>
  <files>
    HotAndColdFriends/Resources/Assets.xcassets/WeatherIcons/
  </files>
  <action>
Skapa asset catalog imagesets för alla 14 väderikoner.

För varje ikon i `Design/friendscast-design-pack/svg-icons/`:

1. Skapa katalog `HotAndColdFriends/Resources/Assets.xcassets/WeatherIcons/<name>.imageset/`
2. Kopiera SVG-filen dit
3. Skapa `Contents.json`:
```json
{
  "images": [
    {
      "filename": "<name>.svg",
      "idiom": "universal"
    }
  ],
  "info": {
    "author": "xcode",
    "version": 1
  },
  "properties": {
    "preserves-vector-representation": true
  }
}
```

Gör detta för alla 14:
- sun-clear, cloud-sun, cloud-overcast, cloud-moon, moon-clear
- rain, heavy-rain, drizzle, snow, sleet, hail
- thunderstorm, fog, wind

Skapa också `WeatherIcons`-gruppens Contents.json:
```json
{
  "info": {
    "author": "xcode",
    "version": 1
  }
}
```

Använd ett bash-script för att automatisera:
```bash
SRC=Design/friendscast-design-pack/svg-icons
DST=HotAndColdFriends/Resources/Assets.xcassets/WeatherIcons
mkdir -p "$DST"
for svg in "$SRC"/*.svg; do
    name=$(basename "$svg" .svg)
    mkdir -p "$DST/$name.imageset"
    cp "$svg" "$DST/$name.imageset/"
    cat > "$DST/$name.imageset/Contents.json" << EOF
{
  "images": [{"filename": "$name.svg", "idiom": "universal"}],
  "info": {"author": "xcode", "version": 1},
  "properties": {"preserves-vector-representation": true}
}
EOF
done
cat > "$DST/Contents.json" << EOF
{"info": {"author": "xcode", "version": 1}}
EOF
```
  </action>
  <verify>
    <automated>ls /Users/richardsandenskog/Claude/weatherfriends/HotAndColdFriends/Resources/Assets.xcassets/WeatherIcons/*.imageset | wc -l | tr -d ' ' | grep -q "14" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>
    1. 14 imageset-kataloger skapade i WeatherIcons/.
    2. Varje imageset innehåller SVG-fil + Contents.json med preserves-vector-representation.
    3. WeatherIcons grupp-Contents.json skapad.
  </done>
</task>

<task type="auto">
  <name>Task 2: Skapa WeatherIconMapper för central WeatherKit → custom asset mapping</name>
  <files>
    HotAndColdFriends/DesignSystem/WeatherIconMapper.swift
  </files>
  <action>
Skapa ny fil `HotAndColdFriends/DesignSystem/WeatherIconMapper.swift`.

```swift
import SwiftUI

enum WeatherIconMapper {
    /// Mappar ett WeatherKit SF Symbol-namn till motsvarande custom asset-namn.
    /// Returnerar nil om inget custom asset matchar — caller kan falla tillbaka till SF Symbol.
    static func assetName(for symbolName: String) -> String? {
        // Normalisera: ta bort ".fill" suffix
        let normalized = symbolName.replacingOccurrences(of: ".fill", with: "")

        switch normalized {
        // Clear
        case "sun.max", "sun.min":
            return "sun-clear"
        case "moon", "moon.stars":
            return "moon-clear"

        // Partly cloudy
        case "cloud.sun":
            return "cloud-sun"
        case "cloud.moon":
            return "cloud-moon"

        // Overcast
        case "cloud", "smoke":
            return "cloud-overcast"

        // Rain
        case "cloud.rain":
            return "rain"
        case "cloud.heavyrain":
            return "heavy-rain"
        case "cloud.drizzle":
            return "drizzle"

        // Winter
        case "cloud.snow", "snowflake":
            return "snow"
        case "cloud.sleet":
            return "sleet"
        case "cloud.hail":
            return "hail"

        // Severe
        case "cloud.bolt", "cloud.bolt.rain":
            return "thunderstorm"

        // Atmosphere
        case "cloud.fog":
            return "fog"
        case "wind", "wind.circle":
            return "wind"

        default:
            return nil
        }
    }

    /// Returnerar en Image med custom väderikon, eller faller tillbaka till SF Symbol.
    @ViewBuilder
    static func icon(for symbolName: String, size: CGFloat = 24) -> some View {
        if let asset = assetName(for: symbolName) {
            Image(asset)
                .resizable()
                .aspectRatio(contentMode: .fit)
                .frame(width: size, height: size)
        } else {
            Image(systemName: symbolName)
                .font(.system(size: size * 0.7))
                .frame(width: size, height: size)
        }
    }
}
```

Mappningen täcker de vanligaste WeatherKit-symbolerna. Fallback till SF Symbol garanterar att inga vyer saknar ikon vid ovanliga väderförhållanden.
  </action>
  <verify>
    <automated>grep -c "sun-clear\|cloud-sun\|rain\|snow\|thunderstorm\|fog\|wind" /Users/richardsandenskog/Claude/weatherfriends/HotAndColdFriends/DesignSystem/WeatherIconMapper.swift | xargs test 7 -le && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>
    1. WeatherIconMapper.assetName(for:) mappar alla 14 väderikoner.
    2. WeatherIconMapper.icon(for:size:) returnerar Image med custom asset eller SF Symbol fallback.
    3. Normalisering av ".fill"-suffix hanteras.
  </done>
</task>

<task type="auto">
  <name>Task 3: Generera ny app-ikon från SVG (1024x1024 PNG utan alfakanal)</name>
  <files>
    HotAndColdFriends/Resources/Assets.xcassets/AppIcon.appiconset/AppIcon.png,
    HotAndColdFriends/Resources/Assets.xcassets/AppIcon.appiconset/Contents.json
  </files>
  <action>
Konvertera app-icon.svg till 1024x1024 PNG utan alfakanal.

1. Använd `rsvg-convert` (om installerat) eller `python3` med cairosvg, eller `sips`:

Alternativ A — rsvg-convert (via brew):
```bash
rsvg-convert -w 1024 -h 1024 Design/friendscast-design-pack/svg-ui/app-icon.svg -o /tmp/AppIcon_alpha.png
sips -s format jpeg /tmp/AppIcon_alpha.png --out /tmp/AppIcon.jpg
sips -s format png /tmp/AppIcon.jpg --out HotAndColdFriends/Resources/Assets.xcassets/AppIcon.appiconset/AppIcon.png
```

Alternativ B — python3 med cairosvg:
```bash
pip3 install cairosvg Pillow
python3 -c "
import cairosvg
from PIL import Image
cairosvg.svg2png(url='Design/friendscast-design-pack/svg-ui/app-icon.svg',
                 write_to='/tmp/AppIcon_alpha.png', output_width=1024, output_height=1024)
img = Image.open('/tmp/AppIcon_alpha.png').convert('RGB')
img.save('HotAndColdFriends/Resources/Assets.xcassets/AppIcon.appiconset/AppIcon.png')
"
```

Alternativ C — macOS qlmanage + sips:
```bash
qlmanage -t -s 1024 -o /tmp/ Design/friendscast-design-pack/svg-ui/app-icon.svg
sips -s format jpeg /tmp/app-icon.svg.png --out /tmp/AppIcon.jpg
sips -s format png /tmp/AppIcon.jpg --out HotAndColdFriends/Resources/Assets.xcassets/AppIcon.appiconset/AppIcon.png
```

2. Verifiera att PNG saknar alfakanal:
```bash
sips -g hasAlpha HotAndColdFriends/Resources/Assets.xcassets/AppIcon.appiconset/AppIcon.png
```
Om `hasAlpha: yes` — kör JPEG round-trip igen.

3. Uppdatera Contents.json att referera till AppIcon.png:
```json
{
  "images": [
    {
      "filename": "AppIcon.png",
      "idiom": "universal",
      "platform": "ios",
      "size": "1024x1024"
    }
  ],
  "info": {
    "author": "xcode",
    "version": 1
  }
}
```

4. Ta bort eventuell gammal placeholder-ikon om filnamnet avviker.
  </action>
  <verify>
    <automated>test -f /Users/richardsandenskog/Claude/weatherfriends/HotAndColdFriends/Resources/Assets.xcassets/AppIcon.appiconset/AppIcon.png && sips -g pixelWidth /Users/richardsandenskog/Claude/weatherfriends/HotAndColdFriends/Resources/Assets.xcassets/AppIcon.appiconset/AppIcon.png 2>/dev/null | grep -q "1024" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>
    1. AppIcon.png är 1024x1024 utan alfakanal.
    2. Contents.json refererar korrekt till AppIcon.png.
    3. Gammal placeholder-ikon borttagen (om den fanns).
  </done>
</task>

<task type="auto">
  <name>Task 4: Importera logotyp och empty state-illustrationer till asset catalog</name>
  <files>
    HotAndColdFriends/Resources/Assets.xcassets/LogoHorizontal.imageset/,
    HotAndColdFriends/Resources/Assets.xcassets/EmptyStateFriends.imageset/,
    HotAndColdFriends/Resources/Assets.xcassets/EmptyStateChat.imageset/
  </files>
  <action>
Importera 3 SVG UI-assets till asset catalog.

För varje asset — skapa imageset med SVG direkt:

**1. LogoHorizontal:**
```bash
mkdir -p HotAndColdFriends/Resources/Assets.xcassets/LogoHorizontal.imageset
cp Design/friendscast-design-pack/svg-ui/logo-horizontal.svg HotAndColdFriends/Resources/Assets.xcassets/LogoHorizontal.imageset/
```
Contents.json:
```json
{
  "images": [{"filename": "logo-horizontal.svg", "idiom": "universal"}],
  "info": {"author": "xcode", "version": 1},
  "properties": {"preserves-vector-representation": true}
}
```

**2. EmptyStateFriends:**
```bash
mkdir -p HotAndColdFriends/Resources/Assets.xcassets/EmptyStateFriends.imageset
cp Design/friendscast-design-pack/svg-ui/empty-state-no-friends.svg HotAndColdFriends/Resources/Assets.xcassets/EmptyStateFriends.imageset/
```
Contents.json med `"filename": "empty-state-no-friends.svg"`.

**3. EmptyStateChat:**
```bash
mkdir -p HotAndColdFriends/Resources/Assets.xcassets/EmptyStateChat.imageset
cp Design/friendscast-design-pack/svg-ui/empty-state-no-chat.svg HotAndColdFriends/Resources/Assets.xcassets/EmptyStateChat.imageset/
```
Contents.json med `"filename": "empty-state-no-chat.svg"`.
  </action>
  <verify>
    <automated>test -d /Users/richardsandenskog/Claude/weatherfriends/HotAndColdFriends/Resources/Assets.xcassets/LogoHorizontal.imageset && test -d /Users/richardsandenskog/Claude/weatherfriends/HotAndColdFriends/Resources/Assets.xcassets/EmptyStateFriends.imageset && test -d /Users/richardsandenskog/Claude/weatherfriends/HotAndColdFriends/Resources/Assets.xcassets/EmptyStateChat.imageset && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>
    1. LogoHorizontal.imageset med logo-horizontal.svg.
    2. EmptyStateFriends.imageset med empty-state-no-friends.svg.
    3. EmptyStateChat.imageset med empty-state-no-chat.svg.
    4. Alla har preserves-vector-representation.
  </done>
</task>

<task type="auto">
  <name>Task 5: Integrera WeatherIconMapper i FriendRowView</name>
  <files>
    HotAndColdFriends/Features/FriendList/FriendRowView.swift
  </files>
  <action>
Ersätt SF Symbol-baserad väderikon i FriendRowView med WeatherIconMapper.

Hitta alla förekomster av `Image(systemName: friendWeather.symbolName)` eller liknande väderikon-rendering och ersätt med:

```swift
WeatherIconMapper.icon(for: friendWeather.symbolName, size: 24)
```

T.ex. ändra (ungefärligt rad 50-54):
```swift
// Från:
Image(systemName: friendWeather.symbolName)
    .font(.body)
    .foregroundStyle(.secondary)
    .frame(width: 24, height: 24)

// Till:
WeatherIconMapper.icon(for: friendWeather.symbolName, size: 24)
```

OBS: WeatherIconMapper.icon() returnerar redan en view med rätt storlek — ta bort eventuella överflödiga modifiers (.font, .frame). Behåll dock .foregroundStyle om SVG-ikonerna är template/symbol-mode (tintable). Om SVG-ikonerna har fasta färger — ta bort .foregroundStyle.

Kontrollera om andra vyer också använder `Image(systemName: ...)` för väderikoner:
```bash
grep -rn "symbolName" HotAndColdFriends/Features/ --include="*.swift"
```
Lista alla men ändra BARA FriendRowView i denna plan — övriga vyer migreras i fas 10.
  </action>
  <verify>
    <automated>grep -q "WeatherIconMapper" /Users/richardsandenskog/Claude/weatherfriends/HotAndColdFriends/Features/FriendList/FriendRowView.swift && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>
    1. FriendRowView använder WeatherIconMapper.icon(for:size:) istället för Image(systemName:) för väderikoner.
    2. Eventuella överflödiga modifiers (.font, .frame) borttagna.
  </done>
</task>

<task type="auto">
  <name>Task 6: Lägg till logotyp på LoginView</name>
  <files>
    HotAndColdFriends/Features/Login/LoginView.swift
  </files>
  <action>
Lägg till den horisontella logotypen på LoginView.

1. Läs LoginView.swift och identifiera var branding/titel visas (troligen en Text med appnamn eller ikon).

2. Lägg till logotypen ovanför eller istället för befintlig app-titel:
```swift
Image("LogoHorizontal")
    .resizable()
    .aspectRatio(contentMode: .fit)
    .frame(maxWidth: 220)
    .padding(.bottom, Spacing.lg)
```

Placera logotypen centralt i vyns övre del — ovanför login-knapparna. Behåll eventuell befintlig subtitle/tagline under logotypen.

VIKTIGT: Behåll all befintlig login-funktionalitet. Lägg bara till/ersätt visuell branding.
  </action>
  <verify>
    <automated>grep -q "LogoHorizontal" /Users/richardsandenskog/Claude/weatherfriends/HotAndColdFriends/Features/Login/LoginView.swift && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>
    1. LoginView visar Image("LogoHorizontal") centralt i övre delen.
    2. All login-funktionalitet oförändrad.
  </done>
</task>

<task type="auto">
  <name>Task 7: Lägg till empty state-illustrationer i FriendListView och ConversationListView</name>
  <files>
    HotAndColdFriends/Features/FriendList/FriendListView.swift,
    HotAndColdFriends/Features/Chat/ConversationListView.swift
  </files>
  <action>
**FriendListView — empty state för tom vänlista:**

1. Läs FriendListView.swift och hitta var tom lista renderas (sök efter ContentUnavailableView, "no friends", empty state-text, eller villkor som kollar tom array).

2. Ersätt eller komplettera befintlig empty state med illustration:
```swift
VStack(spacing: Spacing.md) {
    Image("EmptyStateFriends")
        .resizable()
        .aspectRatio(contentMode: .fit)
        .frame(maxWidth: 200)

    Text("No friends yet")
        .font(.bubbleH3)
        .foregroundStyle(.bubbleTextPrimary)

    Text("Add friends to see their weather")
        .font(.bubbleBody)
        .foregroundStyle(.bubbleTextSecondary)
}
.padding(Spacing.xl)
```

**ConversationListView — empty state för tom chattlista:**

1. Läs ConversationListView.swift och hitta empty state.

2. Ersätt eller komplettera med illustration:
```swift
VStack(spacing: Spacing.md) {
    Image("EmptyStateChat")
        .resizable()
        .aspectRatio(contentMode: .fit)
        .frame(maxWidth: 200)

    Text("No conversations yet")
        .font(.bubbleH3)
        .foregroundStyle(.bubbleTextPrimary)

    Text("Start chatting with a friend about the weather")
        .font(.bubbleBody)
        .foregroundStyle(.bubbleTextSecondary)
}
.padding(Spacing.xl)
```

VIKTIGT: Om det redan finns ContentUnavailableView — ersätt den. Om det finns ett if/else-villkor med tom text — integrera illustrationen i det befintliga villkoret. Behåll eventuella "add friend"-knappar i empty state.
  </action>
  <verify>
    <automated>grep -q "EmptyStateFriends" /Users/richardsandenskog/Claude/weatherfriends/HotAndColdFriends/Features/FriendList/FriendListView.swift && grep -q "EmptyStateChat" /Users/richardsandenskog/Claude/weatherfriends/HotAndColdFriends/Features/Chat/ConversationListView.swift && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>
    1. FriendListView visar EmptyStateFriends-illustration vid tom vänlista.
    2. ConversationListView visar EmptyStateChat-illustration vid tom chattlista.
    3. Illustrationer har titel + subtitle-text.
    4. Eventuella befintliga knappar (t.ex. "Add friend") bevarade.
  </done>
</task>

<task type="auto">
  <name>Task 8: Registrera nya filer i Xcode-projektet och verifiera kompilering</name>
  <files>
    HotAndColdFriends.xcodeproj/project.pbxproj
  </files>
  <action>
1. Lägg till `WeatherIconMapper.swift` i Xcode-projektets main target (och widget-target om den behöver väderikoner).

2. Verifiera att alla nya asset catalog-filer (WeatherIcons/, LogoHorizontal, EmptyStateFriends, EmptyStateChat) automatiskt inkluderas av Xcode (asset catalogs scannas automatiskt — men verifiera).

3. Verifiera kompilering:
```bash
xcodebuild -project HotAndColdFriends.xcodeproj -scheme HotAndColdFriends -destination 'platform=iOS Simulator,name=iPhone 16' build 2>&1 | tail -20
```

4. Om kompileringsfel — fixa dem. Vanliga problem:
   - Asset-namn stavfel (case-sensitive)
   - WeatherIconMapper inte i target
   - SVG rendering-problem (kan behöva "Render As: Template Image" i Contents.json för tintable ikoner)
   - Spacing/CornerRadius/Font-extensions från plan 09-01 behövs — verifiera att de finns
  </action>
  <verify>
    <automated>cd /Users/richardsandenskog/Claude/weatherfriends && xcodebuild -project HotAndColdFriends.xcodeproj -scheme HotAndColdFriends -destination 'platform=iOS Simulator,name=iPhone 16' build 2>&1 | tail -5 | grep -q "BUILD SUCCEEDED" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>
    1. WeatherIconMapper.swift registrerad i main target.
    2. Alla asset catalog-resurser tillgängliga.
    3. Projektet kompilerar utan fel (BUILD SUCCEEDED).
  </done>
</task>

</tasks>

<verification>
1. `ls HotAndColdFriends/Resources/Assets.xcassets/WeatherIcons/*.imageset | wc -l` returnerar 14
2. `test -f HotAndColdFriends/Resources/Assets.xcassets/AppIcon.appiconset/AppIcon.png` lyckades
3. `sips -g hasAlpha HotAndColdFriends/Resources/Assets.xcassets/AppIcon.appiconset/AppIcon.png` visar "no" (eller verifiera via JPEG round-trip)
4. `test -d HotAndColdFriends/Resources/Assets.xcassets/LogoHorizontal.imageset` lyckades
5. `test -d HotAndColdFriends/Resources/Assets.xcassets/EmptyStateFriends.imageset` lyckades
6. `test -d HotAndColdFriends/Resources/Assets.xcassets/EmptyStateChat.imageset` lyckades
7. `grep "WeatherIconMapper" HotAndColdFriends/Features/FriendList/FriendRowView.swift` matchar
8. `grep "LogoHorizontal" HotAndColdFriends/Features/Login/LoginView.swift` matchar
9. `grep "EmptyStateFriends" HotAndColdFriends/Features/FriendList/FriendListView.swift` matchar
10. `grep "EmptyStateChat" HotAndColdFriends/Features/Chat/ConversationListView.swift` matchar
11. `xcodebuild ... build` visar BUILD SUCCEEDED
</verification>

<success_criteria>
- 14 custom SVG väderikoner finns i asset catalog och används i FriendRowView via WeatherIconMapper
- WeatherIconMapper mappar alla vanliga WeatherKit symbolnamn med SF Symbol fallback för ovanliga
- Ny app-ikon (1024x1024 PNG utan alfakanal) genererad från design-paketets SVG
- Horisontell logotyp visas på LoginView
- Empty state-illustrationer visas i FriendListView (tom vänlista) och ConversationListView (tom chattlista)
- Projektet kompilerar utan fel
</success_criteria>

<output>
After completion, create `.planning/phases/09-design-foundation/09-02-SUMMARY.md`
</output>
