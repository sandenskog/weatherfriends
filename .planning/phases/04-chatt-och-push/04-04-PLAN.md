---
phase: 04-chatt-och-push
plan: 04
type: execute
wave: 1
depends_on:
  - 04-03
files_modified:
  - HotAndColdFriends/Services/WeatherAlertService.swift
  - HotAndColdFriends/App/HotAndColdFriendsApp.swift
  - functions/src/weatherAlertTrigger.ts
  - functions/src/weatherAlertScheduler.ts
  - functions/src/index.ts
autonomous: true
gap_closure: true
requirements:
  - PUSH-01
  - CHAT-01
  - CHAT-02
  - CHAT-03
  - CHAT-04
  - CHAT-05
  - PUSH-03

must_haves:
  truths:
    - "Nar appen startar kontrolleras WeatherKit weatherAlerts for varje vans koordinater och hasActiveAlert/alertSummary skrivs till Firestore"
    - "Nar hasActiveAlert andras fran false till true pa ett friend-dokument skickas en FCM push-notis till vanens agare"
    - "Max 1 extremvader-notis per van per 24 timmar (rate-limiting via lastAlertSentAt)"
    - "Push-notisen har personlig ton med vannens namn och stad samt deep link till chatten"
    - "Om WeatherKit inte returnerar alerts (nil/tom) skrivs hasActiveAlert: false utan error"
  artifacts:
    - path: "HotAndColdFriends/Services/WeatherAlertService.swift"
      provides: "iOS-service som kontrollerar WeatherKit weatherAlerts och skriver hasActiveAlert till Firestore"
      contains: "weatherAlerts"
    - path: "functions/src/weatherAlertTrigger.ts"
      provides: "Firestore onDocumentUpdated trigger som skickar FCM vid hasActiveAlert-andring"
      contains: "onDocumentUpdated"
    - path: "functions/src/weatherAlertScheduler.ts"
      provides: "Uppdaterad scheduler som refererar till trigger-arkitekturen (ej placeholder)"
      contains: "onSchedule"
    - path: "functions/src/index.ts"
      provides: "Exporterar weatherAlertTrigger utover befintliga funktioner"
      contains: "weatherAlertTrigger"
  key_links:
    - from: "HotAndColdFriends/Services/WeatherAlertService.swift"
      to: "Firestore users/{uid}/friends/{friendId}"
      via: "updateData med hasActiveAlert och alertSummary"
      pattern: "updateData"
    - from: "functions/src/weatherAlertTrigger.ts"
      to: "Firestore users/{uid}/friends/{friendId}"
      via: "onDocumentUpdated trigger"
      pattern: "onDocumentUpdated"
    - from: "functions/src/weatherAlertTrigger.ts"
      to: "FCM sendEachForMulticast"
      via: "firebase-admin/messaging"
      pattern: "sendEachForMulticast"
---

<objective>
Stang PUSH-01-gapet: extremvader-push-notiser. iOS-klienten kontrollerar WeatherKit weatherAlerts vid app-start och skriver hasActiveAlert till Firestore. En ny Cloud Function triggas av Firestore-andringen och skickar FCM-push med rate-limiting.

Purpose: Extremvader-push ar appens unika sociala trigger — "Storm hos Anna i Tokyo — hor av dig!" — och PUSH-01 ar den enda overifierade must-haven fran fas 4. Utan detta levereras inte kravet.
Output: WeatherAlertService (iOS), weatherAlertTrigger (Cloud Function), uppdaterad weatherAlertScheduler.
</objective>

<execution_context>
@/Users/richardsandenskog/.claude/get-shit-done/workflows/execute-plan.md
@/Users/richardsandenskog/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-chatt-och-push/04-CONTEXT.md
@.planning/phases/04-chatt-och-push/04-RESEARCH.md
@.planning/phases/04-chatt-och-push/04-VERIFICATION.md

<interfaces>
<!-- Befintlig Friend-modell — maste utvidgas med hasActiveAlert/alertSummary -->

From HotAndColdFriends/Models/Friend.swift:
```swift
struct Friend: Codable, Identifiable {
    @DocumentID var id: String?
    var displayName: String
    var photoURL: String?
    var city: String
    var cityLatitude: Double?
    var cityLongitude: Double?
    var isFavorite: Bool
    var isDemo: Bool
    @ServerTimestamp var createdAt: Timestamp?
}
```

From HotAndColdFriends/Services/AppWeatherService.swift:
```swift
@Observable
@MainActor
class AppWeatherService {
    private let service = WeatherKit.WeatherService.shared
    // currentWeather(latitude:longitude:) — returnerar CurrentWeather
    // detailedWeather(latitude:longitude:) — returnerar tuple med current, hourly, daily
}
```

From HotAndColdFriends/Services/FriendService.swift:
```swift
@Observable
@MainActor
class FriendService {
    func fetchFriends(uid: String) async throws -> [Friend]
    func updateFriend(uid: String, friendId: String, data: [String: Any]) async throws
}
```

From HotAndColdFriends/App/HotAndColdFriendsApp.swift:
```swift
@main
struct HotAndColdFriendsApp: App {
    @UIApplicationDelegateAdaptor(AppDelegate.self) var delegate
    @State private var authManager: AuthManager
    @State private var appWeatherService: AppWeatherService
    @State private var friendService: FriendService
    @State private var chatService: ChatService
    // .task { delegate.registerForPushNotifications() }
}
```

From functions/src/index.ts:
```typescript
export { onNewMessage } from "./chatPushTrigger";
export { checkExtremeWeather } from "./weatherAlertScheduler";
export const guessContactLocations = onCall({ ... }, async (request) => { ... });
```

From functions/src/chatPushTrigger.ts:
```typescript
// Befintlig fungerande push-trigger — referens for FCM-monster
export const onNewMessage = onDocumentCreated({
  document: "conversations/{conversationId}/messages/{messageId}",
  region: "europe-west1",
}, async (event) => { ... });
```

From HotAndColdFriends/Services/ChatService.swift:
```swift
func getOrCreateDirectConversation(currentUid: String, friendUid: String) async throws -> String
```

<!-- Firestore-datamodell for friend-dokument -->
```
users/{uid}/friends/{friendId}/
  displayName: "Anna"
  city: "Tokyo"
  cityLatitude: 35.6762
  cityLongitude: 139.6503
  isFavorite: true
  isDemo: false
  createdAt: Timestamp
  // NYA FALT (laggs till av denna plan):
  hasActiveAlert: true/false
  alertSummary: "Storm" | null
  lastAlertSentAt: Timestamp | null
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: iOS WeatherAlertService — kontrollera weatherAlerts och skriv till Firestore</name>
  <files>
    HotAndColdFriends/Services/WeatherAlertService.swift,
    HotAndColdFriends/Models/Friend.swift,
    HotAndColdFriends/App/HotAndColdFriendsApp.swift
  </files>
  <action>
**WeatherAlertService.swift** — Ny service som kontrollerar extremvader per van:

```swift
import Foundation
import WeatherKit
import CoreLocation
import FirebaseFirestore
```

Skapa `WeatherAlertService` (INTE @Observable — detta ar en bakgrundstjanst, inget UI behover lyssna):

```swift
@MainActor
class WeatherAlertService {
    private let weatherService = WeatherKit.WeatherService.shared
    private let db = Firestore.firestore()

    /// Kontrollera weatherAlerts for alla vanner med koordinater och uppdatera Firestore.
    /// Anropas vid app-start fran HotAndColdFriendsApp .task{}.
    func checkAlertsForFriends(uid: String, friends: [Friend]) async {
        for friend in friends {
            guard let friendId = friend.id,
                  let lat = friend.cityLatitude,
                  let lon = friend.cityLongitude else { continue }

            // Skip demo-vanner
            guard !friend.isDemo else { continue }

            do {
                let location = CLLocation(latitude: lat, longitude: lon)
                let weather = try await weatherService.weather(for: location, including: .alerts)

                // weather ar [WeatherAlert]? — kan vara nil for regioner utan stod
                let hasAlert: Bool
                let summary: String?

                if let alerts = weather, !alerts.isEmpty {
                    hasAlert = true
                    // Anvand forsta alertens summary som notistext
                    summary = alerts.first?.summary ?? "Extremt vader"
                } else {
                    hasAlert = false
                    summary = nil
                }

                // Skriv till Firestore — detta triggar Cloud Function om hasActiveAlert andras
                var updateData: [String: Any] = [
                    "hasActiveAlert": hasAlert
                ]
                if let summary {
                    updateData["alertSummary"] = summary
                } else {
                    updateData["alertSummary"] = FieldValue.delete()
                }

                try await db.collection("users")
                    .document(uid)
                    .collection("friends")
                    .document(friendId)
                    .updateData(updateData)

            } catch {
                // WeatherKit-fel ska INTE krascha appen — logga och fortsatt
                print("WeatherAlertService: Kunde inte kontrollera alerts for \(friend.displayName): \(error.localizedDescription)")
                continue
            }
        }
    }
}
```

**VIKTIGT om WeatherKit .alerts:**
- `WeatherService.weather(for:including:)` med `.alerts` returnerar `[WeatherAlert]?`
- Returvarde kan vara `nil` for regioner utan stod (t.ex. Sverige) — detta ar INTE ett fel
- Hantera `nil` och tomma arrays som "inga aktiva varningar" (hasActiveAlert = false)
- Om WeatherKit returnerar fel (nattverksproblem etc.) — logga och skippa vannen, fortsatt med nasta

**Friend.swift** — Lagg till tre nya optionella falt (UTAN att bryta befintlig Codable):

Lagg till fore `@ServerTimestamp var createdAt`:
```swift
var hasActiveAlert: Bool?
var alertSummary: String?
@ServerTimestamp var lastAlertSentAt: Timestamp?
```

Dessa falt ar optionella (Bool?, String?, Timestamp?) sa befintliga friend-dokument utan dessa falt fortsatter fungera utan dekodningsfel.

**HotAndColdFriendsApp.swift** — Anropa alert-check vid app-start:

Lagg till en ny @State-property:
```swift
@State private var weatherAlertService = WeatherAlertService()
```

I body, utoka den befintliga `.task {}` pa WindowGroup:
```swift
.task {
    delegate.registerForPushNotifications()
    // Kontrollera extremvader for vanners platser
    if let uid = authManager.currentUser?.id {
        let friends = (try? await friendService.fetchFriends(uid: uid)) ?? []
        await weatherAlertService.checkAlertsForFriends(uid: uid, friends: friends)
    }
}
```

OBS: Alert-checken kor EFTER push-registrering. Om anvandaren inte ar inloggad (uid == nil) skippas checken tyst. fetchFriends-felet fangas med try? for att aldrig blockera app-start.
  </action>
  <verify>
    <automated>cd /Users/richardsandenskog/Claude/weatherfriends && test -f HotAndColdFriends/Services/WeatherAlertService.swift && grep -q "hasActiveAlert" HotAndColdFriends/Models/Friend.swift && grep -q "weatherAlertService" HotAndColdFriends/App/HotAndColdFriendsApp.swift && grep -q "checkAlertsForFriends" HotAndColdFriends/Services/WeatherAlertService.swift && grep -q "alertSummary" HotAndColdFriends/Models/Friend.swift && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>WeatherAlertService kontrollerar WeatherKit .alerts per van och skriver hasActiveAlert/alertSummary till Firestore vid app-start. Friend-modellen har nya optionella falt. HotAndColdFriendsApp anropar alert-check i .task{}.</done>
</task>

<task type="auto">
  <name>Task 2: Cloud Function weatherAlertTrigger — FCM push vid hasActiveAlert-andring</name>
  <files>
    functions/src/weatherAlertTrigger.ts,
    functions/src/weatherAlertScheduler.ts,
    functions/src/index.ts
  </files>
  <action>
**weatherAlertTrigger.ts** — Ny Cloud Function (Firestore onDocumentUpdated):

```typescript
import { onDocumentUpdated } from "firebase-functions/v2/firestore";
import { getFirestore, FieldValue } from "firebase-admin/firestore";
import { getMessaging } from "firebase-admin/messaging";
import { initializeApp, getApps } from "firebase-admin/app";

if (getApps().length === 0) initializeApp();
```

Exportera `onFriendAlertUpdated`:

- Trigger: `onDocumentUpdated({ document: "users/{uid}/friends/{friendId}", region: "europe-west1" })`
- Logik:
  1. Hamta `before` och `after` data fran event.data.
  2. Kontrollera om `hasActiveAlert` andrades fran `false` (eller undefined) till `true`:
     ```typescript
     const before = event.data?.before.data();
     const after = event.data?.after.data();
     if (!before || !after) return;

     const wasActive = before.hasActiveAlert === true;
     const isActive = after.hasActiveAlert === true;
     if (!isActive || wasActive) {
       // Ingen andring till active — skippa
       return;
     }
     ```
  3. **Rate-limiting:** Kontrollera `lastAlertSentAt` pa after-dokumentet. Om det ar mindre an 24 timmar sedan, skippa:
     ```typescript
     const lastSent = after.lastAlertSentAt?.toDate();
     if (lastSent) {
       const hoursSince = (Date.now() - lastSent.getTime()) / (1000 * 60 * 60);
       if (hoursSince < 24) {
         console.log(`Rate limited: alert for friend ${event.params.friendId} sent ${hoursSince.toFixed(1)}h ago`);
         return;
       }
     }
     ```
  4. Hamta `uid` (vanens agare) fran event.params.
  5. Hamta agarens FCM-token fran `users/{uid}`:
     ```typescript
     const db = getFirestore();
     const userDoc = await db.collection("users").doc(event.params.uid).get();
     const fcmToken = userDoc.data()?.fcmToken;
     if (!fcmToken) return;
     ```
  6. Hamta vannens namn och stad fran `after`-data:
     ```typescript
     const friendName = after.displayName ?? "En van";
     const friendCity = after.city ?? "";
     const alertSummary = after.alertSummary ?? "Extremt vader";
     ```
  7. **Skapa deterministiskt conversationId** for deep link (samma logik som iOS-klienten):
     - Vi vet INTE vannens uid (Friend-dokumentet har inget uid-falt).
     - Anvand `friendId` som placeholder i data-payload — iOS-appen kan hantera navigering. Alternativt skicka `type: "weatherAlert"` och lat iOS-appen navigera baserat pa friend-dokumentet.
     ```typescript
     // Deep link data — iOS-appen kan opna chatten via friend-context
     const data: Record<string, string> = {
       type: "weatherAlert",
       friendId: event.params.friendId,
       friendName,
       friendCity,
     };
     ```
  8. Skicka FCM-push med personlig, vanlig ton:
     ```typescript
     const messaging = getMessaging();
     try {
       await messaging.send({
         token: fcmToken,
         notification: {
           title: `${alertSummary} hos ${friendName}`,
           body: friendCity
             ? `Extremt vader i ${friendCity} — hor av dig!`
             : `Hor av dig till ${friendName}!`,
         },
         data,
         apns: {
           payload: {
             aps: { sound: "default" },
           },
         },
       });
       console.log(`Weather alert push sent to ${event.params.uid} for friend ${friendName}`);
     } catch (error: unknown) {
       const errMsg = error instanceof Error ? error.message : String(error);
       console.error(`Failed to send weather alert push: ${errMsg}`);
       // Om token ar ogiltig, ta bort den
       if (errMsg.includes("registration-token-not-registered")) {
         await db.collection("users").doc(event.params.uid)
           .set({ fcmToken: FieldValue.delete() }, { merge: true });
       }
     }
     ```
  9. Uppdatera `lastAlertSentAt` pa friend-dokumentet for rate-limiting:
     ```typescript
     await db.collection("users")
       .doc(event.params.uid)
       .collection("friends")
       .doc(event.params.friendId)
       .update({ lastAlertSentAt: FieldValue.serverTimestamp() });
     ```

**weatherAlertScheduler.ts** — Uppdatera fran placeholder till dokumenterad fallback:

Ersatt hela placeholder-implementationen. Behal `onSchedule` men uppdatera kommentarerna och logiken:

```typescript
import { onSchedule } from "firebase-functions/v2/scheduler";
import { getFirestore } from "firebase-admin/firestore";
import { initializeApp, getApps } from "firebase-admin/app";

if (getApps().length === 0) initializeApp();

/**
 * Schemalagd extremvader-kontroll — komplement till onFriendAlertUpdated.
 *
 * Primart ansvar: Rensa gamla alerts som iOS-klienten inte langre uppdaterar.
 * iOS-klienten satter hasActiveAlert vid app-start, men om anvandaren inte oppnar
 * appen pa lange kan gamla alerts ligga kvar. Denna scheduler rensar alerts aldre
 * an 24 timmar.
 *
 * Push-notiser skickas av weatherAlertTrigger.ts (onDocumentUpdated), INTE har.
 */
export const checkExtremeWeather = onSchedule(
  {
    schedule: "every 60 minutes",
    region: "europe-west1",
  },
  async (_event) => {
    const db = getFirestore();
    const cutoff = new Date(Date.now() - 24 * 60 * 60 * 1000);

    // Hamta alla users
    const usersSnap = await db.collection("users").get();
    let cleaned = 0;

    for (const userDoc of usersSnap.docs) {
      // Hamta vanner med aktiva alerts
      const friendsSnap = await userDoc.ref
        .collection("friends")
        .where("hasActiveAlert", "==", true)
        .get();

      for (const friendDoc of friendsSnap.docs) {
        const data = friendDoc.data();
        const lastSent = data.lastAlertSentAt?.toDate();

        // Om alert ar aldre an 24h, rensa
        if (lastSent && lastSent < cutoff) {
          await friendDoc.ref.update({
            hasActiveAlert: false,
            alertSummary: null,
          });
          cleaned++;
        }
      }
    }

    console.log(`checkExtremeWeather: Cleaned ${cleaned} stale alerts`);
  }
);
```

**index.ts** — Lagg till export av weatherAlertTrigger:

Behal befintliga exports. Lagg till:
```typescript
export { onFriendAlertUpdated } from "./weatherAlertTrigger";
```

**Verifiera att TypeScript kompilerar:** `cd functions && npm run build`

OBS: `onDocumentUpdated` kan krava en nyare version av `firebase-functions`. Kontrollera att `firebase-functions` >= 4.3.0 (v2 Firestore triggers). Projektet har ^6.0.0 — detta ar mer an tillrackligt.
  </action>
  <verify>
    <automated>cd /Users/richardsandenskog/Claude/weatherfriends && test -f functions/src/weatherAlertTrigger.ts && grep -q "onDocumentUpdated" functions/src/weatherAlertTrigger.ts && grep -q "onFriendAlertUpdated" functions/src/index.ts && grep -q "lastAlertSentAt" functions/src/weatherAlertTrigger.ts && grep -q "hasActiveAlert" functions/src/weatherAlertTrigger.ts && cd functions && npm run build 2>&1 | tail -3 && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>weatherAlertTrigger.ts skickar FCM-push nar hasActiveAlert andras till true med rate-limiting (24h). weatherAlertScheduler.ts rensar gamla alerts. index.ts exporterar alla funktioner. TypeScript kompilerar utan fel.</done>
</task>

</tasks>

<verification>
1. WeatherAlertService.swift existerar och anropar WeatherKit .alerts per van
2. Friend.swift har hasActiveAlert, alertSummary och lastAlertSentAt som optionella falt
3. HotAndColdFriendsApp.swift anropar checkAlertsForFriends vid app-start
4. weatherAlertTrigger.ts triggas av onDocumentUpdated pa users/{uid}/friends/{friendId}
5. weatherAlertTrigger.ts kontrollerar hasActiveAlert-andring (false -> true)
6. weatherAlertTrigger.ts har rate-limiting (24h via lastAlertSentAt)
7. weatherAlertTrigger.ts skickar FCM med personlig ton och deep link-data
8. weatherAlertScheduler.ts rensar gamla alerts (inte langre placeholder)
9. index.ts exporterar onFriendAlertUpdated + checkExtremeWeather + onNewMessage + guessContactLocations
10. TypeScript kompilerar utan fel (npm run build)
</verification>

<success_criteria>
- iOS-klienten skriver hasActiveAlert/alertSummary till Firestore vid app-start baserat pa WeatherKit weatherAlerts
- Cloud Function weatherAlertTrigger skickar FCM-push nar hasActiveAlert andras till true med rate-limiting
- Personlig, vanlig ton i notisen: "[Alert] hos [Namn]" + "Extremt vader i [Stad] — hor av dig!"
- Inga kraschar om WeatherKit inte returnerar alerts (nil/tom for regioner utan stod)
- Alla Cloud Functions kompilerar och exporteras korrekt
- PUSH-01 ar nu fullt implementerat end-to-end
</success_criteria>

<output>
After completion, create `.planning/phases/04-chatt-och-push/04-04-SUMMARY.md`
</output>
