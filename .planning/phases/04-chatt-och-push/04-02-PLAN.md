---
phase: 04-chatt-och-push
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - HotAndColdFriends/Features/Chat/ConversationListView.swift
  - HotAndColdFriends/Features/Chat/ConversationListViewModel.swift
  - HotAndColdFriends/Features/Chat/ChatView.swift
  - HotAndColdFriends/Features/Chat/ChatViewModel.swift
  - HotAndColdFriends/Features/Chat/ChatBubbleView.swift
  - HotAndColdFriends/Features/Chat/WeatherHeaderView.swift
  - HotAndColdFriends/Features/Chat/NewConversationSheet.swift
  - HotAndColdFriends/Features/Chat/WeatherStickerView.swift
autonomous: true
requirements:
  - CHAT-01
  - CHAT-02
  - CHAT-03
  - CHAT-04
  - CHAT-05

must_haves:
  truths:
    - "Anvandare ser en lista med alla sina konversationer sorterade efter senaste meddelande"
    - "Anvandare kan oppna en konversation och se meddelanden i iMessage-stil (egna hoger/bla, andras vanster/gra)"
    - "Anvandare kan skriva och skicka textmeddelanden i realtid"
    - "Vanner-vader visas som header ovanfor chatbubborna"
    - "Anvandare kan skapa ny 1-till-1 konversation fran konversationslistan"
    - "Anvandare kan skapa gruppchatt med 2+ vanner"
    - "Anvandare kan skicka vader-sticker (eget eller vannens vader)"
    - "Anvandare kan rapportera ett meddelande via long press"
    - "Anvandare kan blockera en annan anvandare"
  artifacts:
    - path: "HotAndColdFriends/Features/Chat/ConversationListView.swift"
      provides: "Lista med alla chattar"
      contains: "struct ConversationListView"
    - path: "HotAndColdFriends/Features/Chat/ChatView.swift"
      provides: "Chattvy med bubblor och meddelandeinmatning"
      contains: "struct ChatView"
    - path: "HotAndColdFriends/Features/Chat/NewConversationSheet.swift"
      provides: "Skapa ny konversation (1-till-1 eller grupp)"
      contains: "struct NewConversationSheet"
    - path: "HotAndColdFriends/Features/Chat/WeatherStickerView.swift"
      provides: "Vader-sticker rendering i chatten"
      contains: "struct WeatherStickerView"
  key_links:
    - from: "ConversationListView"
      to: "ChatView"
      via: "NavigationLink/navigationDestination"
      pattern: "navigationDestination"
    - from: "ChatView"
      to: "ChatService.sendMessage"
      via: "ChatViewModel.send()"
      pattern: "sendMessage"
    - from: "ChatView"
      to: "ChatService.sendWeatherSticker"
      via: "ChatViewModel.sendSticker()"
      pattern: "sendWeatherSticker"
    - from: "ChatBubbleView"
      to: "Report context menu"
      via: "contextMenu long press"
      pattern: "contextMenu"
---

<objective>
Bygga alla chattrelaterade SwiftUI-vyer: konversationslista, chattvy med iMessage-stil bubblor, vader-header, ny konversation-sheet, vader-sticker, och rapport/blockering-UI.

Purpose: Detta ar den synliga chatten — anvandaren kan skicka meddelanden, skapa chattar, skicka vader-stickers och rapportera/blockera. Uppfyller CHAT-01 till CHAT-05.
Output: 8 SwiftUI-filer i Features/Chat/ som ger full chattfunktionalitet.
</objective>

<execution_context>
@/Users/richardsandenskog/.claude/get-shit-done/workflows/execute-plan.md
@/Users/richardsandenskog/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-chatt-och-push/04-CONTEXT.md
@.planning/phases/04-chatt-och-push/04-RESEARCH.md
@.planning/phases/04-chatt-och-push/04-01-SUMMARY.md

<interfaces>
<!-- Modeller och tjanster fran Plan 04-01 -->

From HotAndColdFriends/Models/Conversation.swift:
```swift
struct Conversation: Codable, Identifiable {
    @DocumentID var id: String?
    var participants: [String]
    var isGroup: Bool
    var groupName: String?
    var lastMessage: String?
    var lastMessageAt: Timestamp?
    var lastMessageSenderId: String?
    @ServerTimestamp var createdAt: Timestamp?
}
```

From HotAndColdFriends/Models/ChatMessage.swift:
```swift
enum MessageType: String, Codable {
    case text
    case weatherSticker
}

struct WeatherStickerData: Codable {
    var city: String
    var countryCode: String
    var temperatureCelsius: Double
    var conditionSymbol: String
    var ownerUid: String
}

struct ChatMessage: Codable, Identifiable {
    @DocumentID var id: String?
    var senderId: String
    var type: MessageType
    var text: String?
    var weatherData: WeatherStickerData?
    @ServerTimestamp var sentAt: Timestamp?
}
```

From HotAndColdFriends/Models/Report.swift:
```swift
struct Report: Codable, Identifiable {
    @DocumentID var id: String?
    var reporterUid: String
    var reportedUid: String
    var messageId: String
    var conversationId: String
    var reason: String
    @ServerTimestamp var createdAt: Timestamp?
}
```

From HotAndColdFriends/Services/ChatService.swift:
```swift
@Observable @MainActor
class ChatService {
    var conversations: [Conversation]
    var messages: [ChatMessage]

    func startListeningToConversations(uid: String)
    func startListeningToMessages(conversationId: String)
    func stopListeningToMessages()
    func stopAll()
    func sendMessage(_ text: String, conversationId: String, senderId: String) async throws
    func sendWeatherSticker(data: WeatherStickerData, conversationId: String, senderId: String) async throws
    func getOrCreateDirectConversation(currentUid: String, friendUid: String) async throws -> String
    func createGroupConversation(creatorUid: String, participantUids: [String], groupName: String?) async throws -> String
    func blockUser(uid: String, blockedUid: String) async throws
    func unblockUser(uid: String, blockedUid: String) async throws
    func isBlocked(uid: String, blockedUid: String) async -> Bool
    func fetchBlockedUserIds(uid: String) async throws -> Set<String>
    func reportMessage(report: Report) async throws
}
```

From HotAndColdFriends/Services/FriendService.swift:
```swift
@Observable @MainActor
class FriendService {
    func fetchFriends(uid: String) async throws -> [Friend]
    func toggleFavorite(uid: String, friend: Friend) async throws
}
```

From HotAndColdFriends/Services/AppWeatherService.swift:
```swift
@Observable @MainActor
class AppWeatherService {
    func currentWeather(latitude: Double, longitude: Double) async throws -> CurrentWeather
}
```

From HotAndColdFriends/Models/Friend.swift:
```swift
struct Friend: Codable, Identifiable {
    @DocumentID var id: String?
    var displayName: String
    var photoURL: String?
    var city: String
    var cityLatitude: Double?
    var cityLongitude: Double?
    var isFavorite: Bool
    var isDemo: Bool
}
```

From HotAndColdFriends/Core/Navigation/MainTabView.swift:
```swift
struct MainTabView: View {
    @Binding var openConversationId: String?
    // TabView med Vanner + Chattar flikar
    // ConversationListView far openConversationId-binding
}
```

From HotAndColdFriends/Features/FriendList/FriendRowView.swift:
```swift
// Ateranvandbart monster: profilbild + namn + stad + vaderikon
// Color.temperatureColor(celsius:) extension finns har
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ConversationListView, ChatView med iMessage-bubblor och vaderheader</name>
  <files>
    HotAndColdFriends/Features/Chat/ConversationListView.swift,
    HotAndColdFriends/Features/Chat/ConversationListViewModel.swift,
    HotAndColdFriends/Features/Chat/ChatView.swift,
    HotAndColdFriends/Features/Chat/ChatViewModel.swift,
    HotAndColdFriends/Features/Chat/ChatBubbleView.swift,
    HotAndColdFriends/Features/Chat/WeatherHeaderView.swift
  </files>
  <action>
**ConversationListViewModel.swift** — @Observable @MainActor klass:
- `var conversations: [Conversation] = []` (bindning till ChatService.conversations)
- `var friendsMap: [String: Friend] = [:]` — uid -> Friend for att visa namn/bild
- `var blockedUserIds: Set<String> = Set()`
- `func load(uid:, chatService:, friendService:)` — hamta vanner (for namn/bild-mappning), ladda blockerade, starta conversations-lyssnare.
  - Skapa friendsMap fran fetchFriends — key ar friend.id, value ar Friend.
  - OBS: Friend.id ar Firestore document ID (auto-genererat), INTE uid. For konversationer behover vi deltagarnas uid. Losning: konversationens participants innehaller uid:s. For att visa namn/bild per deltagare i chatten, anvand UserService.fetchUser(uid:) for att hamta AppUser. Cacha resultaten i en `var usersMap: [String: AppUser] = [:]`.
- `func displayName(for conversation:, currentUid:)` — returnerar vannens namn (1-till-1) eller gruppnamn.
- `func filteredConversations` — computed property som filtrerar bort konversationer dar alla andra deltagare ar blockerade.

**ConversationListView.swift** — ERSATT placeholder fran 04-01:
- `@Environment(ChatService.self)` och `@Environment(AuthManager.self)`
- `@Binding var openConversationId: String?`
- `@State private var viewModel = ConversationListViewModel()`
- `@State private var showNewConversation = false`
- Toolbar med "Ny konversation"-knapp (plus-ikon) som presenterar NewConversationSheet som sheet.
- navigationTitle("Chattar")
- Lista med konversationer — varje rad visar:
  - Profilbild (vannens, eller gruppikonplaceholder)
  - Namn (vannens displayName for 1-till-1, groupName for grupp)
  - Senaste meddelande (lastMessage) + tidsstampel
  - Vannens aktuella vaderikon (SF Symbol) — hamtas fran AppWeatherService baserat pa vannens koordinater. Kan vara asynkront laddad.
- `navigationDestination(for: String.self)` som oppnar ChatView med conversationId.
- `.task {}` som anropar viewModel.load()
- `.onChange(of: openConversationId)` — om icke-nil, navigera till den konversationen (for deep link fran push).

**ChatViewModel.swift** — @Observable @MainActor klass:
- `var messages: [ChatMessage] = []` (bindning till ChatService.messages)
- `var messageText: String = ""`
- `var conversation: Conversation?`
- `var participantUsers: [String: AppUser] = [:]` — uid -> AppUser for bubblor
- `var isLoading = false`
- `func load(conversationId:, chatService:, userService:)` — starta message-listener, hamta konversationsdokumentet, hamta AppUser for varje participant.
- `func send(chatService:, senderId:)` — anropa chatService.sendMessage(), rensa messageText.
- `func sendSticker(data:, chatService:, senderId:)` — anropa chatService.sendWeatherSticker().

**ChatView.swift** — Chattvy med bubblor:
- `@Environment(ChatService.self)`, `@Environment(AuthManager.self)`, `@Environment(AppWeatherService.self)`
- `let conversationId: String`
- `@State private var viewModel = ChatViewModel()`
- `@State private var showWeatherStickerPicker = false`
- Layout (top till bottom):
  1. **WeatherHeaderView** — visar vannens stad, temperatur och vaderikon (for 1-till-1). For grupp: horisontell ScrollView med varje deltagares vader.
  2. **ScrollViewReader + ScrollView** med LazyVStack av ChatBubbleView for varje meddelande. Scrolla automatiskt till botten nar nya meddelanden kommer (`.onChange(of: chatService.messages.count)`).
  3. **Meddelandeinmatning** langst ner: HStack med:
     - TextField("Skriv ett meddelande...") bundet till viewModel.messageText
     - Vader-sticker-knapp (SF Symbol "cloud.sun") som presenterar picker-sheet
     - Skicka-knapp (pil-ikon "arrow.up.circle.fill") — blafargad, disabled om messageText ar tom
- `.task {}` som anropar viewModel.load()
- `.onDisappear { chatService.stopListeningToMessages() }`
- navigationTitle satt till vannens namn (1-till-1) eller gruppnamn.

**ChatBubbleView.swift** — Enskild meddelandebubbla:
- `let message: ChatMessage`
- `let isCurrentUser: Bool`
- `let senderName: String?` (visas i gruppchatt ovanfor bubblan)
- Layout:
  - Om `isCurrentUser`: hogerjusterad, bla bakgrund (.blue), vit text
  - Om inte: vansterjusterad, gra bakgrund (Color(.systemGray5)), svart text
  - Rundade horn (20pt radie)
  - Om type == .weatherSticker: rendera WeatherStickerView istallet for text
  - Tidsstampel i liten text under bubblan (formaterad som "HH:mm")
  - I grupp: visa senderName i liten text ovanfor bubblan (ej for currentUser)
- **contextMenu** (long press) — for ALLA meddelanden (inte bara andras):
  - "Rapportera" (flag-ikon) — ALLTID synlig, App Store Guideline 1.2-krav. Visar bekraftelseAlert. Vid bekraftelse: anropar ChatService.reportMessage().
  - "Blockera anvandare" — BARA for andras meddelanden. Visar bekraftelseAlert ("Blockera [namn]? Du kommer inte se deras meddelanden"). Vid bekraftelse: anropar ChatService.blockUser().

**WeatherHeaderView.swift** — Vader-info ovanfor chatbubborna:
- `let participants: [AppUser]` (exklusive current user)
- `@Environment(AppWeatherService.self)` for att hamta vader
- For 1-till-1: visa stad + temperatur + vaderikon centrerat
- For grupp: horisontell ScrollView med kompakta kort per deltagare (namn, stad, temp, ikon)
- Anvand Color.temperatureColor(celsius:) for temperaturtexten
- Asynkron vaderladdning via `.task {}` for varje deltagare

Kor `xcodegen` efter att alla filer skapats.
  </action>
  <verify>
    <automated>cd /Users/richardsandenskog/Claude/weatherfriends && test -f HotAndColdFriends/Features/Chat/ConversationListView.swift && test -f HotAndColdFriends/Features/Chat/ChatView.swift && test -f HotAndColdFriends/Features/Chat/ChatBubbleView.swift && test -f HotAndColdFriends/Features/Chat/WeatherHeaderView.swift && grep -q "struct ChatView" HotAndColdFriends/Features/Chat/ChatView.swift && grep -q "contextMenu" HotAndColdFriends/Features/Chat/ChatBubbleView.swift && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>ConversationListView visar alla konversationer med namn, senaste meddelande och vaderikon. ChatView visar meddelanden i iMessage-stil med bla/gra bubblor, vader-header, och meddelandeinmatning. ChatBubbleView har contextMenu med Rapportera och Blockera. WeatherHeaderView visar deltagares vader.</done>
</task>

<task type="auto">
  <name>Task 2: NewConversationSheet, WeatherStickerView och blockerings-/rapportbekraftelse</name>
  <files>
    HotAndColdFriends/Features/Chat/NewConversationSheet.swift,
    HotAndColdFriends/Features/Chat/WeatherStickerView.swift
  </files>
  <action>
**NewConversationSheet.swift** — Sheet for att skapa ny konversation:
- `@Environment(ChatService.self)`, `@Environment(AuthManager.self)`, `@Environment(FriendService.self)`
- `@Environment(\.dismiss) private var dismiss`
- `var onConversationCreated: (String) -> Void` — callback med conversationId
- `@State private var friends: [Friend] = []`
- `@State private var selectedFriendIds: Set<String> = Set()`  // Friend document IDs
- `@State private var searchText = ""`
- `@State private var groupName = ""`
- `@State private var isCreating = false`
- `@State private var isGroupMode = false` — toggle langst upp

Layout:
- NavigationStack med title "Ny konversation"
- Toggle for "Skapa grupp" langst upp — nar pa visas groupName-textfalt
- Sokfalt for att filtrera vanner
- Lista med vanner — varje rad: profilbild/initialer, namn, stad, checkbox
- For 1-till-1: tryck pa van -> skapa/oppna direkt konversation (getOrCreateDirectConversation) -> callback -> dismiss
- For grupp: multi-select med checkboxes. "Skapa grupp"-knapp i toolbar (.topBarTrailing) som ar aktiv nar >= 2 vanner valda.
- Vid skapande: anropa createGroupConversation, callback med conversationId, dismiss.

OBS: Friend.id ar Firestore document ID — men konversationer anvander uid. For denna vy behover vi mappa Friend -> uid. Losning: Friend-modellen har INTE uid pa sig sjalv — vi behover utoka logiken. ALTERNATIV (enklare): Anvand Friend document ID som identifierare i konversationer. MEN: da matchar inte conversations.participants mot Auth uid. BAST: Lagg till ett `uid` falt pa Friend om vannen ar en riktig anvandare (inte demo). For denna fas: anta att Friend.id ar det unika ID:t som anvands. Om vannen inte har ett matchande Firebase Auth-konto (de flesta vanner i denna app ar offline-kontakter), skapar vi konversationen med Friend.id som en "pseudo-uid" i participants.

ENKLASTE LOSNINGEN: For 1-till-1, anvand `chatService.getOrCreateDirectConversation(currentUid: currentUid, friendUid: friend.id!)` dar friend.id ar Friend-dokumentets Firestore-ID. For grupper liknande. Detta fungerar for appens skala — vanner ar lokala kontakter, inte Firebase Auth-anvandare.

**WeatherStickerView.swift** — Vader-sticker som visas i chatten:
- `let weatherData: WeatherStickerData`
- Kompakt kort-design:
  - Bakgrund: temperaturfargad gradient (anvand Color.temperatureColor)
  - SF Symbol vaderikon (conditionSymbol) stort centrerat
  - Stad under ikonen
  - Temperatur med gradtecken
  - Rundade horn, padding
  - Max bredd ~200pt
- Anvands BADE som bubbla i chatten OCH som picker-alternativ

Skapa aven en **WeatherStickerPickerView** (kan vara privat struct i WeatherStickerView.swift eller i ChatView.swift):
- Visar tva alternativ:
  1. "Mitt vader" — anvandaren eget vader (fran AuthManager.currentUser koordinater + AppWeatherService)
  2. "Vannens vader" — vannens vader (fran konversationens andra deltagare)
- For grupp: visa lista med alla deltagares vader att valja fran
- Vid val: anropa ChatViewModel.sendSticker() -> dismiss

Kor `xcodegen` efter att alla filer skapats.
  </action>
  <verify>
    <automated>cd /Users/richardsandenskog/Claude/weatherfriends && test -f HotAndColdFriends/Features/Chat/NewConversationSheet.swift && test -f HotAndColdFriends/Features/Chat/WeatherStickerView.swift && grep -q "struct NewConversationSheet" HotAndColdFriends/Features/Chat/NewConversationSheet.swift && grep -q "struct WeatherStickerView" HotAndColdFriends/Features/Chat/WeatherStickerView.swift && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>NewConversationSheet lat anvandare valja van for 1-till-1 eller multi-select for grupp. WeatherStickerView renderar kompakt vaderkort med temperaturfarg, ikon och stad. Vader-sticker picker lat anvandare valja eget eller vannens vader att skicka.</done>
</task>

</tasks>

<verification>
1. ConversationListView visar konversationer med namn, senaste meddelande, tidsstampel och vaderikon
2. ChatView visar meddelanden i iMessage-stil (bla hoger, gra vanster) med vader-header
3. ChatBubbleView har contextMenu med Rapportera och Blockera
4. NewConversationSheet skapar bade 1-till-1 och gruppkonversationer
5. WeatherStickerView renderar kompakt vaderkort med temperaturfarg
6. Vader-sticker picker visar eget och vannens vader som alternativ
7. xcodegen har korts — projektet kompilerar med alla nya filer
</verification>

<success_criteria>
- Alla 8 Features/Chat/*.swift-filer existerar
- ConversationListView navigerar till ChatView via conversationId
- ChatView skickar meddelanden via ChatService
- iMessage-stil: egna meddelanden bla/hoger, andras gra/vanster
- Vader-header visar deltagares stad + temperatur + ikon
- Rapport via contextMenu -> bekraftelse -> Firestore reports
- Blockering via contextMenu -> bekraftelse -> blockedUsers-subkollektion
- NewConversationSheet stodjer bade 1-till-1 och grupp
- WeatherStickerView renderar korrekta vader-stickers
</success_criteria>

<output>
After completion, create `.planning/phases/04-chatt-och-push/04-02-SUMMARY.md`
</output>
