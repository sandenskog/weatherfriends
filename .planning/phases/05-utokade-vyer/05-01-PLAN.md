---
phase: 05-utokade-vyer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - HotAndColdFriends/Features/FriendList/FriendsTabView.swift
  - HotAndColdFriends/Features/FriendList/FriendMapView.swift
  - HotAndColdFriends/Features/FriendList/FriendMapViewModel.swift
  - HotAndColdFriends/Features/FriendList/FriendListView.swift
  - HotAndColdFriends/Core/Navigation/MainTabView.swift
  - project.yml
autonomous: true
requirements:
  - VIEW-03

must_haves:
  truths:
    - "Segmented control (Lista | Karta) visas i Vänner-fliken"
    - "Lista-vyn fungerar identiskt som innan (favoriter, övriga, demo-banner, swipe-favoriter)"
    - "Kartvy visar vänner med giltiga koordinater som nålar med profilbild/initialer och temperatur"
    - "Favoriter har större nålar (48pt) än övriga vänner (36pt)"
    - "Tap på kartnål öppnar WeatherDetailSheet"
    - "Kartan zoomar automatiskt för att visa alla synliga nålar"
    - "Vänner utan koordinater utelämnas tyst från kartan"
  artifacts:
    - path: "HotAndColdFriends/Features/FriendList/FriendsTabView.swift"
      provides: "Segmented control wrapper med Lista/Karta/Kategorier-enum och vyväxling"
      contains: "FriendsTab"
    - path: "HotAndColdFriends/Features/FriendList/FriendMapView.swift"
      provides: "MapKit-kartvy med FriendMapPin-annotations"
      contains: "FriendMapView"
    - path: "HotAndColdFriends/Features/FriendList/FriendMapViewModel.swift"
      provides: "ViewModel med förladdat profilbild-cache"
      contains: "FriendMapViewModel"
  key_links:
    - from: "HotAndColdFriends/Core/Navigation/MainTabView.swift"
      to: "FriendsTabView"
      via: "Ersätter FriendListView i Vänner-flik"
      pattern: "FriendsTabView"
    - from: "HotAndColdFriends/Features/FriendList/FriendsTabView.swift"
      to: "FriendListView, FriendMapView"
      via: "switch selectedTab case .list/.map"
      pattern: "switch selectedTab"
    - from: "HotAndColdFriends/Features/FriendList/FriendMapView.swift"
      to: "WeatherDetailSheet"
      via: ".sheet(item:) vid tap på nål"
      pattern: "sheet.*selectedFriendWeather"
---

<objective>
Skapa FriendsTabView-wrapper med segmented control (Lista | Karta | Kategorier) och implementera MapKit-kartvy med custom kartnålar.

Purpose: Kartvy (VIEW-03) ger användare ett visuellt sätt att se var vänner befinner sig geografiskt med väderinfo per nål. FriendsTabView är den gemensamma strukturen som alla tre vyer (lista, karta, kategorier) delar.

Output: FriendsTabView med segmented control, FriendMapView med MapKit-nålar, FriendMapViewModel med bildcache. Kategorier-fallet visar en placeholder Text() som Plan 05-02 implementerar.
</objective>

<execution_context>
@/Users/richardsandenskog/.claude/get-shit-done/workflows/execute-plan.md
@/Users/richardsandenskog/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-utokade-vyer/05-RESEARCH.md

@HotAndColdFriends/Features/FriendList/FriendListView.swift
@HotAndColdFriends/Features/FriendList/FriendListViewModel.swift
@HotAndColdFriends/Features/FriendList/FriendRowView.swift
@HotAndColdFriends/Features/FriendList/WeatherDetailSheet.swift
@HotAndColdFriends/Core/Navigation/MainTabView.swift
@HotAndColdFriends/Models/FriendWeather.swift
@HotAndColdFriends/Models/Friend.swift
@HotAndColdFriends/Services/AppWeatherService.swift

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From HotAndColdFriends/Models/FriendWeather.swift:
```swift
struct FriendWeather: Identifiable {
    let friend: Friend
    let weather: CurrentWeather?
    var id: String { friend.id ?? UUID().uuidString }
    var temperatureCelsius: Double?
    var temperatureFormatted: String
    var symbolName: String
}
```

From HotAndColdFriends/Models/Friend.swift:
```swift
struct Friend: Codable, Identifiable {
    @DocumentID var id: String?
    var displayName: String
    var photoURL: String?
    var city: String
    var cityLatitude: Double?
    var cityLongitude: Double?
    var isFavorite: Bool
    var isDemo: Bool
}
```

From HotAndColdFriends/Features/FriendList/FriendListViewModel.swift:
```swift
@Observable @MainActor
class FriendListViewModel {
    var favorites: [FriendWeather] = []
    var others: [FriendWeather] = []
    var isLoading: Bool
    var errorMessage: String?
    var showDemoBanner: Bool
    func load(uid:friendService:weatherService:) async
    func refresh(uid:friendService:weatherService:) async
    func toggleFavorite(uid:friend:friendService:) async
    func removeDemoData(uid:friendService:) async
}
```

From HotAndColdFriends/Features/FriendList/FriendRowView.swift:
```swift
extension Color {
    static func temperatureColor(celsius: Double) -> Color
}
```

From HotAndColdFriends/Core/Navigation/MainTabView.swift:
```swift
struct MainTabView: View {
    @Binding var openConversationId: String?
    @Binding var openWeatherAlertFriendId: String?
    // Vänner-flik (tag 0): NavigationStack { FriendListView(openWeatherAlertFriendId:) }
    // Chattar-flik (tag 1): NavigationStack { ConversationListView(openConversationId:) }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Skapa FriendsTabView med segmented control och refaktorera FriendListView</name>
  <files>
    HotAndColdFriends/Features/FriendList/FriendsTabView.swift,
    HotAndColdFriends/Features/FriendList/FriendListView.swift,
    HotAndColdFriends/Core/Navigation/MainTabView.swift,
    project.yml
  </files>
  <action>
**1. Skapa FriendsTabView.swift:**

Skapa `FriendsTabView` som wrapper-vy med segmented control. Denna vy:
- Definierar `FriendsTab` enum (CaseIterable): `.list` ("Lista"), `.map` ("Karta"), `.categories` ("Kategorier")
- Äger `@State private var selectedTab: FriendsTab = .list`
- Äger `@State private var viewModel = FriendListViewModel()` — viewmodel lyfts HIT från FriendListView
- Tar `@Binding var openWeatherAlertFriendId: String?` (behövs för deep link)
- Tar `@Environment(AuthManager.self)`, `@Environment(AppWeatherService.self)`, `@Environment(FriendService.self)` — samma som FriendListView har idag
- Har `.task {}` som laddar data via `viewModel.load()` (flytta från FriendListView)
- Layout: `NavigationStack` ytterst (flytta från FriendListView), sedan `VStack(spacing: 0)` med:
  - `Picker("Vy", selection: $selectedTab)` med `.pickerStyle(.segmented)` och `.padding(.horizontal)`
  - `switch selectedTab` som renderar:
    - `.list`: `FriendListView(viewModel: viewModel, openWeatherAlertFriendId: $openWeatherAlertFriendId)` — pass viewmodel som parameter
    - `.map`: `FriendMapView(friendWeathers: viewModel.favorites + viewModel.others)` (skapas i Task 2)
    - `.categories`: `Text("Kategorier kommer snart")` (placeholder — Plan 05-02 ersätter)
- Flytta hit ALL toolbar-logik från FriendListView (logga ut, plus-meny, profil-knapp)
- Flytta hit ALL sheet-presentering från FriendListView (WeatherDetailSheet, AddFriendSheet, ContactImportView, ProfileView, alert)
- Flytta hit refreshable-logik (som nu körs oavsett vilken flik som visas)

**2. Refaktorera FriendListView.swift:**

FriendListView ska bli en "dum" vy som enbart renderar listan. Ta bort:
- `@Environment(AuthManager.self)` — behövs inte längre
- `@Environment(AppWeatherService.self)` — behövs inte längre
- `@Environment(FriendService.self)` — behövs inte längre
- `@State private var viewModel` — tas emot som parameter istället
- `NavigationStack` wrapping — flyttad till FriendsTabView
- Alla toolbar-items — flyttade till FriendsTabView
- Alla `.sheet()` modifiers — flyttade till FriendsTabView
- `.task {}` för data-laddning — flyttad till FriendsTabView
- `.alert()` modifier — flyttad till FriendsTabView
- `.onChange(of: openWeatherAlertFriendId)` — flyttad till FriendsTabView

FriendListView behåller:
- `let viewModel: FriendListViewModel` (mottagen parameter, INTE @State)
- `@Binding var openWeatherAlertFriendId: String?`
- `@Binding var selectedFriendWeather: FriendWeather?` — binding till FriendsTabView:s @State
- `mainList` property med List (favoriter, övriga, attribution)
- `demoBanner`, `favoritesSection`, `othersSection`, `attributionFooter`
- `.refreshable` delegeras uppåt via closure-parameter ELLER behålls om FriendsTabView skickar ner nödvändiga parametrar

Konkret signatur:
```swift
struct FriendListView: View {
    let viewModel: FriendListViewModel
    @Binding var selectedFriendWeather: FriendWeather?
    // refreshAction, toggleFavoriteAction etc. som closures ELLER
    // skicka ner uid/friendService/weatherService/authManager som parametrar
}
```

Enklaste lösningen: Skicka ner uid, friendService och weatherService som parametrar till FriendListView så att .refreshable, toggleFavorite och removeDemoData kan fungera. Alternativt: skicka ner authManager som parameter och behåll @Environment för resten. Välj det enklaste som kompilerar.

**Viktigt:** FriendListView-refaktoreringen ska vara strikt bakåtkompatibel vad gäller UI — listan, favoriter, swipe-actions, demo-banner, attribution ska se och fungera identiskt.

**3. Uppdatera MainTabView.swift:**

Byt ut `FriendListView(openWeatherAlertFriendId: $openWeatherAlertFriendId)` mot `FriendsTabView(openWeatherAlertFriendId: $openWeatherAlertFriendId)`. Ta bort `NavigationStack`-wrapping runt Vänner-fliken — det hanteras nu av FriendsTabView internt.

```swift
// Före:
NavigationStack {
    FriendListView(openWeatherAlertFriendId: $openWeatherAlertFriendId)
}
.tabItem { ... }

// Efter:
FriendsTabView(openWeatherAlertFriendId: $openWeatherAlertFriendId)
    .tabItem { ... }
```

**4. Kör xcodegen:**

Nya .swift-filer kräver att xcodegen körs om:
```bash
cd /Users/richardsandenskog/Claude/weatherfriends && xcodegen generate
```
  </action>
  <verify>
    <automated>cd /Users/richardsandenskog/Claude/weatherfriends && xcodegen generate 2>&1 && xcodebuild build -project HotAndColdFriends.xcodeproj -scheme HotAndColdFriends -destination 'platform=iOS Simulator,name=iPhone 16' -quiet 2>&1 | tail -5</automated>
  </verify>
  <done>
    - FriendsTabView.swift existerar med FriendsTab-enum och segmented control
    - FriendListView.swift tar viewModel som parameter (inte @State)
    - MainTabView refererar till FriendsTabView istället för FriendListView
    - Projektet kompilerar utan fel
    - Lista-vyn fungerar identiskt (favoriter, övriga, demo-banner, swipe, attribution)
  </done>
</task>

<task type="auto">
  <name>Task 2: Implementera FriendMapView med MapKit-nålar och FriendMapViewModel med bildcache</name>
  <files>
    HotAndColdFriends/Features/FriendList/FriendMapView.swift,
    HotAndColdFriends/Features/FriendList/FriendMapViewModel.swift,
    project.yml
  </files>
  <action>
**1. Skapa FriendMapViewModel.swift:**

`@Observable @MainActor` klass som hanterar förladdat bildcache:

```swift
@Observable
@MainActor
class FriendMapViewModel {
    var loadedImages: [String: UIImage] = [:]  // key = photoURL
    var isLoading = false

    func preloadImages(for friendWeathers: [FriendWeather]) async {
        isLoading = true
        defer { isLoading = false }

        let urls = friendWeathers.compactMap { $0.friend.photoURL }
            .filter { loadedImages[$0] == nil }

        await withTaskGroup(of: (String, UIImage?).self) { group in
            for urlString in Set(urls) {
                group.addTask {
                    guard let url = URL(string: urlString),
                          let (data, _) = try? await URLSession.shared.data(from: url),
                          let image = UIImage(data: data) else {
                        return (urlString, nil)
                    }
                    return (urlString, image)
                }
            }
            for await (urlString, image) in group {
                if let image {
                    loadedImages[urlString] = image
                }
            }
        }
    }
}
```

**Varför bildcache:** Research (05-RESEARCH.md, Pitfall 1) visar att AsyncImage i Map Annotation ger renderingsproblem. Synkron UIImage i annotations undviker detta.

**2. Skapa FriendMapView.swift:**

```swift
import SwiftUI
import MapKit

struct FriendMapView: View {
    let friendWeathers: [FriendWeather]
    @State private var selectedFriendWeather: FriendWeather?
    @State private var position: MapCameraPosition = .automatic
    @State private var viewModel = FriendMapViewModel()
    @Environment(AppWeatherService.self) private var weatherService

    private var mappableFriends: [FriendWeather] {
        friendWeathers.filter {
            $0.friend.cityLatitude != nil && $0.friend.cityLongitude != nil
        }
    }

    var body: some View {
        Map(position: $position) {
            ForEach(mappableFriends) { fw in
                Annotation("", coordinate: CLLocationCoordinate2D(
                    latitude: fw.friend.cityLatitude!,
                    longitude: fw.friend.cityLongitude!
                )) {
                    FriendMapPin(
                        friendWeather: fw,
                        isFavorite: fw.friend.isFavorite,
                        cachedImage: viewModel.loadedImages[fw.friend.photoURL ?? ""]
                    )
                    .onTapGesture { selectedFriendWeather = fw }
                }
            }
        }
        .sheet(item: $selectedFriendWeather) { fw in
            WeatherDetailSheet(friendWeather: fw)
                .environment(weatherService)
        }
        .task {
            await viewModel.preloadImages(for: friendWeathers)
        }
    }
}
```

**3. Skapa FriendMapPin som privat struct i FriendMapView.swift:**

Nålen visar profilbild (förladdat UIImage) eller initialer-cirkel med temperaturfärgad border. Temperatur visas nedanför i en capsule med ultraThinMaterial-bakgrund.

```swift
struct FriendMapPin: View {
    let friendWeather: FriendWeather
    let isFavorite: Bool
    let cachedImage: UIImage?

    private var pinSize: CGFloat { isFavorite ? 48 : 36 }
    private var tempColor: Color {
        friendWeather.temperatureCelsius.map { Color.temperatureColor(celsius: $0) } ?? .secondary
    }

    var body: some View {
        VStack(spacing: 2) {
            profileCircle
                .frame(width: pinSize, height: pinSize)
                .clipShape(Circle())
                .overlay(Circle().stroke(tempColor, lineWidth: isFavorite ? 3 : 2))
            Text(friendWeather.temperatureFormatted)
                .font(.caption2.weight(.semibold))
                .foregroundStyle(tempColor)
                .padding(.horizontal, 4)
                .background(.ultraThinMaterial, in: Capsule())
        }
    }

    @ViewBuilder
    private var profileCircle: some View {
        if let image = cachedImage {
            Image(uiImage: image)
                .resizable()
                .scaledToFill()
        } else {
            ZStack {
                Circle().fill(Color(.systemGray5))
                Text(initials(from: friendWeather.friend.displayName))
                    .font(isFavorite ? .caption.weight(.semibold) : .caption2.weight(.semibold))
                    .foregroundStyle(.secondary)
            }
        }
    }

    private func initials(from name: String) -> String {
        let parts = name.split(separator: " ")
        let letters = parts.prefix(2).compactMap { $0.first.map { String($0) } }
        return letters.joined().uppercased()
    }
}
```

**4. Koppla ihop med FriendsTabView:**

FriendsTabView:s `.map`-case ska rendera `FriendMapView(friendWeathers: viewModel.favorites + viewModel.others)`. Kontrollera att detta redan gjordes i Task 1.

**5. Empty state-hantering:**

Om `mappableFriends` är tom (inga vänner med koordinater): visa en ContentUnavailableView eller enkel Text med "Inga vänner med platsdata att visa på kartan". Använd befintligt demo-data-koncept — demo-vänner har koordinater och visas på kartan.

**6. Kör xcodegen om nya filer skapades:**
```bash
cd /Users/richardsandenskog/Claude/weatherfriends && xcodegen generate
```
  </action>
  <verify>
    <automated>cd /Users/richardsandenskog/Claude/weatherfriends && xcodegen generate 2>&1 && xcodebuild build -project HotAndColdFriends.xcodeproj -scheme HotAndColdFriends -destination 'platform=iOS Simulator,name=iPhone 16' -quiet 2>&1 | tail -5</automated>
  </verify>
  <done>
    - FriendMapView.swift existerar med MapKit Map + Annotation
    - FriendMapViewModel.swift existerar med bildcache-logik
    - FriendMapPin renderar profilbild (UIImage) eller initialer med temperaturfärgad border
    - Favoriter har pinSize 48, övriga 36
    - Tap på nål öppnar WeatherDetailSheet
    - MapCameraPosition.automatic zoomar till alla nålar
    - Vänner utan koordinater filtreras bort utan indikation
    - Projektet kompilerar utan fel
  </done>
</task>

</tasks>

<verification>
1. `xcodebuild build` kompilerar utan fel
2. Segmented control visas med tre val (Lista, Karta, Kategorier)
3. Lista-flik: identisk som innan (favoriter, övriga, demo-banner, swipe, attribution, WeatherDetailSheet)
4. Karta-flik: MapKit-karta med nålar per vän (profilbild/initialer + temperatur)
5. Favoriter har större nålar (48pt) med tjockare border (3pt)
6. Tap på kartnål öppnar WeatherDetailSheet
7. Kartan zoomar automatiskt in på alla synliga nålar
8. Deep link (openWeatherAlertFriendId) fungerar fortfarande
</verification>

<success_criteria>
- VIEW-03 uppfyllt: Kartvy med vänners platser och väderinfo via MapKit
- Segmented control-navigation fungerar för alla tre flikar (kategorier visar placeholder)
- Befintlig lista-funktionalitet obruten
- Projektet kompilerar och körs
</success_criteria>

<output>
After completion, create `.planning/phases/05-utokade-vyer/05-01-SUMMARY.md`
</output>
