---
phase: 04.2-chatt-uid-mismatch
verified: 2026-03-03T17:30:00Z
status: passed
score: 5/5 must-haves verified
re_verification: false
gaps: []
human_verification:
  - test: "Starta 1-till-1-chatt och verifiera att mottagaren ser konversationen"
    expected: "Mottagaren ser konversationen i sin konversationslista direkt"
    why_human: "Kräver två inloggade enheter/konton — kan inte verifieras programmatiskt"
  - test: "Tryck på vän utan authUid (demo-vän) och försök starta chatt"
    expected: "Felmeddelande 'Den här vännen har inget konto i appen än.' visas"
    why_human: "Kräver körning av appen med demo-data"
---

# Phase 04.2: Chatt-UID-mismatch Verification Report

**Phase Goal:** Chattar skapas med korrekt Firebase Auth UID — så att mottagaren ser konversationen i sin lista
**Verified:** 2026-03-03T17:30:00Z
**Status:** PASSED
**Re-verification:** Nej — initial verifiering

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|---------|
| 1 | NewConversationSheet skickar vännens Auth UID (inte Friend.id) till getOrCreateDirectConversation() | VERIFIED | `openDirectConversation` använder `friend.authUid` (rad 221), `guard let friendAuthUid = friend.authUid` — anropar chatService med `friendUid: friendAuthUid` |
| 2 | Mottagaren ser konversationen i sin konversationslista (participants innehåller rätt Auth UID) | VERIFIED (kod) | ChatService.getOrCreateDirectConversation() sätter `participants: [currentUid, friendUid]` där friendUid nu är Auth UID — lyssnarfrågan matchar; manuell test krävs för runtime-bekräftelse |
| 3 | Gruppkonversation skapas med Auth UID:n för alla valda vänner | VERIFIED | toggleSelection (rad 207) lagrar `friend.authUid` i selectedFriendIds; createGroup skickar `participantUids: Array(selectedFriendIds)` till createGroupConversation() |
| 4 | Vänner utan authUid (lokala/demo) kan inte starta chatt — tydligt felmeddelande visas | VERIFIED | `guard let friendAuthUid = friend.authUid, !currentUid.isEmpty else { errorMessage = "Den här vännen har inget konto i appen än." return }` (rad 221-223) |
| 5 | Checkmark-logik i gruppval matchar authUid-baserad selectedFriendIds | VERIFIED | `if let uid = friend.authUid, selectedFriendIds.contains(uid)` (rad 135) — konsistent med toggleSelection |

**Score:** 5/5 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `HotAndColdFriends/Models/Friend.swift` | Friend-modell med authUid-fält | VERIFIED | Rad 6: `var authUid: String?` tillagt direkt efter `@DocumentID var id: String?` med förklarande kommentar |
| `HotAndColdFriends/Features/Chat/NewConversationSheet.swift` | Chattstart med korrekt Auth UID | VERIFIED | Tre separata ställen använder `friend.authUid`: openDirectConversation (rad 221), toggleSelection (rad 207), friendRow checkmark (rad 135) |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| NewConversationSheet.openDirectConversation | ChatService.getOrCreateDirectConversation | friend.authUid istället för friend.id | WIRED | `guard let friendAuthUid = friend.authUid` -> `friendUid: friendAuthUid` — korrekt Auth UID skickas |
| NewConversationSheet.toggleSelection | selectedFriendIds | friend.authUid istället för friend.id | WIRED | `guard let authUid = friend.authUid` -> `selectedFriendIds.insert(authUid)` — Auth UID lagras |
| NewConversationSheet.friendRow checkmark | selectedFriendIds.contains | friend.authUid-kontroll | WIRED | `if let uid = friend.authUid, selectedFriendIds.contains(uid)` — konsistent med toggleSelection |

### Requirements Coverage

| Requirement | Källa | Beskrivning | Status | Bevis |
|-------------|-------|-------------|--------|-------|
| CHAT-01 | PLAN 04.2-01 + REQUIREMENTS.md Phase 4.2 | Användare kan skicka 1-till-1 meddelanden till vänner | SATISFIED | openDirectConversation använder korrekt Auth UID -> participants-array i Firestore matchar nu mottagarens uid; ChatService.getOrCreateDirectConversation placerar rätt UID |
| CHAT-02 | PLAN 04.2-01 + REQUIREMENTS.md Phase 4.2 | Användare kan skapa och delta i gruppchattar | SATISFIED | toggleSelection + createGroup-flödet använder Auth UIDs; createGroupConversation tar emot korrekt participants-array |

Båda krav är markerade som `[x]` i REQUIREMENTS.md och `requirements-completed: [CHAT-01, CHAT-02]` i SUMMARY-frontmatter. Inga orphaned requirements identifierade — inga ytterligare krav i REQUIREMENTS.md mappar till Phase 4.2.

### Anti-Patterns Found

Inga anti-patterns hittades.

| Fil | Rad | Mönster | Allvarlighetsgrad | Påverkan |
|-----|-----|---------|-------------------|---------|
| — | — | — | — | — |

Specifikt kontrollerat:
- `friend.id` används INTE längre i konversationslogik i NewConversationSheet.swift (noll träffar)
- Inga TODO/FIXME/PLACEHOLDER-kommentarer i ändrade filer
- Inga tomma implementationer — alla guards har konkret felhantering
- console.log-motsvarigheter (Swift print) — inga i ändrade filer

### Commits verifierade

| Hash | Beskrivning | Filer |
|------|-------------|-------|
| `6d26049` | feat: lägg till authUid på Friend-modellen | Friend.swift (+1 rad) |
| `a3d8b70` | fix: använd authUid istället för friend.id i NewConversationSheet | NewConversationSheet.swift (+10/-7 rader) |

Båda commits existerar i git-historiken och matchar SUMMARY-dokumentation.

### Human Verification Required

#### 1. End-to-end runtime-test: mottagaren ser konversationen

**Test:** Logga in med konto A, starta en 1-till-1-chatt med en vän som har ett riktigt konto (konto B). Logga sedan in med konto B och verifiera att konversationslistan visar konversationen.
**Expected:** Konto B ser konversationen i sin lista omedelbart (eller vid nästa sync).
**Varför human:** Kräver två inloggade konton och en körande app — kan inte verifieras med statisk kodanalys.

#### 2. Felmeddelande för vän utan konto

**Test:** Öppna NewConversationSheet, tryck på en demo-vän eller en vän som lagts till manuellt (utan authUid).
**Expected:** Alert visas med texten "Den här vännen har inget konto i appen än." och ingen krasch sker.
**Varför human:** Kräver körning av appen med demo-data eller en vän utan authUid i Firestore.

### Gaps Summary

Inga gaps. Alla fem observable truths är verifierade mot faktisk kod.

Fasen löste den dokumenterade buggen: `friend.id` (Firestore dokument-ID) ersattes på samtliga tre relevanta ställen i NewConversationSheet med `friend.authUid` (Firebase Auth UID). Friend-modellen utökades bakåtkompatibelt med `var authUid: String?`. ChatService.getOrCreateDirectConversation och createGroupConversation tar emot och lagrar korrekta Auth UIDs i participants-arrayen, vilket möjliggör att mottagarens lyssnare (`whereField("participants", arrayContains: uid)`) hittar konversationen.

---

_Verified: 2026-03-03T17:30:00Z_
_Verifier: Claude (gsd-verifier)_
