---
phase: 10-komponenter
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - HotAndColdFriends/DesignSystem/BubblePopButton.swift
  - HotAndColdFriends/Features/Chat/ChatBubbleView.swift
  - HotAndColdFriends/Features/Chat/WeatherStickerView.swift
autonomous: true
requirements:
  - COMP-02
  - COMP-03
  - COMP-04

must_haves:
  truths:
    - "Knappar med pill-form (Capsule) och brand-gradient (bubblePrimary → bubbleSecondary) finns som återanvändbar komponent"
    - "Egna chattbubblor har chatMine-gradient (FF6B8A → FF8E6B) med asymmetrisk border radius"
    - "Andras chattbubblor har vit bakgrund med tunn bubbleBorder"
    - "Väder-stickers har gradient-border och gradient-temperaturtext (temperaturzon-gradient), ljus bakgrund"
    - "BubblePopButton ger subtil bounce-scale vid tryck"
  artifacts:
    - path: "HotAndColdFriends/DesignSystem/BubblePopButton.swift"
      provides: "Återanvändbar pill-knapp med gradient och bounce"
      exports: ["BubblePopButton"]
    - path: "HotAndColdFriends/Features/Chat/ChatBubbleView.swift"
      provides: "Chattbubblor med gradient/vit + asymmetrisk border radius"
      contains: "LinearGradient.chatMine"
    - path: "HotAndColdFriends/Features/Chat/WeatherStickerView.swift"
      provides: "Väder-sticker med gradient-accent och TemperatureZone-gradient"
      contains: "TemperatureZone"
  key_links:
    - from: "ChatBubbleView"
      to: "LinearGradient.chatMine"
      via: "isCurrentUser branch i bubbleContent"
      pattern: "chatMine"
    - from: "WeatherStickerView"
      to: "TemperatureZone.gradient"
      via: "gradient-border och temperaturtext"
      pattern: "TemperatureZone.*gradient"
---

<objective>
Skapa `BubblePopButton` som en återanvändbar pill-knapp med gradient och bounce, samt omforma `ChatBubbleView` (gradient/vit med asymmetrisk radius) och `WeatherStickerView` (gradient-accent styling).

Purpose: Appens interaktiva element måste matcha Bubble Pop-systemets varma, lekfulla känsla. Knappar, chattbubblor och stickers är de mest frekvent använda UI-elementen.
Output: BubblePopButton.swift (ny) + uppdaterad ChatBubbleView.swift + uppdaterad WeatherStickerView.swift
</objective>

<execution_context>
@/Users/richardsandenskog/.claude/get-shit-done/workflows/execute-plan.md
@/Users/richardsandenskog/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md

<interfaces>
<!-- Key types executorn behöver. Extraherade från kodbasen. -->

Från HotAndColdFriends/DesignSystem/BubblePopColors.swift:
```swift
static let bubblePrimary = Color(hex: 0xFF6B8A)
static let bubbleSecondary = Color(hex: 0xFF8E6B)
static let bubbleSurface = Color(hex: 0xFFFFFF)
static let bubbleBorder = Color(hex: 0xE8ECF4)
static let chatMineStart = Color(hex: 0xFF6B8A)
static let chatMineEnd = Color(hex: 0xFF8E6B)

static var chatMine: LinearGradient  // topLeading → bottomTrailing
```

Från HotAndColdFriends/DesignSystem/BubblePopTypography.swift:
```swift
static var bubbleButton: Font  // Baloo 2 SemiBold 15pt
static var bubbleTemperature: Font
static var bubbleTemperatureSmall: Font
static var bubbleCaption: Font
```

Från HotAndColdFriends/DesignSystem/BubblePopShadows.swift:
```swift
func shadowGlowPrimary() -> some View  // brand-glow för knappar
func shadowMd() -> some View
```

Från HotAndColdFriends/DesignSystem/BubblePopSpacing.swift:
```swift
enum Spacing { xs = 4; sm = 8; md = 16; lg = 24 }
enum CornerRadius { sm = 12; md = 20; xl = 50 }
```

Från HotAndColdFriends/DesignSystem/TemperatureZone.swift:
```swift
enum TemperatureZone { init(celsius: Double); var gradient: LinearGradient; var color: Color }
```

Befintlig ChatBubbleView (nuläge att ersätta):
- Egna bubblor: `.background(Color.blue)` + `.clipShape(RoundedRectangle(cornerRadius: 20))`
- Andras bubblor: `.background(Color(.systemGray5))` + `.clipShape(RoundedRectangle(cornerRadius: 20))`

Befintlig WeatherStickerView (nuläge att förstärka):
- Har redan en subtil gradient-bakgrund men använder `color.opacity(0.15/0.05)` — ska göras tydligare
- Använder `Image(systemName:)` — ska ersättas med WeatherIconMapper.icon(for:size:)

Från HotAndColdFriends/Models/ChatMessage.swift:
```swift
struct WeatherStickerData: Codable {
    var city: String
    var temperatureCelsius: Double
    var conditionSymbol: String  // SF Symbol-namn
    var ownerUid: String
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Skapa BubblePopButton — pill-knapp med gradient och bounce-effekt</name>
  <files>HotAndColdFriends/DesignSystem/BubblePopButton.swift</files>
  <action>
Skapa ny fil `HotAndColdFriends/DesignSystem/BubblePopButton.swift`.

BubblePopButton ska:
1. Ta `title: String`, `action: () -> Void`, och valfri `isDestructive: Bool = false`
2. Pill-form: Capsule-shape med `LinearGradient(colors: [.bubblePrimary, .bubbleSecondary], startPoint: .leading, endPoint: .trailing)` som bakgrund — eller `.bubbleError` solid bakgrund om `isDestructive = true`
3. Text: `Text(title).font(.bubbleButton).foregroundStyle(.white)`
4. Padding: `.padding(.horizontal, Spacing.lg).padding(.vertical, Spacing.sm + Spacing.xs)` (= 12pt vertikalt, 24pt horisontellt)
5. Bounce vid tryck: `@State private var isPressed = false`. Lägg till `.scaleEffect(isPressed ? 0.96 : 1.0).animation(.spring(response: 0.25, dampingFraction: 0.6), value: isPressed)`. Använd `.simultaneousGesture(DragGesture(minimumDistance: 0).onChanged { _ in isPressed = true }.onEnded { _ in isPressed = false; action() })`.
6. Skugga: `.shadowGlowPrimary()` — brand-glow under knappen

Inkludera `#Preview` med tre knappar (primary, secondary title, destructive).

Registrera filen i Xcode-projektet via `project.pbxproj` — lägg till under `HotAndColdFriends/DesignSystem`-gruppen, samma mönster som AvatarView.swift från plan 10-01 (om den redan är tillagd) eller befintliga DesignSystem-filer.
  </action>
  <verify>
    <automated>xcodebuild -project /Users/richardsandenskog/Claude/weatherfriends/HotAndColdFriends.xcodeproj -scheme HotAndColdFriends -destination 'generic/platform=iOS Simulator' build 2>&1 | tail -5</automated>
  </verify>
  <done>BubblePopButton.swift finns, bygger utan fel, pill-form med gradient och bounce-scale</done>
</task>

<task type="auto">
  <name>Task 2: Omforma ChatBubbleView med gradient/vit + asymmetrisk border radius, och WeatherStickerView med gradient-accent</name>
  <files>
    HotAndColdFriends/Features/Chat/ChatBubbleView.swift
    HotAndColdFriends/Features/Chat/WeatherStickerView.swift
  </files>
  <action>
**ChatBubbleView.swift — bubbla-styling:**

Ersätt `bubbleContent` computed property (det icke-sticker-blocket):

Nuläge:
```swift
Text(message.text ?? "")
    .font(.body)
    .foregroundStyle(isCurrentUser ? .white : .primary)
    .padding(.horizontal, 14)
    .padding(.vertical, 10)
    .background(isCurrentUser ? Color.blue : Color(.systemGray5))
    .clipShape(RoundedRectangle(cornerRadius: 20))
```

Nytt — egna bubblor (isCurrentUser = true):
```swift
Text(message.text ?? "")
    .font(.bubbleBody)
    .foregroundStyle(.white)
    .padding(.horizontal, 16)
    .padding(.vertical, 10)
    .background(LinearGradient.chatMine)
    .clipShape(UnevenRoundedRectangle(
        cornerRadii: .init(topLeading: 20, bottomLeading: 20, bottomTrailing: 6, topTrailing: 20)
    ))
```

Nytt — andras bubblor (isCurrentUser = false):
```swift
Text(message.text ?? "")
    .font(.bubbleBody)
    .foregroundStyle(.bubbleTextPrimary)
    .padding(.horizontal, 16)
    .padding(.vertical, 10)
    .background(Color.bubbleSurface)
    .overlay(
        UnevenRoundedRectangle(
            cornerRadii: .init(topLeading: 6, bottomLeading: 20, bottomTrailing: 20, topTrailing: 20)
        )
        .strokeBorder(Color.bubbleBorder, lineWidth: 1)
    )
    .clipShape(UnevenRoundedRectangle(
        cornerRadii: .init(topLeading: 6, bottomLeading: 20, bottomTrailing: 20, topTrailing: 20)
    ))
```

Notera: `UnevenRoundedRectangle` kräver iOS 16+. Kontrollera att deployment target är iOS 16+ (det bör vara det). Asymmetrisk radius: den hörnet närmast avsändaren (bottomTrailing för egna, topLeading för andras) är 6pt — lekfull "pratbubbla-hake"-känsla.

---

**WeatherStickerView.swift — gradient-accent styling:**

Förstärk stickerns visuella identitet:

1. Ersätt `Image(systemName: weatherData.conditionSymbol)` med `WeatherIconMapper.icon(for: weatherData.conditionSymbol, size: 36)` (kräver `import`-check — WeatherIconMapper är i samma modul)

2. Temperaturtexten: Ersätt `.foregroundStyle(color)` med en gradient fill:
```swift
Text("\(Int(celsius.rounded()))°")
    .font(.bubbleTemperature)
    .foregroundStyle(zone.gradient)
```
Definiera `private var zone: TemperatureZone { TemperatureZone(celsius: celsius) }` i stickern.

3. Bakgrund: Gör gradient-bakgrunden tydligare — ersätt opacity(0.15/0.05) med:
```swift
.background(
    zone.gradient.opacity(0.12)
)
```

4. Border: Ersätt den tunna strokeBorder med en gradient-border via overlay:
```swift
.overlay(
    RoundedRectangle(cornerRadius: 16)
        .strokeBorder(
            LinearGradient(
                colors: [zone.color.opacity(0.5), zone.color.opacity(0.2)],
                startPoint: .topLeading,
                endPoint: .bottomTrailing
            ),
            lineWidth: 1.5
        )
)
```

5. Stadsnamn: Uppdatera till `.font(.bubbleCaption).foregroundStyle(.bubbleTextSecondary)`
  </action>
  <verify>
    <automated>xcodebuild -project /Users/richardsandenskog/Claude/weatherfriends/HotAndColdFriends.xcodeproj -scheme HotAndColdFriends -destination 'generic/platform=iOS Simulator' build 2>&1 | tail -5</automated>
  </verify>
  <done>Bygger utan fel. ChatBubbleView har gradient på egna bubblor och vit/border på andras, med asymmetrisk radius. WeatherStickerView visar tydligare gradient-accent och korrekt ikon via WeatherIconMapper.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>BubblePopButton (ny pill-knapp med gradient och bounce), omformat ChatBubbleView (gradient/vit chattbubblor med asymmetrisk radius) och förbättrad WeatherStickerView (gradient-accent). Bygg och kör i simulator.</what-built>
  <how-to-verify>
    1. Navigera till chattfliken — öppna en konversation
    2. Kontrollera egna meddelanden: gradient (rosa → orange), asymmetrisk form (hake i nedre-höger-hörnet)
    3. Kontrollera andras meddelanden: vita med tunn border, asymmetrisk form (hake i övre-vänster-hörnet)
    4. Skicka en väder-sticker eller se en befintlig: gradient-accent ska synas i border och temperaturvärde
    5. Valfritt: Testa BubblePopButton om den används någonstans, eller verifiera via Xcode Preview
  </how-to-verify>
  <resume-signal>Skriv "approved" om det ser rätt ut, eller beskriv problem att åtgärda</resume-signal>
</task>

</tasks>

<verification>
- `xcodebuild` returnerar BUILD SUCCEEDED
- BubblePopButton.swift finns i DesignSystem-mappen och är registrerad i Xcode-projektet
- ChatBubbleView.swift innehåller ingen referens till `Color.blue` eller `Color(.systemGray5)` längre
- WeatherStickerView.swift använder `WeatherIconMapper.icon(for:size:)` istället för `Image(systemName:)`
</verification>

<success_criteria>
Chattgränssnittet har Bubble Pop-styling genomgående: gradient-bubblor, vit/border för andras, stickers med gradient-accent. BubblePopButton är redo att användas i onboarding och andra formulär.
</success_criteria>

<output>
After completion, create `.planning/phases/10-komponenter/10-02-SUMMARY.md`
</output>
