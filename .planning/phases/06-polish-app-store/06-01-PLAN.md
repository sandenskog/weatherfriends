---
phase: 06-polish-app-store
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - HotAndColdFriends/Features/FriendList/FriendRowView.swift
  - HotAndColdFriends/Features/Animations/WeatherAnimationView.swift
  - HotAndColdFriends/Core/Auth/AuthManager.swift
  - HotAndColdFriends/Features/Profile/ProfileView.swift
  - project.yml
autonomous: true
requirements:
  - WTHR-02
  - AUTH-05

must_haves:
  truths:
    - "Animerade väderillustrationer visas bakom vännens profilbild i FriendRowView"
    - "5 vädertyper hanteras: sol, moln, regn, snö, åska"
    - "Animationer stängs av automatiskt om iOS Reduce Motion är aktiverat"
    - "Användare kan radera sitt konto via ProfilView"
    - "Konto-radering tar bort: Firestore-profil, vänner, konversationer, meddelanden, profilbild i Storage, och Firebase Auth-konto"
    - "Apple Sign In-användare får token revocation vid konto-radering"
    - "Re-autentisering triggas automatiskt om Firebase kräver det"
  artifacts:
    - path: "HotAndColdFriends/Features/Animations/WeatherAnimationView.swift"
      provides: "WeatherCondition enum + WeatherAnimationView + partikelanimationer (sol, moln, regn, snö, åska)"
      contains: "WeatherAnimationView"
    - path: "HotAndColdFriends/Features/FriendList/FriendRowView.swift"
      provides: "ZStack med WeatherAnimationView bakom profilbild"
      contains: "WeatherAnimationView"
    - path: "HotAndColdFriends/Core/Auth/AuthManager.swift"
      provides: "deleteAccount() med Firestore/Storage cleanup + Apple token revocation + provider-specifik re-auth"
      contains: "deleteAccount"
    - path: "HotAndColdFriends/Features/Profile/ProfileView.swift"
      provides: "Radera konto-knapp med bekräftelsedialog"
      contains: "Radera konto"
  key_links:
    - from: "HotAndColdFriends/Features/FriendList/FriendRowView.swift"
      to: "WeatherAnimationView"
      via: "ZStack med animationslager bakom profilbild"
      pattern: "WeatherAnimationView"
    - from: "HotAndColdFriends/Features/Profile/ProfileView.swift"
      to: "AuthManager.deleteAccount()"
      via: "Button + Task { await authManager.deleteAccount() }"
      pattern: "deleteAccount"
    - from: "HotAndColdFriends/Core/Auth/AuthManager.swift"
      to: "Firestore/Storage/Auth cleanup"
      via: "cleanupUserData() + revokeAppleToken() + user.delete()"
      pattern: "cleanupUserData"
---

<objective>
Implementera animerade väderillustrationer (WTHR-02) och konto-radering (AUTH-05) — de två kvarvarande kraven som inte berör widget-target.

Purpose: Väderanimationer skapar den visuella livfullhet som differentierar appen. Konto-radering är ett App Store-obligatoriskt krav sedan 2023 — utan det avvisas appen.

Output: WeatherAnimationView med 5 vädertyper, integrerad i FriendRowView. deleteAccount() i AuthManager med full dataradering, integrerad i ProfileView.
</objective>

<execution_context>
@/Users/richardsandenskog/.claude/get-shit-done/workflows/execute-plan.md
@/Users/richardsandenskog/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-polish-app-store/06-RESEARCH.md

@HotAndColdFriends/Features/FriendList/FriendRowView.swift
@HotAndColdFriends/Features/Profile/ProfileView.swift
@HotAndColdFriends/Core/Auth/AuthManager.swift
@HotAndColdFriends/Models/FriendWeather.swift
@HotAndColdFriends/Models/Friend.swift
@HotAndColdFriends/Services/FriendService.swift
@HotAndColdFriends/Services/UserService.swift
@HotAndColdFriends/App/HotAndColdFriendsApp.swift

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From HotAndColdFriends/Models/FriendWeather.swift:
```swift
struct FriendWeather: Identifiable {
    let friend: Friend
    let weather: CurrentWeather?
    var id: String { friend.id ?? UUID().uuidString }
    var temperatureCelsius: Double?
    var symbolName: String  // SF Symbol-namn från WeatherKit, t.ex. "sun.max.fill", "cloud.rain.fill"
}
```

From HotAndColdFriends/Features/FriendList/FriendRowView.swift:
```swift
struct FriendRowView: View {
    let friendWeather: FriendWeather
    // profileImage: @ViewBuilder — ZStack med AsyncImage/initialsCircle, 40x40, clipShape(Circle())
}
extension Color {
    static func temperatureColor(celsius: Double) -> Color
}
```

From HotAndColdFriends/Core/Auth/AuthManager.swift:
```swift
@Observable @MainActor
class AuthManager: NSObject {
    var currentUser: AppUser?
    var authState: AuthState
    func signOut()
    func signInWithApple() async throws
    func signInWithGoogle() async throws
    func signInWithFacebook() async throws
    // deleteAccount() SAKNAS — ska implementeras
    // Private helpers: randomNonceString(), sha256(), rootViewController()
    // Har redan: currentNonce, appleSignInContinuation (för Apple Sign In async/await-brygga)
}
```

From HotAndColdFriends/Features/Profile/ProfileView.swift:
```swift
struct ProfileView: View {
    let uid: String
    @Environment(AuthManager.self) private var authManager
    @Environment(UserService.self) private var userService
    // isOwnProfile: Bool — true om authManager.currentUser?.id == uid
    // Redigera-knapp visas i isOwnProfile-blocket — konto-radering läggs nedanför
}
```

From HotAndColdFriends/Core/Auth/AuthState.swift:
```swift
enum AuthState {
    case unauthenticated
    case authenticating
    case authenticated
    case needsOnboarding
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Skapa WeatherAnimationView och integrera i FriendRowView</name>
  <files>
    HotAndColdFriends/Features/Animations/WeatherAnimationView.swift,
    HotAndColdFriends/Features/FriendList/FriendRowView.swift,
    project.yml
  </files>
  <action>
**1. Skapa katalogen `HotAndColdFriends/Features/Animations/` och filen `WeatherAnimationView.swift`:**

Filen ska innehålla:

**a) WeatherCondition enum:**
```swift
enum WeatherCondition {
    case sun, clouds, rain, snow, thunder

    static func from(symbolName: String) -> WeatherCondition {
        if symbolName.contains("sun") || symbolName.contains("clear") { return .sun }
        if symbolName.contains("cloud") && !symbolName.contains("rain") && !symbolName.contains("snow") && !symbolName.contains("thunder") { return .clouds }
        if symbolName.contains("rain") || symbolName.contains("drizzle") { return .rain }
        if symbolName.contains("snow") || symbolName.contains("sleet") { return .snow }
        if symbolName.contains("thunder") || symbolName.contains("lightning") { return .thunder }
        return .clouds  // fallback
    }
}
```

**b) WeatherAnimationView** — huvudkomponent som delegerar till rätt animation:
```swift
struct WeatherAnimationView: View {
    let condition: WeatherCondition
    @Environment(\.accessibilityReduceMotion) private var reduceMotion

    var body: some View {
        if reduceMotion {
            // Statisk ikon — ingen animation vid Reduce Motion
            staticIcon
        } else {
            switch condition {
            case .sun: SunPulseView()
            case .clouds: CloudDriftView()
            case .rain: ParticleAnimationView(type: .rain)
            case .snow: ParticleAnimationView(type: .snow)
            case .thunder: ThunderFlashView()
            }
        }
    }

    private var staticIcon: some View {
        // SF Symbol som fallback vid Reduce Motion
        Image(systemName: sfSymbol)
            .font(.system(size: 16))
            .foregroundStyle(.white.opacity(0.5))
    }

    private var sfSymbol: String {
        switch condition {
        case .sun: return "sun.max.fill"
        case .clouds: return "cloud.fill"
        case .rain: return "cloud.rain.fill"
        case .snow: return "cloud.snow.fill"
        case .thunder: return "cloud.bolt.fill"
        }
    }
}
```

**c) SunPulseView** — subtil pulsande sol med gulaktig glöd:
- Bakgrundscirkel med `.yellow.opacity(0.15)` som pulserar `scaleEffect` 1.0 → 1.15 med `.easeInOut(duration: 1.5).repeatForever(autoreverses: true)`
- Använd `.onAppear` för att starta animationen

**d) CloudDriftView** — två moln som flyter horisontellt:
- Använd Canvas + TimelineView(.animation) för två molnformer (ellipser) som rör sig långsamt horisontellt
- Molnen ska vara vita med opacity 0.3–0.5
- Hastighet: ~20 sekunder per full cykel (subtilt)

**e) ParticleAnimationView** — partiklar för regn och snö:
- Enum `ParticleType { case rain, snow }`
- Använd Canvas + TimelineView(.animation)
- 15–20 partiklar med slumpmässiga x-positioner (startX) och varaktigheter (duration)
- Regn: capsule-former (2x6pt), vita med opacity 0.6, faller vertikalt
- Snö: cirklar (3x3pt), vita med opacity 0.5, faller långsammare med liten horisontell drift
- Partiklar loopar baserat på `timeline.date.timeIntervalSinceReferenceDate.truncatingRemainder(dividingBy: particle.duration)`

**f) ThunderFlashView** — blixtar med periodisk flash:
- Bakgrund: mörk mörkgrå cirkel med opacity 0.2
- Flash-effekt: `.white.opacity(flashOpacity)` som periodiskt blixtar (0 → 0.4 → 0 under ~0.15s) var 3–4 sekund
- Använd `Timer.publish` eller `TimelineView` med manuell timing

**g) Particle struct** (privat, används av ParticleAnimationView):
```swift
private struct Particle: Identifiable {
    let id = UUID()
    let startX: CGFloat      // 0...1 normaliserad x-position
    let duration: Double      // sekunder per loop (variation: 1.5–3.0)
    let size: CGFloat         // partikelstorlek
    let drift: CGFloat        // horisontell drift (0 för regn, ±0.1 för snö)

    static func initial(count: Int, type: ParticleType) -> [Particle] {
        (0..<count).map { _ in
            Particle(
                startX: CGFloat.random(in: 0...1),
                duration: type == .rain ? Double.random(in: 0.8...1.5) : Double.random(in: 2.0...4.0),
                size: type == .rain ? 2 : 3,
                drift: type == .rain ? 0 : CGFloat.random(in: -0.1...0.1)
            )
        }
    }
}
```

**Alla animationer ska vara:**
- Klippta till 40x40 cirkel (`.frame(width: 40, height: 40).clipShape(Circle())`)
- Subtila — inte distrahera från innehållet
- Energisnåla — undvik onödiga redraws

**2. Modifiera FriendRowView.swift — integrera WeatherAnimationView bakom profilbilden:**

Ändra `profileImage` @ViewBuilder till att wrapoa i en ZStack:

```swift
@ViewBuilder
private var profileImage: some View {
    ZStack {
        // Animationslagret bakom
        WeatherAnimationView(condition: WeatherCondition.from(symbolName: friendWeather.symbolName))
            .frame(width: 40, height: 40)
            .clipShape(Circle())

        // Profilbilden ovanpå (befintlig kod — oförändrad)
        if let urlString = friendWeather.friend.photoURL, let url = URL(string: urlString) {
            AsyncImage(url: url) { phase in
                switch phase {
                case .success(let image):
                    image.resizable().scaledToFill()
                default:
                    initialsCircle
                }
            }
        } else {
            initialsCircle
        }
    }
}
```

**Viktigt:** Profilbilden (AsyncImage) ska vara ovanpå med viss genomskinlighet ELLER animationen ska vara synlig runt kanterna. Prova: `.opacity(0.85)` på profilbilden så att animationen syns igenom svagt, ELLER lägg animationen som en ring/border runt profilbilden istället. Välj det som ser bäst ut. Om animationen bakom en opak profilbild inte syns alls, justera — antingen via `.blendMode(.overlay)` på animationen eller genom att profilbilden får en liten `.padding(3)` innanför clipShape så att animationen syns som en ram.

**3. Kör xcodegen:**
```bash
cd /Users/richardsandenskog/Claude/weatherfriends && xcodegen generate
```
  </action>
  <verify>
    <automated>cd /Users/richardsandenskog/Claude/weatherfriends && xcodegen generate 2>&1 && xcodebuild build -project HotAndColdFriends.xcodeproj -scheme HotAndColdFriends -destination 'platform=iOS Simulator,name=iPhone 16' -quiet 2>&1 | tail -5</automated>
  </verify>
  <done>
    - WeatherAnimationView.swift existerar med WeatherCondition enum och 5 animationstyper
    - FriendRowView.swift har ZStack med WeatherAnimationView bakom profilbild
    - @Environment(\.accessibilityReduceMotion) respekteras — statisk ikon vid Reduce Motion
    - Canvas + TimelineView används för partikelanimationer (regn/snö)
    - Alla animationer klippta till 40x40 cirkel
    - Projektet kompilerar utan fel
  </done>
</task>

<task type="auto">
  <name>Task 2: Implementera konto-radering i AuthManager och ProfileView</name>
  <files>
    HotAndColdFriends/Core/Auth/AuthManager.swift,
    HotAndColdFriends/Features/Profile/ProfileView.swift
  </files>
  <action>
**1. Lägg till DeleteAccountError enum i AuthManager.swift** (ovanför eller nedanför befintliga AuthError):
```swift
enum DeleteAccountError: LocalizedError {
    case noUser
    case requiresRecentLogin
    case appleTokenRevocationFailed
    case deletionFailed(Error)

    var errorDescription: String? {
        switch self {
        case .noUser: return "Inget inloggat konto hittades."
        case .requiresRecentLogin: return "Du behöver logga in igen innan kontot kan raderas."
        case .appleTokenRevocationFailed: return "Kunde inte återkalla Apple-token."
        case .deletionFailed(let error): return "Konto-radering misslyckades: \(error.localizedDescription)"
        }
    }
}
```

**2. Lägg till `deleteAccount()` metod i AuthManager:**

```swift
func deleteAccount() async throws {
    guard let user = Auth.auth().currentUser else {
        throw DeleteAccountError.noUser
    }
    let uid = user.uid

    // 1. Rensa Firestore och Storage INNAN Firebase Auth-radering
    try await cleanupUserData(uid: uid)

    // 2. Apple Sign In kräver token revocation FÖRE user.delete()
    if user.providerData.contains(where: { $0.providerID == "apple.com" }) {
        try await revokeAppleToken()
    }

    // 3. Radera Firebase Auth-konto
    do {
        try await user.delete()
    } catch let error as NSError
        where error.code == AuthErrorCode.requiresRecentLogin.rawValue {
        throw DeleteAccountError.requiresRecentLogin
    } catch {
        throw DeleteAccountError.deletionFailed(error)
    }

    // 4. Rensa lokal state
    self.currentUser = nil
    self.authState = .unauthenticated
}
```

**3. Lägg till `cleanupUserData(uid:)` privat metod i AuthManager:**

Importera `FirebaseStorage` överst i filen (om det inte redan finns).

```swift
private func cleanupUserData(uid: String) async throws {
    let db = Firestore.firestore()

    // 1. Radera vänner (subcollection users/{uid}/friends/*)
    let friendsSnapshot = try await db.collection("users").document(uid)
        .collection("friends").getDocuments()
    // WriteBatch stöder max 500 operationer — chunka om nödvändigt
    let friendDocs = friendsSnapshot.documents
    for chunk in stride(from: 0, to: friendDocs.count, by: 400) {
        let batch = db.batch()
        let end = min(chunk + 400, friendDocs.count)
        for doc in friendDocs[chunk..<end] {
            batch.deleteDocument(doc.reference)
        }
        try await batch.commit()
    }

    // 2. Radera konversationer där uid är deltagare + deras meddelanden
    let convsSnapshot = try await db.collection("conversations")
        .whereField("participants", arrayContains: uid)
        .getDocuments()
    for convDoc in convsSnapshot.documents {
        // Radera meddelanden i subcollection
        let messagesSnapshot = try await convDoc.reference
            .collection("messages").getDocuments()
        for msgChunk in stride(from: 0, to: messagesSnapshot.documents.count, by: 400) {
            let batch = db.batch()
            let end = min(msgChunk + 400, messagesSnapshot.documents.count)
            for msgDoc in messagesSnapshot.documents[msgChunk..<end] {
                batch.deleteDocument(msgDoc.reference)
            }
            try await batch.commit()
        }
        // Radera konversationsdokumentet
        try await convDoc.reference.delete()
    }

    // 3. Radera användarprofil
    try await db.collection("users").document(uid).delete()

    // 4. Radera profilbild i Firebase Storage (try? — bild kanske inte finns)
    let storageRef = Storage.storage().reference().child("profileImages/\(uid)")
    try? await storageRef.delete()
}
```

**4. Lägg till `revokeAppleToken()` privat metod i AuthManager:**

Denna metod använder samma Apple Sign In-flöde som redan finns (ASAuthorizationController + continuation pattern). Den begär en ny authorization utan scopes, enbart för att få authorizationCode:

```swift
private func revokeAppleToken() async throws {
    let nonce = randomNonceString()
    currentNonce = nonce

    let provider = ASAuthorizationAppleIDProvider()
    let request = provider.createRequest()
    request.requestedScopes = []  // Inga scopes — bara authorization code
    request.nonce = sha256(nonce)

    let authorization = try await withCheckedThrowingContinuation { (continuation: CheckedContinuation<ASAuthorization, Error>) in
        self.appleSignInContinuation = continuation
        let controller = ASAuthorizationController(authorizationRequests: [request])
        controller.delegate = self
        controller.presentationContextProvider = self
        controller.performRequests()
    }

    guard let appleIDCredential = authorization.credential as? ASAuthorizationAppleIDCredential,
          let authorizationCode = appleIDCredential.authorizationCode,
          let codeString = String(data: authorizationCode, encoding: .utf8) else {
        throw DeleteAccountError.appleTokenRevocationFailed
    }

    try await Auth.auth().revokeToken(withAuthorizationCode: codeString)
}
```

**5. Lägg till `reauthenticate()` metod i AuthManager** (för requiresRecentLogin-fallet):

```swift
func reauthenticate() async throws {
    guard let user = Auth.auth().currentUser else {
        throw DeleteAccountError.noUser
    }
    let providerID = user.providerData.first?.providerID ?? ""

    if providerID.contains("apple") {
        try await signInWithApple()
    } else if providerID.contains("google") {
        try await signInWithGoogle()
    } else if providerID.contains("facebook") {
        try await signInWithFacebook()
    }
}
```

**6. Modifiera ProfileView.swift — lägg till konto-radering i isOwnProfile-blocket:**

Lägg till state-variabler:
```swift
@State private var showDeleteConfirmation = false
@State private var isDeletingAccount = false
@State private var showReauthAlert = false
@State private var deleteError: String?
```

Längst ner i `isOwnProfile`-blocket (EFTER redigera-knappen, FÖRE `Spacer(minLength: 32)`), lägg till:

```swift
// Konto-radering
Section {
    Button(role: .destructive) {
        showDeleteConfirmation = true
    } label: {
        if isDeletingAccount {
            ProgressView()
                .frame(maxWidth: .infinity)
                .padding(.vertical, 10)
        } else {
            Label("Radera konto", systemImage: "trash")
                .font(.subheadline.weight(.medium))
                .frame(maxWidth: .infinity)
                .padding(.vertical, 10)
        }
    }
    .disabled(isDeletingAccount)
}
.padding(.horizontal)
.padding(.top, 24)
```

Lägg till alerts (som modifiers på VStack eller ScrollView):

```swift
.alert("Radera konto?", isPresented: $showDeleteConfirmation) {
    Button("Radera", role: .destructive) {
        Task { await performDeleteAccount() }
    }
    Button("Avbryt", role: .cancel) {}
} message: {
    Text("Ditt konto och all din data raderas permanent. Åtgärden kan inte ångras.")
}
.alert("Inloggning krävs", isPresented: $showReauthAlert) {
    Button("Logga in igen") {
        Task {
            do {
                try await authManager.reauthenticate()
                // Försök radera igen efter re-auth
                await performDeleteAccount()
            } catch {
                deleteError = error.localizedDescription
            }
        }
    }
    Button("Avbryt", role: .cancel) {}
} message: {
    Text("Av säkerhetsskäl behöver du logga in igen innan kontot kan raderas.")
}
.alert("Fel", isPresented: Binding(
    get: { deleteError != nil },
    set: { if !$0 { deleteError = nil } }
)) {
    Button("OK") { deleteError = nil }
} message: {
    Text(deleteError ?? "")
}
```

Lägg till privat metod i ProfileView:
```swift
private func performDeleteAccount() async {
    isDeletingAccount = true
    defer { isDeletingAccount = false }
    do {
        try await authManager.deleteAccount()
        // authState sätts till .unauthenticated av deleteAccount() →
        // AppRouter navigerar automatiskt till LoginView
    } catch let error as DeleteAccountError where error == .requiresRecentLogin {
        showReauthAlert = true
    } catch {
        deleteError = error.localizedDescription
    }
}
```

**OBS:** `DeleteAccountError.requiresRecentLogin` jämförelse kräver Equatable-conformance. Lägg till `: Equatable` ELLER matcha via `if case .requiresRecentLogin = error`. Enklast: använd `if case`-matching:
```swift
} catch {
    if case DeleteAccountError.requiresRecentLogin = error {
        showReauthAlert = true
    } else {
        deleteError = error.localizedDescription
    }
}
```

**7. Importera FirebaseStorage i AuthManager.swift** om det inte redan finns:
```swift
import FirebaseStorage
```
  </action>
  <verify>
    <automated>cd /Users/richardsandenskog/Claude/weatherfriends && xcodegen generate 2>&1 && xcodebuild build -project HotAndColdFriends.xcodeproj -scheme HotAndColdFriends -destination 'platform=iOS Simulator,name=iPhone 16' -quiet 2>&1 | tail -5</automated>
  </verify>
  <done>
    - AuthManager har deleteAccount() som raderar all användardata
    - cleanupUserData() raderar Firestore-profil, vänner, konversationer, meddelanden och Storage-profilbild
    - revokeAppleToken() kallas för Apple Sign In-användare
    - reauthenticate() stöder alla tre providers (Apple, Google, Facebook)
    - ProfileView visar "Radera konto"-knapp med röd destruktiv stil
    - Bekräftelsedialog visas innan radering
    - Re-autentiserings-alert visas om Firebase kräver ny inloggning
    - ProgressView visas under radering
    - authState sätts till .unauthenticated efter lyckad radering
    - Projektet kompilerar utan fel
  </done>
</task>

</tasks>

<verification>
1. `xcodebuild build` kompilerar utan fel
2. WeatherAnimationView visar korrekt animation baserat på symbolName
3. FriendRowView har animationslager bakom profilbild
4. Reduce Motion: statisk ikon istället för animation
5. ProfileView visar "Radera konto"-knapp (bara för egen profil)
6. Bekräftelsedialog med "Ditt konto och all din data raderas permanent"
7. AuthManager.deleteAccount() finns med cleanupUserData + revokeAppleToken
</verification>

<success_criteria>
- WTHR-02 uppfyllt: Animerade väderillustrationer bakom vännens profilbild med 5 vädertyper
- AUTH-05 uppfyllt: Användare kan radera sitt konto med full dataradering och Apple token revocation
- Reduce Motion respekteras
- Projektet kompilerar och körs
</success_criteria>

<output>
After completion, create `.planning/phases/06-polish-app-store/06-01-SUMMARY.md`
</output>
