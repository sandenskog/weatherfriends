---
phase: 07-tech-debt-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - HotAndColdFriends/Features/Chat/ConversationListView.swift
  - HotAndColdFriends/Features/Chat/ConversationListViewModel.swift
  - HotAndColdFriends/Models/WidgetFriendEntry+AppExtension.swift
autonomous: true
requirements:
  - CHAT-01
  - CHAT-02

must_haves:
  truths:
    - "ConversationListView injicerar FriendService via @Environment istället för throwaway-instans"
    - "ConversationListViewModel tar emot UserService som parameter istället för att äga en egen instans"
    - "Dead code from(friendWeather:) existerar inte längre i kodbasen"
    - "Debug-print saknas i FriendListViewModel (redan uppfyllt — verifieras)"
    - "05-02-SUMMARY.md inkluderar VIEW-02 och PUSH-02 i frontmatter (redan uppfyllt — verifieras)"
  artifacts:
    - path: "HotAndColdFriends/Features/Chat/ConversationListView.swift"
      provides: "@Environment(FriendService.self) + @Environment(UserService.self) injicerade korrekt"
      contains: "@Environment(FriendService.self)"
    - path: "HotAndColdFriends/Features/Chat/ConversationListViewModel.swift"
      provides: "UserService via parameter-injection i load() och refreshUsersMapIfNeeded()"
      contains: "userService: UserService"
  key_links:
    - from: "HotAndColdFriends/Features/Chat/ConversationListView.swift"
      to: "ConversationListViewModel.load()"
      via: "@Environment services skickas som parametrar till viewModel.load()"
      pattern: "viewModel.load.*friendService.*userService"
    - from: "HotAndColdFriends/Features/Chat/ConversationListView.swift"
      to: "ConversationListViewModel.refreshUsersMapIfNeeded()"
      via: "@Environment userService skickas till refreshUsersMapIfNeeded()"
      pattern: "refreshUsersMapIfNeeded.*userService"
---

<objective>
Eliminera tech debt identifierad i v1.0 milestone audit: fixa DI-brott i ConversationListView/ViewModel och ta bort dead code i WidgetFriendEntry+AppExtension.swift. Verifiera att redan fixade items (debug-print, 05-02-SUMMARY frontmatter) faktiskt är klara.

Purpose: Korrekt DI säkerställer att chat-funktionalitet (CHAT-01, CHAT-02) använder delade service-instanser istället för throwaway-objekt — konsekvent med hela appens @Environment-mönster. Dead code removal håller kodbasen ren.

Output: ConversationListView/ViewModel med korrekt DI, WidgetFriendEntry+AppExtension.swift borttagen.
</objective>

<execution_context>
@/Users/richardsandenskog/.claude/get-shit-done/workflows/execute-plan.md
@/Users/richardsandenskog/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-tech-debt-cleanup/07-RESEARCH.md

@HotAndColdFriends/Features/Chat/ConversationListView.swift
@HotAndColdFriends/Features/Chat/ConversationListViewModel.swift
@HotAndColdFriends/Models/WidgetFriendEntry+AppExtension.swift
@HotAndColdFriends/App/HotAndColdFriendsApp.swift

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From HotAndColdFriends/App/HotAndColdFriendsApp.swift:
```swift
// Alla services skapas som @State och injiceras i environment-kedjan:
@State private var userService: UserService
@State private var friendService: FriendService
// ...
.environment(userService)
.environment(friendService)
// UserService och FriendService är REDAN tillgängliga via @Environment i alla vyer
```

From HotAndColdFriends/Features/Chat/ConversationListView.swift (NUVARANDE — ska ändras):
```swift
// Rad 41 — THROWAWAY FriendService():
await viewModel.load(uid: currentUid, chatService: chatService, friendService: FriendService())

// Rad 44-49 — refreshUsersMapIfNeeded utan userService:
await viewModel.refreshUsersMapIfNeeded(
    conversations: newConversations,
    currentUid: currentUid
)
```

From HotAndColdFriends/Features/Chat/ConversationListViewModel.swift (NUVARANDE — ska ändras):
```swift
// Rad 14 — BRYTER DI-MÖNSTRET:
private var userService = UserService()

// Rad 16 — load() saknar userService-parameter:
func load(uid: String, chatService: ChatService, friendService: FriendService) async

// Rad 32 — refreshUsersMapIfNeeded saknar userService-parameter:
func refreshUsersMapIfNeeded(conversations: [Conversation], currentUid: String) async

// Rad 36 — refreshUsersMap saknar userService-parameter:
private func refreshUsersMap(conversations: [Conversation], currentUid: String) async
```

From HotAndColdFriends/Features/FriendList/FriendsTabView.swift (referensexempel):
```swift
// Korrekt DI-mönster att följa:
@Environment(FriendService.self) private var friendService
@State private var viewModel = FriendListViewModel()

// .task{}:
await viewModel.load(uid: uid, friendService: friendService, weatherService: weatherService)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fixa DI i ConversationListView och ConversationListViewModel</name>
  <files>
    HotAndColdFriends/Features/Chat/ConversationListView.swift,
    HotAndColdFriends/Features/Chat/ConversationListViewModel.swift
  </files>
  <action>
**1. Modifiera ConversationListViewModel.swift — ta bort privat UserService-instans, lägg till userService-parameter i alla metoder:**

a) **Ta bort rad 14:** `private var userService = UserService()`

b) **Ändra `load()` signaturen** — lägg till `userService: UserService`:
```swift
func load(uid: String, chatService: ChatService, friendService: FriendService, userService: UserService) async {
```
Och uppdatera anropet till `refreshUsersMap` inuti `load()`:
```swift
await refreshUsersMap(conversations: chatService.conversations, currentUid: uid, userService: userService)
```

c) **Ändra `refreshUsersMapIfNeeded()` signaturen** — lägg till `userService: UserService`:
```swift
func refreshUsersMapIfNeeded(conversations: [Conversation], currentUid: String, userService: UserService) async {
    await refreshUsersMap(conversations: conversations, currentUid: currentUid, userService: userService)
}
```

d) **Ändra `refreshUsersMap()` signaturen** — lägg till `userService: UserService`:
```swift
private func refreshUsersMap(conversations: [Conversation], currentUid: String, userService: UserService) async {
```
Metoden refererar redan till `userService.fetchUser(uid:)` — den behöver bara använda parametern istället för den borttagna propertyn.

**2. Modifiera ConversationListView.swift — lägg till @Environment för FriendService och UserService:**

a) **Lägg till** `@Environment`-deklarationer efter befintliga `@Environment`-rader:
```swift
@Environment(FriendService.self) private var friendService
@Environment(UserService.self) private var userService
```

b) **Ändra `.task{}`-blocket** (rad 39-42) — använd injicerade services istället för throwaway:
```swift
.task {
    guard !currentUid.isEmpty else { return }
    await viewModel.load(uid: currentUid, chatService: chatService, friendService: friendService, userService: userService)
}
```

c) **Ändra `.onChange(of: chatService.conversations)`-blocket** (rad 43-49) — skicka med userService:
```swift
.onChange(of: chatService.conversations) { _, newConversations in
    Task {
        await viewModel.refreshUsersMapIfNeeded(
            conversations: newConversations,
            currentUid: currentUid,
            userService: userService
        )
    }
}
```

**3. Kör xcodegen och verifiera kompilering:**
```bash
cd /Users/richardsandenskog/Claude/weatherfriends && xcodegen generate
```
  </action>
  <verify>
    <automated>cd /Users/richardsandenskog/Claude/weatherfriends && xcodegen generate 2>&1 && xcodebuild build -project HotAndColdFriends.xcodeproj -scheme HotAndColdFriends -destination 'platform=iOS Simulator,name=iPhone 16' -quiet 2>&1 | tail -5</automated>
  </verify>
  <done>
    - ConversationListView har `@Environment(FriendService.self) private var friendService`
    - ConversationListView har `@Environment(UserService.self) private var userService`
    - ConversationListView skickar `friendService` (ej `FriendService()`) till viewModel.load()
    - ConversationListView skickar `userService` till viewModel.refreshUsersMapIfNeeded()
    - ConversationListViewModel har INTE `private var userService = UserService()`
    - ConversationListViewModel.load() tar `userService: UserService` som parameter
    - ConversationListViewModel.refreshUsersMapIfNeeded() tar `userService: UserService` som parameter
    - Projektet kompilerar utan fel
  </done>
</task>

<task type="auto">
  <name>Task 2: Ta bort dead code och verifiera redan fixade items</name>
  <files>
    HotAndColdFriends/Models/WidgetFriendEntry+AppExtension.swift
  </files>
  <action>
**1. Verifiera att `from(friendWeather:)` INTE anropas någonstans:**
```bash
grep -r "from(friendWeather" HotAndColdFriends/
```
Förväntat resultat: Enda träff ska vara definitionen i WidgetFriendEntry+AppExtension.swift. Om fler träffar hittas — AVBRYT och rapportera.

**2. Ta bort filen `HotAndColdFriends/Models/WidgetFriendEntry+AppExtension.swift`:**
```bash
rm HotAndColdFriends/Models/WidgetFriendEntry+AppExtension.swift
```

**3. Kör xcodegen** (filen plockas automatiskt bort ur projekt eftersom `sources` pekar på hela HotAndColdFriends-mappen):
```bash
cd /Users/richardsandenskog/Claude/weatherfriends && xcodegen generate
```

**4. Verifiera redan fixade items:**

a) **Debug-print i FriendListViewModel.swift** — kontrollera att inga print()-anrop existerar:
```bash
grep -n "print(" HotAndColdFriends/Features/FriendList/FriendListViewModel.swift
```
Förväntat: inga träffar (redan fixat).

b) **05-02-SUMMARY.md frontmatter** — kontrollera att VIEW-02 och PUSH-02 finns i provides:
```bash
grep -A2 "provides:" .planning/phases/05-utokade-vyer/05-02-SUMMARY.md
```
Förväntat: `provides: ["VIEW-02", "PUSH-02"]` (redan fixat).

**5. Verifiera att projektet fortfarande kompilerar:**
```bash
xcodebuild build -project HotAndColdFriends.xcodeproj -scheme HotAndColdFriends -destination 'platform=iOS Simulator,name=iPhone 16' -quiet
```
  </action>
  <verify>
    <automated>cd /Users/richardsandenskog/Claude/weatherfriends && xcodegen generate 2>&1 && xcodebuild build -project HotAndColdFriends.xcodeproj -scheme HotAndColdFriends -destination 'platform=iOS Simulator,name=iPhone 16' -quiet 2>&1 | tail -5</automated>
  </verify>
  <done>
    - WidgetFriendEntry+AppExtension.swift existerar INTE längre
    - `grep -r "from(friendWeather" HotAndColdFriends/` returnerar inga träffar
    - FriendListViewModel.swift saknar print()-anrop (redan fixat — verifierat)
    - 05-02-SUMMARY.md har VIEW-02 och PUSH-02 i provides (redan fixat — verifierat)
    - Projektet kompilerar utan fel
  </done>
</task>

</tasks>

<verification>
1. `xcodebuild build` kompilerar utan fel för HotAndColdFriends-schemat
2. ConversationListView har `@Environment(FriendService.self)` och `@Environment(UserService.self)`
3. ConversationListView skickar injicerade services (ej throwaway-instanser) till viewModel
4. ConversationListViewModel har INTE `private var userService = UserService()`
5. WidgetFriendEntry+AppExtension.swift existerar inte
6. `grep -r "from(friendWeather" HotAndColdFriends/` returnerar noll träffar
7. FriendListViewModel.swift saknar print()-anrop
8. 05-02-SUMMARY.md frontmatter inkluderar VIEW-02 och PUSH-02
</verification>

<success_criteria>
- Alla 5 success criteria från ROADMAP Phase 7 uppfyllda:
  1. ConversationListView använder @Environment FriendService — FIXAD
  2. ConversationListViewModel tar UserService via parameter — FIXAD
  3. Debug-print borttagen ur FriendListViewModel — VERIFIERAD (redan klar)
  4. Dead code from(friendWeather:) borttagen — BORTTAGEN
  5. 05-02-SUMMARY.md inkluderar VIEW-02 och PUSH-02 — VERIFIERAD (redan klar)
- CHAT-01 och CHAT-02 integration quality förbättrad genom korrekt DI
- Projektet kompilerar utan fel
</success_criteria>

<output>
After completion, create `.planning/phases/07-tech-debt-cleanup/07-01-SUMMARY.md`
</output>
