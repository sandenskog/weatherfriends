---
phase: 15-design-system-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - HotAndColdFriends/Features/FriendList/WeatherDetailSheet.swift
  - HotAndColdFriends/Features/Chat/ConversationListView.swift
  - HotAndColdFriends/Features/Profile/FriendProfileView.swift
  - HotAndColdFriends/Features/Profile/EditProfileView.swift
  - HotAndColdFriends/Features/Chat/NewConversationSheet.swift
  - HotAndColdFriends/Features/FriendList/FriendCategoryView.swift
  - HotAndColdFriends/Features/Animations/MotionReducer.swift
autonomous: true
requirements:
  - COMP-06

must_haves:
  truths:
    - "Alla vyer som visar avatar-cirklar använder AvatarView med temperaturzon-gradient — inga grå Color(.systemGray5) initialsCircle() kvarstår"
    - "MotionReducer.swift innehåller bara MotionReducer-structen — inga orphaned MotionReducedModifier eller CrossfadeIfReducedModifier"
    - "Appen bygger utan kompileringsfel efter alla ändringar"
  artifacts:
    - path: "HotAndColdFriends/Features/FriendList/WeatherDetailSheet.swift"
      provides: "AvatarView med temperaturzon-gradient istället för initialsCircle"
      contains: "AvatarView("
    - path: "HotAndColdFriends/Features/Chat/ConversationListView.swift"
      provides: "AvatarView istället för grå initialsCircle"
      contains: "AvatarView("
    - path: "HotAndColdFriends/Features/Profile/FriendProfileView.swift"
      provides: "AvatarView istället för grå initialsCircle"
      contains: "AvatarView("
    - path: "HotAndColdFriends/Features/Profile/EditProfileView.swift"
      provides: "AvatarView istället för grå initialsCircle"
      contains: "AvatarView("
    - path: "HotAndColdFriends/Features/Chat/NewConversationSheet.swift"
      provides: "AvatarView istället för grå initialsCircle"
      contains: "AvatarView("
    - path: "HotAndColdFriends/Features/FriendList/FriendCategoryView.swift"
      provides: "AvatarView istället för grå initialsCircle"
      contains: "AvatarView("
    - path: "HotAndColdFriends/Features/Animations/MotionReducer.swift"
      provides: "Ingen MotionReducedModifier eller CrossfadeIfReducedModifier"
  key_links:
    - from: "WeatherDetailSheet.swift"
      to: "AvatarView"
      via: "AvatarView(displayName:temperatureCelsius:size:photoURL:)"
      pattern: "AvatarView\\("
    - from: "ConversationListView.swift"
      to: "AvatarView"
      via: "AvatarView(displayName:temperatureCelsius:size:photoURL:)"
      pattern: "AvatarView\\("
---

<objective>
Migrera alla kvarvarande initialsCircle()-implementationer till AvatarView och ta bort MotionReducer dead code.

Purpose: COMP-06 kräver att avatarer visar temperaturzon-gradient. Sex vyer har fortfarande egna grå initialsCircle()-implementationer som inte använder design-systemets AvatarView. Dessutom finns MotionReducedModifier och CrossfadeIfReducedModifier kvar i MotionReducer.swift trots att de inte används.
Output: Alla avatarer i appen renderas via AvatarView, MotionReducer.swift är rensat.
</objective>

<execution_context>
@/Users/richardsandenskog/.claude/get-shit-done/workflows/execute-plan.md
@/Users/richardsandenskog/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- AvatarView — the component all views must adopt -->
From HotAndColdFriends/DesignSystem/AvatarView.swift:
```swift
struct AvatarView: View {
    let displayName: String
    let temperatureCelsius: Double?
    var size: CGFloat = 52
    var photoURL: String? = nil
}
```
AvatarView handles photoURL internally (AsyncImage with gradient fallback).
When temperatureCelsius is nil, it falls back to arctic zone gradient.

From HotAndColdFriends/Models/FriendWeather.swift:
```swift
struct FriendWeather: Identifiable {
    let friend: Friend
    let weather: CurrentWeather?
    var temperatureCelsius: Double? { weather?.temperature.converted(to: .celsius).value }
}
```

From HotAndColdFriends/Models/Friend.swift:
```swift
struct Friend: Codable, Identifiable {
    @DocumentID var id: String?
    var displayName: String
    var photoURL: String?
    var city: String
    // ...
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrera alla initialsCircle() till AvatarView</name>
  <files>
    HotAndColdFriends/Features/FriendList/WeatherDetailSheet.swift,
    HotAndColdFriends/Features/Chat/ConversationListView.swift,
    HotAndColdFriends/Features/Profile/FriendProfileView.swift,
    HotAndColdFriends/Features/Profile/EditProfileView.swift,
    HotAndColdFriends/Features/Chat/NewConversationSheet.swift,
    HotAndColdFriends/Features/FriendList/FriendCategoryView.swift
  </files>
  <action>
Ersätt alla initialsCircle()-implementationer med AvatarView i dessa 6 filer. AvatarView hanterar redan AsyncImage+fallback internt, så den ersätter HELA profileImage/avatarImage-blocket (inte bara initialsCircle-grenen).

**WeatherDetailSheet.swift** (har temperaturdata):
- Ersätt hela `profileImage`-computed property (rad ~230-253) med:
  `AvatarView(displayName: friendWeather.friend.displayName, temperatureCelsius: friendWeather.temperatureCelsius, size: 80, photoURL: friendWeather.friend.photoURL)`
- Ta bort `initialsCircle(size:)` och `initials(from:)` funktionerna

**FriendCategoryView.swift** (har temperaturdata via FriendWeatherCard):
- I `FriendWeatherCard`, ersätt avatarImage-blocket (rad ~165-188) med:
  `AvatarView(displayName: friendWeather.friend.displayName, temperatureCelsius: friendWeather.temperatureCelsius, size: 32, photoURL: friendWeather.friend.photoURL)`
- Storlek 32 matchar befintlig `.frame(width: 32, height: 32)`
- Ta bort `initialsCircle` computed property och `initials(from:)` funktion

**ConversationListView.swift** (ingen temperatur, använd nil):
- Ersätt avatarImage-blocket (rad ~158-188) — behåll `isGroup`-grenen (ZStack med person.3.fill) men ersätt `initialsCircle` med:
  `AvatarView(displayName: displayName, temperatureCelsius: nil, size: 40, photoURL: otherUser?.photoURL)`
  Storlek 40 matchar `.frame(width: 40, height: 40)` i befintlig kod.
- Ta bort `initialsCircle` computed property och `initials(from:)` funktion
- VIKTIGT: Behåll grupp-ikonen (person.3.fill) som egen gren — AvatarView är för individuella användare

**FriendProfileView.swift** (ingen temperatur, använd nil):
- Ersätt hela profileImage-blocket (rad ~37-65) med:
  `AvatarView(displayName: friend.displayName, temperatureCelsius: nil, size: 100, photoURL: friend.photoURL)`
- Ta bort `initialsCircle(name:size:)` och `initials(from:)` funktionerna

**EditProfileView.swift** (egen profil, ingen temperatur):
- Ersätt profileImage-blocket med:
  `AvatarView(displayName: displayName, temperatureCelsius: nil, size: 80, photoURL: viewModel.user?.photoURL?.absoluteString)`
  OBS: `viewModel.user?.profileImageURL` är en URL?, AvatarView tar String? — konvertera med `.absoluteString`
  Storlek 80 matchar befintlig `.frame(width: 80, height: 80)`
- Behåll eventuell selectedImage-gren om det finns en image picker
- Ta bort `initialsCircle` computed property och `initials(from:)` funktion

**NewConversationSheet.swift** (Friend-modell, ingen temperatur):
- Ersätt avatarImage-blocket (rad ~178-202) med:
  `AvatarView(displayName: friend.displayName, temperatureCelsius: nil, size: 36, photoURL: friend.photoURL)`
  Storlek 36 matchar `.frame(width: 36, height: 36)` i befintlig kod.
- Ta bort `initialsCircle(name:)` funktion

Varje fil ska efter ändring:
1. Inte ha NÅGON `initialsCircle` funktion/property
2. Inte ha NÅGON `initials(from:)` hjälpfunktion (AvatarView beräknar initialer internt)
3. Använda AvatarView med korrekt displayName, temperatureCelsius (nil om otillgänglig), size och photoURL
  </action>
  <verify>
    <automated>cd /Users/richardsandenskog/Claude/weatherfriends && grep -r "initialsCircle\|Color(.systemGray5)" HotAndColdFriends/ --include="*.swift" | grep -v "Widget" | grep -v ".build/" ; test $? -eq 1 && echo "PASS: No initialsCircle or gray circles remain" || echo "FAIL: Found remaining initialsCircle references"</automated>
  </verify>
  <done>Alla 6 vyer använder AvatarView. Ingen initialsCircle()-implementation kvarstår i HotAndColdFriends/ (widget exkluderad). Appen bygger utan fel.</done>
</task>

<task type="auto">
  <name>Task 2: Ta bort MotionReducer dead code och verifiera build</name>
  <files>HotAndColdFriends/Features/Animations/MotionReducer.swift</files>
  <action>
Ta bort `MotionReducedModifier` och `CrossfadeIfReducedModifier` från MotionReducer.swift. Dessa structs används INGENSTANS i kodbasen (verifierat via grep — enda träffen är definitionsfilen själv).

MotionReducer.swift ska efter ändring vara TOM eller helt borttagen. Kontrollera först om det finns NÅGOT annat i filen som används:
- Om filen BARA innehåller MotionReducedModifier och CrossfadeIfReducedModifier — ta bort hela filen
- Om filen innehåller andra structs/extensions som används — behåll de, ta bort bara dead code

Baserat på nuvarande innehåll (enbart dessa 2 structs) — ta bort hela filen.

Ta även bort filen från Xcode-projektets sources om den refereras i project.yml eller liknande.

Verifiera sedan att projektet bygger:
```bash
cd /Users/richardsandenskog/Claude/weatherfriends && xcodebuild -scheme HotAndColdFriends -destination 'platform=iOS Simulator,name=iPhone 16' build 2>&1 | tail -5
```
  </action>
  <verify>
    <automated>cd /Users/richardsandenskog/Claude/weatherfriends && ! test -f HotAndColdFriends/Features/Animations/MotionReducer.swift && echo "PASS: MotionReducer.swift removed" || echo "FAIL: File still exists"</automated>
  </verify>
  <done>MotionReducer.swift är borttagen. Projektet bygger utan kompileringsfel. Ingen referens till MotionReducedModifier eller CrossfadeIfReducedModifier finns kvar.</done>
</task>

</tasks>

<verification>
1. `grep -r "initialsCircle" HotAndColdFriends/ --include="*.swift"` — inga träffar
2. `grep -r "MotionReducedModifier\|CrossfadeIfReducedModifier" HotAndColdFriends/ --include="*.swift"` — inga träffar
3. `grep -r "AvatarView" HotAndColdFriends/ --include="*.swift"` — träffar i alla 6 migrerade filer + AvatarView.swift + FriendRowView + ProfileView
4. Xcode build lyckas utan fel
</verification>

<success_criteria>
- Alla 6 vyer med initialsCircle() migrerade till AvatarView
- MotionReducer.swift borttagen (dead code)
- Inga kompileringsfel
- COMP-06 uppfyllt: avatarer visar temperaturzon-gradient konsekvent
</success_criteria>

<output>
After completion, create `.planning/phases/15-design-system-cleanup/15-01-SUMMARY.md`
</output>
