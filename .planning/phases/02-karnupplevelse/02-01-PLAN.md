---
phase: 02-karnupplevelse
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - HotAndColdFriends/Models/Friend.swift
  - HotAndColdFriends/Models/FriendWeather.swift
  - HotAndColdFriends/Services/AppWeatherService.swift
  - HotAndColdFriends/Services/FriendService.swift
  - HotAndColdFriends/Services/DemoFriendService.swift
  - HotAndColdFriends/Resources/HotAndColdFriends.entitlements
  - project.yml
autonomous: true
requirements:
  - WTHR-01
  - WTHR-03
  - VIEW-04

user_setup:
  - service: Apple WeatherKit
    why: "WeatherKit kräver aktivering i Apple Developer Portal"
    dashboard_config:
      - task: "Aktivera WeatherKit capability för App ID se.sandenskog.hotandcoldfriends"
        location: "Apple Developer Portal -> Certificates, Identifiers & Profiles -> Identifiers -> se.sandenskog.hotandcoldfriends -> App Services -> WeatherKit (check)"
      - task: "Vänta 30 minuter efter aktivering innan test på device"
        location: "Väntetid krävs av Apple"

must_haves:
  truths:
    - "WeatherKit returnerar aktuellt väder (temperatur, ikon, vind, fuktighet) för en given koordinat"
    - "Väderdata cachas i 30 minuter — upprepad hämtning inom TTL returnerar cachad data utan nytt API-anrop"
    - "Friend-modellen lagrar namn, stad, koordinater, isFavorite och isDemo"
    - "FriendService kan spara, hämta och ta bort vänner i Firestore subcollection users/{uid}/friends/{friendId}"
    - "DemoFriendService levererar 8 fiktiva vänner med internationella städer och riktiga koordinater"
  artifacts:
    - path: "HotAndColdFriends/Services/AppWeatherService.swift"
      provides: "WeatherKit-integration med Actor-baserad TTL-cache"
      min_lines: 60
    - path: "HotAndColdFriends/Models/Friend.swift"
      provides: "Codable Friend-modell med Firestore-stöd"
      min_lines: 15
    - path: "HotAndColdFriends/Models/FriendWeather.swift"
      provides: "Friend + CurrentWeather sammanslaget värde-objekt"
      min_lines: 15
    - path: "HotAndColdFriends/Services/FriendService.swift"
      provides: "Firestore CRUD för friends subcollection"
      min_lines: 40
    - path: "HotAndColdFriends/Services/DemoFriendService.swift"
      provides: "Hårdkodade demo-vänner med riktiga koordinater"
      min_lines: 30
    - path: "HotAndColdFriends/Resources/HotAndColdFriends.entitlements"
      provides: "WeatherKit entitlement"
      contains: "com.apple.developer.weatherkit"
  key_links:
    - from: "HotAndColdFriends/Services/AppWeatherService.swift"
      to: "WeatherKit.WeatherService.shared"
      via: "weather(for:including:) async API"
      pattern: "WeatherKit\\.WeatherService\\.shared\\.weather"
    - from: "HotAndColdFriends/Services/FriendService.swift"
      to: "Firestore users/{uid}/friends"
      via: "Firestore subcollection"
      pattern: "collection.*users.*friends"
    - from: "project.yml"
      to: "HotAndColdFriends/Resources/HotAndColdFriends.entitlements"
      via: "entitlements path reference"
      pattern: "entitlements"
---

<objective>
Bygg WeatherService med Apple WeatherKit (30-min TTL-cache), Friend-modell, FriendService (Firestore CRUD) och DemoFriendService (8 fiktiva vänner med riktiga koordinater). Skapa WeatherKit-entitlements och uppdatera project.yml.

Purpose: Alla data-lager och tjänster som listvy och onboarding-favoriter behöver. Utan dessa existerar inget väder och inga vänner.
Output: 5 nya filer (modeller + services), 1 entitlements-fil, uppdaterad project.yml
</objective>

<execution_context>
@/Users/richardsandenskog/.claude/get-shit-done/workflows/execute-plan.md
@/Users/richardsandenskog/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-karnupplevelse/02-RESEARCH.md

<interfaces>
<!-- Befintliga typer och kontrakt som executor behöver. -->

From HotAndColdFriends/Models/AppUser.swift:
```swift
struct AppUser: Codable, Identifiable {
    @DocumentID var id: String?
    var displayName: String
    var photoURL: String?
    var city: String
    var cityLatitude: Double?
    var cityLongitude: Double?
    var authProvider: String
    @ServerTimestamp var createdAt: Timestamp?
    @ServerTimestamp var updatedAt: Timestamp?
    var profileImageURL: URL? { ... }
}
```

From HotAndColdFriends/Services/UserService.swift:
```swift
@Observable @MainActor class UserService {
    func createUserProfile(_ user: AppUser) async throws
    func fetchUser(uid: String) async throws -> AppUser?
    func updateUser(uid: String, data: [String: Any]) async throws
    func uploadProfileImage(uid: String, imageData: Data) async throws -> String
}
```

From HotAndColdFriends/App/HotAndColdFriendsApp.swift:
```swift
@main struct HotAndColdFriendsApp: App {
    @State private var authManager: AuthManager
    @State private var userService: UserService
    // Nya services behöver injiceras här via .environment()
}
```

From project.yml (targets section):
```yaml
targets:
  HotAndColdFriends:
    type: application
    platform: iOS
    deploymentTarget: "17.0"
    sources:
      - path: HotAndColdFriends
        excludes:
          - "**/@eaDir"
    # Entitlements behöver läggas till här
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Friend-modell, FriendWeather-modell och FriendService</name>
  <files>HotAndColdFriends/Models/Friend.swift, HotAndColdFriends/Models/FriendWeather.swift, HotAndColdFriends/Services/FriendService.swift, HotAndColdFriends/Services/DemoFriendService.swift</files>
  <action>
**1. Skapa Friend.swift** i Models/:
```swift
struct Friend: Codable, Identifiable {
    @DocumentID var id: String?
    var displayName: String
    var photoURL: String?
    var city: String
    var cityLatitude: Double?
    var cityLongitude: Double?
    var isFavorite: Bool
    var isDemo: Bool
    @ServerTimestamp var createdAt: Timestamp?
}
```
- Importera `FirebaseFirestore`
- Codable + Identifiable, samma mönster som AppUser
- `isDemo` default false (sätts bara av DemoFriendService)

**2. Skapa FriendWeather.swift** i Models/:
```swift
import WeatherKit

struct FriendWeather: Identifiable {
    let friend: Friend
    let weather: CurrentWeather

    var id: String { friend.id ?? UUID().uuidString }

    var temperatureCelsius: Double {
        weather.temperature.converted(to: .celsius).value
    }

    var temperatureFormatted: String {
        weather.temperature.converted(to: .celsius)
            .formatted(.measurement(width: .narrow))
    }

    var symbolName: String { weather.symbolName }
    var conditionDescription: String { weather.condition.description }
    var humidity: Double { weather.humidity * 100 }
    var windSpeed: Double { weather.wind.speed.converted(to: .metersPerSecond).value }
}
```
- Importera `WeatherKit` och `Foundation`
- `temperatureCelsius` används för sortering (Double)
- `temperatureFormatted` för visning ("7°")
- `symbolName` för SF Symbol-ikoner (WeatherKit returnerar rätt namn direkt)
- `humidity` som procent (0-100), `windSpeed` i m/s

**3. Skapa FriendService.swift** i Services/:
- `@Observable @MainActor class FriendService`
- Privat Firestore-referens: `private let db = Firestore.firestore()`
- Subcollection-path: `users/{uid}/friends/{friendId}`

Metoder:
- `func fetchFriends(uid: String) async throws -> [Friend]` — hämtar alla dokument från subcollection, returnerar [Friend] sorterat på displayName
- `func addFriend(uid: String, friend: Friend) async throws` — sätter nytt dokument med auto-genererat ID (addDocument). Om `friend.isFavorite == true`, kontrollera att befintliga favoriter < 6 innan sparning — kasta `FriendServiceError.maxFavoritesReached` om 6 redan finns
- `func updateFriend(uid: String, friendId: String, data: [String: Any]) async throws` — uppdaterar specifika fält
- `func removeFriend(uid: String, friendId: String) async throws` — tar bort dokument
- `func toggleFavorite(uid: String, friend: Friend) async throws` — om `friend.isFavorite == true` → sätter till false. Om false → kontrollerar att favoriter < 6, sätter till true. Kastar `FriendServiceError.maxFavoritesReached` vid 7:e.
- `func removeDemoFriends(uid: String) async throws` — hämtar alla vänner med isDemo == true och tar bort dem
- `func favoritesCount(uid: String) async throws -> Int` — query med whereField("isFavorite", isEqualTo: true) + count

Felhantering:
```swift
enum FriendServiceError: LocalizedError {
    case maxFavoritesReached
    case missingFriendID
    var errorDescription: String? { ... }
}
```
- `maxFavoritesReached`: "Du har redan 6 favoriter. Ta bort en för att lägga till en ny."
- `missingFriendID`: "Vän-ID saknas."

**4. Skapa DemoFriendService.swift** i Services/:
- `struct DemoFriendService` (inte class — ren statisk data)
- `static let demoFriends: [Friend]` med 8 vänner (riktiga koordinater):
  1. "Yuki Tanaka" — Tokyo, Japan (35.6762, 139.6503) — isFavorite: true
  2. "Amara Nkosi" — Kapstaden, Sydafrika (-33.9249, 18.4241) — isFavorite: true
  3. "Emma Sullivan" — New York, USA (40.7128, -74.0060) — isFavorite: true
  4. "Oliver Chen" — Sydney, Australien (-33.8688, 151.2093) — isFavorite: false
  5. "Fatima Al-Rashid" — Dubai, UAE (25.2048, 55.2708) — isFavorite: false
  6. "Lars Eriksson" — Stockholm, Sverige (59.3293, 18.0686) — isFavorite: false
  7. "Marie Dubois" — Paris, Frankrike (48.8566, 2.3522) — isFavorite: false
  8. "Carlos Mendez" — Buenos Aires, Argentina (-34.6037, -58.3816) — isFavorite: false
- Alla har `isDemo: true`
- Observera: de tre första (Tokyo, Kapstaden, New York) sätts som favoriter för att demo:n visar sektions-uppdelningen direkt
  </action>
  <verify>
    <automated>cd /Users/richardsandenskog/Claude/weatherfriends && xcodegen generate 2>&1 && xcodebuild -project HotAndColdFriends.xcodeproj -scheme HotAndColdFriends -destination 'platform=iOS Simulator,name=iPhone 16' -quiet build 2>&1 | tail -20</automated>
  </verify>
  <done>Friend.swift och FriendWeather.swift finns i Models/. FriendService.swift med alla CRUD-metoder och max-6-favoritlogik finns i Services/. DemoFriendService.swift med 8 internationella demo-vänner finns i Services/. Projektet kompilerar utan fel.</done>
</task>

<task type="auto">
  <name>Task 2: AppWeatherService med WeatherKit och TTL-cache + entitlements</name>
  <files>HotAndColdFriends/Services/AppWeatherService.swift, HotAndColdFriends/Resources/HotAndColdFriends.entitlements, project.yml, HotAndColdFriends/App/HotAndColdFriendsApp.swift</files>
  <action>
**1. Skapa HotAndColdFriends.entitlements** i Resources/:
```xml
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>com.apple.developer.weatherkit</key>
    <true/>
</dict>
</plist>
```

**2. Uppdatera project.yml** — lägg till entitlements-referens i target-settings:
Under `targets: HotAndColdFriends:` lägg till:
```yaml
    entitlements:
      path: HotAndColdFriends/Resources/HotAndColdFriends.entitlements
      properties:
        com.apple.developer.weatherkit: true
```
Placera detta direkt under `settings:` i target-blocket (på samma nivå som `sources:`, `dependencies:`, `settings:`).

**3. Skapa AppWeatherService.swift** i Services/:

Namnge klassen `AppWeatherService` (INTE `WeatherService`) för att undvika namnkollision med `WeatherKit.WeatherService`.

Intern Actor-cache:
```swift
private actor WeatherCache {
    private struct CachedEntry {
        let weather: CurrentWeather
        let cachedAt: Date
    }
    private var store: [String: CachedEntry] = [:]
    private let ttl: TimeInterval = 30 * 60  // 30 minuter

    func get(key: String) -> CurrentWeather? {
        guard let entry = store[key],
              Date().timeIntervalSince(entry.cachedAt) < ttl else {
            store.removeValue(forKey: key)
            return nil
        }
        return entry.weather
    }

    func set(key: String, weather: CurrentWeather) {
        store[key] = CachedEntry(weather: weather, cachedAt: Date())
    }

    func clear() {
        store.removeAll()
    }
}
```

Huvudklass:
```swift
@Observable
@MainActor
class AppWeatherService {
    private let service = WeatherKit.WeatherService.shared
    private let cache = WeatherCache()

    func currentWeather(latitude: Double, longitude: Double) async throws -> CurrentWeather {
        let key = "\(String(format: "%.3f", latitude)),\(String(format: "%.3f", longitude))"
        if let cached = await cache.get(key: key) {
            return cached
        }
        let location = CLLocation(latitude: latitude, longitude: longitude)
        let weather = try await service.weather(for: location, including: .current)
        await cache.set(key: key, weather: weather)
        return weather
    }

    func detailedWeather(latitude: Double, longitude: Double) async throws -> (CurrentWeather, Forecast<HourWeather>, Forecast<DayWeather>) {
        let location = CLLocation(latitude: latitude, longitude: longitude)
        return try await service.weather(for: location, including: .current, .hourlyForecast, .dailyForecast)
    }

    var attribution: WeatherAttribution {
        get async throws {
            try await WeatherKit.WeatherService.shared.attribution
        }
    }

    func clearCache() async {
        await cache.clear()
    }
}
```

Importera: `WeatherKit`, `CoreLocation`, `Foundation`

Kommentarer om namnkollision: "Döpt AppWeatherService (inte WeatherService) för att undvika namnkollision med WeatherKit.WeatherService"

`currentWeather` — primär metod, använder cache. Avrunda koordinater till 3 decimaler för cache-nyckel (ca 100m precision).
`detailedWeather` — för expanderad vädervy (sheet), UTAN cache (anropas sällan).
`attribution` — Apple Weather-attribution (krävs av Apple).
`clearCache` — för pull-to-refresh.

**4. Uppdatera HotAndColdFriendsApp.swift** — injicera nya services:
- Lägg till `@State private var appWeatherService: AppWeatherService` och `@State private var friendService: FriendService`
- I `init()` (efter FirebaseApp.configure()): initiera `_appWeatherService = State(initialValue: AppWeatherService())` och `_friendService = State(initialValue: FriendService())`
- I body, kedja `.environment(appWeatherService)` och `.environment(friendService)` på AppRouter()

**5. Kör xcodegen** efter alla filändringar: `xcodegen generate`
  </action>
  <verify>
    <automated>cd /Users/richardsandenskog/Claude/weatherfriends && xcodegen generate 2>&1 && xcodebuild -project HotAndColdFriends.xcodeproj -scheme HotAndColdFriends -destination 'platform=iOS Simulator,name=iPhone 16' -quiet build 2>&1 | tail -20</automated>
  </verify>
  <done>AppWeatherService.swift med WeatherKit-integration och 30-min Actor TTL-cache finns i Services/. HotAndColdFriends.entitlements med WeatherKit capability finns i Resources/. project.yml refererar entitlements-filen. HotAndColdFriendsApp injicerar AppWeatherService och FriendService via .environment(). Projektet kompilerar utan fel.</done>
</task>

</tasks>

<verification>
1. `xcodegen generate` lyckas utan varningar
2. `xcodebuild build` kompilerar utan fel
3. Friend.swift, FriendWeather.swift finns i Models/
4. AppWeatherService.swift, FriendService.swift, DemoFriendService.swift finns i Services/
5. HotAndColdFriends.entitlements finns med WeatherKit-nyckel
6. project.yml har entitlements-referens
7. HotAndColdFriendsApp.swift injicerar AppWeatherService och FriendService
</verification>

<success_criteria>
- Friend-modellen med alla fält (displayName, city, coordinates, isFavorite, isDemo) existerar och är Codable
- FriendWeather sammanslår Friend + CurrentWeather med temperatureCelsius för sortering och symbolName för ikoner
- AppWeatherService hämtar väder via WeatherKit med 30-min TTL-cache
- FriendService hanterar Firestore CRUD i subcollection users/{uid}/friends/ med max 6 favoriter
- DemoFriendService levererar 8 fiktiva vänner med riktiga koordinater
- Entitlements-fil korrekt konfigurerad och refererad i project.yml
- Alla nya services injicerade i SwiftUI-miljön
</success_criteria>

<output>
After completion, create `.planning/phases/02-karnupplevelse/02-01-SUMMARY.md`
</output>
