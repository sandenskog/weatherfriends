import SwiftUI

// Placeholder — implementeras i Plan 04-02
struct ConversationListView: View {
    @Binding var openConversationId: String?

    var body: some View {
        Text("Chattar — kommer snart")
            .navigationTitle("Chattar")
    }
}
